# ARC-010.slice-01 · PPC → cache canônico de obras

**Onda:** 6  
**Achado-raiz:** ARC-010 (consumo direto de API fora do cache canônico)

## Escopo
- `src/pages/planejamento/PPC.tsx`: remove `obrasRepo.listResumo()` do `Promise.all`; substitui `useState<Obra[]>` local por derivação de `useObras().data` (mapeado para `{id, nome}`). Import de `obrasRepo` removido.

## Piloto do padrão ARC-010
Estabelece receita para migração das demais páginas (`suprimentos/*`, `planejamento/*`) que consumiam `obrasRepo.listResumo()` em fetch paralelo local: consumir `useObras()` como fonte única e reduzir o `Promise.all` para dados específicos da página.

## Validação
- `tsgo --noEmit` limpo.
- Comportamento preservado: `obras` continua alimentando `obraByid`, filtros e selects.
