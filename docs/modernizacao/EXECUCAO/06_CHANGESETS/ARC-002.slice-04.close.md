# ARC-002.slice-04.close — Encerramento do slice de Contratos

**Onda:** 6 · **Prioridade:** P1 · **Tipo:** REF/MIGRAÇÃO
**Achado:** ARC-002 (god-context)

## Consumers migrados

- `src/pages/Contratos.tsx`
- `src/pages/QuadroContratos.tsx`
- `src/components/contratos/ContratoProfileDialog.tsx`

`useApp()` mantido apenas para `obras`, `hasAccess` e `currentPlayer`. Agregadores (`useAppRead`, `useCatalogos`) inalterados nesta etapa — não expõem `contratos` mutáveis.

## Critérios preservados

- Sem alteração de UI, mobilização, aditivos, importação/exportação ou persistência.
- `useApp()` mantém compatibilidade para consumers não migrados.
- Typecheck (`tsgo`) verde.

## Próximo

Abrir `ARC-002.slice-05` com `ObrasContext`, replicando o padrão de camada de compatibilidade.
