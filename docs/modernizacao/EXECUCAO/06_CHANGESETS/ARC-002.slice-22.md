# ARC-002 · slice-22

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Escopo
1. Consumidores diretos restantes de `useApp()` migrados quando eram auth/perms-only:
   - `src/pages/rh/Colaboradores.tsx`
   - `src/pages/crm/CRMFunil.tsx`
   - `src/lib/authz/index.ts`
2. Consumidores restantes de `useAppRead()` que liam apenas `currentPlayer` migrados para `useAuth()`:
   - `src/pages/suprimentos/Orcamento.tsx`
   - `src/pages/InspecoesQR.tsx`
   - `src/pages/InspecoesAgenda.tsx`
3. Consumidores restantes de `useAppRead()` que liam `hasAccess`/`currentPlayer` migrados para `usePermissions()`:
   - `src/components/layout/NotificationBell.tsx`
   - `src/pages/MobilizacaoProvisoria.tsx`
   - `src/pages/ControleDespesas.tsx`
   - `src/pages/AprovacaoFinanceira.tsx`

## Validação
- `tsgo --noEmit`: sem erros.

## Observação
- Fora das fachadas/contextos de compatibilidade, `useApp()` fica sem consumidores diretos de tela para auth/perms; próximos slices podem focar em remover dependências internas das fachadas ou domiciliar mutações remanescentes.