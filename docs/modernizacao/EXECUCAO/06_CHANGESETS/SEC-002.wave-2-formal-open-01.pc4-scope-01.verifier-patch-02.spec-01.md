# SEC-002.wave-2-formal-open-01.pc4-scope-01.verifier-patch-02.spec-01 — Gate operacional pós-abertura

**Onda:** 2 (OPEN — reativação do verificador operacional)
**Data:** 2026-07-12
**Depende de:** R127 (abertura formal), R128/R129 (self-register da abertura), R116 (constante `EXPECTED_INDEX_ROWS`).

## 1. Escopo

Contratar o patch mínimo que torna `scripts/sec002/verify-wave-2-preconditions.mjs` novamente executável após a abertura formal da Onda 2.

O verificador deixou de ser gate após R127 por duas inversões esperadas: PC2 ainda exigia `BLOCKED`, embora a onda esteja `OPEN`, e PC4 ainda esperava 29 linhas úteis, embora R129 tenha contratado a linha 30. Este changeset corrige ambas sem alterar produção, workflows ou apply-map.

## 2. Patch permitido

1. `verifyPC2()` passa a exigir `frontmatter.status === "OPEN"`.
2. `verifyPC2()` passa a exigir bloco `## Abertura formal` com `Status atual: OPEN`, `Apply-id: wave-2-formal-open-01` e PC1..PC7 verificados.
3. `EXPECTED_INDEX_ROWS` passa de `29` para `30`.
4. `.github/sec002-evidence/INDEX.md` recebe apenas reparo físico da quebra entre linhas 29 e 30, preservando conteúdo semântico de ambas, e `Última atualização` monotônica.
5. Manifest próprio é gravado em `.github/sec002-evidence/wave-2-formal-open-01-pc4-verifier-patch-02/manifest.json`.

## 3. Invariantes OP1–OP12

| ID | Invariante |
|----|------------|
| OP1 | Nenhum arquivo de produção (`src/**`, funções, migrações) é alterado. |
| OP2 | Nenhum workflow ou apply-map é alterado. |
| OP3 | `verifyPC2()` não aceita mais `BLOCKED` como estado operacional pós-abertura. |
| OP4 | `verifyPC2()` exige evidência textual da abertura formal R127. |
| OP5 | `EXPECTED_INDEX_ROWS` é estático e igual a `30`. |
| OP6 | `verifyPC4()` segue usando a constante na contagem e no laço `1..N`. |
| OP7 | `INDEX.md` preserva schema `v2026-07-12.4`. |
| OP8 | Linhas 1–28 do `INDEX.md` permanecem intocadas. |
| OP9 | Linha 29 preserva o conteúdo canônico anterior; só a quebra física antes da linha 30 é corrigida. |
| OP10 | Linha 30 preserva exatamente o conteúdo contratado por OA2/R128. |
| OP11 | `node scripts/sec002/verify-wave-2-preconditions.mjs --json` deve retornar exit 0 com PC1..PC7. |
| OP12 | Reversão exige errata própria; edição direta posterior do verificador ou índice é rejeição. |

## 4. Critérios de aceitação

1. R130 é registrado no `SEC-001.README.md`.
2. O verificador completo retorna `OK` em modo JSON.
3. O índice passa a ter 30 linhas físicas úteis numeradas `1..30`.
4. Onda 2 permanece `OPEN` e pronta para o primeiro apply autorizado do write set.

## 5. Próximo passo formal

`SEC-002.wave-2-formal-open-01.pc4-scope-01.verifier-patch-02.materialize-01` — aplicar o patch sob OP1–OP12.