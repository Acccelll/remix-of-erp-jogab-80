# ARC-002 · slice-09 · CLOSE — DocumentosContext consumers migrados

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Consumers reais migrados para `useDocumentosContext()`

- `src/pages/Board.tsx` — `getAllExpiringDocuments` agora via `useDocumentosContext()`; `useApp()` mantido para `obras/contratos/hasAccess/currentPlayer`.
- `src/pages/rh/ControleDocumentos.tsx` — `getAllExpiringDocuments` via `useDocumentosContext()`; `useApp()` mantido para `hasAccess`.
- `src/components/layout/NotificationBell.tsx` — `getExpiringDocuments` via `useDocumentosContext()`; `useAppRead()` mantido para `hasAccess/currentPlayer`.

## Notas

- `src/contexts/app/useAppRead.ts` ainda expõe `getExpiringDocuments`/`getAllExpiringDocuments` para compatibilidade; remoção fica para slice futura quando não houver consumers via `useAppRead`.
- `useExpiringDocuments` (hook de cálculo real) permanece inalterado; `DocumentosContext` continua como camada de compat sobre `useApp()`.

## Próximo

Abrir `ARC-002.slice-10` — candidato: `MobilizacoesPendentesContext` (cross-domínio colaboradores/veículos/patrimônios) OU consolidar remoção de `getExpiringDocuments` de `useAppRead` após varredura.
