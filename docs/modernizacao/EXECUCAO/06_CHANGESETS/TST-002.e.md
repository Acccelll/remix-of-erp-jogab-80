# TST-002.e — Cobertura de `financeiro` e `obraDetalhe`

**Onda:** pré-condição da Onda 6 (Stage Gate: TST-002 em curso, ver D-9).  
**Achado:** TST-002 — lote E.

## Contexto

Continuação do destravamento incremental da Onda 6 sem iniciar código de
produção da onda. Este lote fixa contratos de duas superfícies críticas:
financeiro, que alimenta análises e a futura sub-onda 5.F, e `obraDetalhe`,
que concentra escrita/consulta de ocorrências, BMs, marcos, RDO e validações.

## Mudanças

- Novo `src/lib/__tests__/integracao/repositories-contrato-e.test.ts` com 26 casos:
  - `financeiroRepo`: `listLancamentosPorObra`, `listLancamentosPorSnapshot`
    (paginação, filtros opcionais, página vazia, erro), `listMatrizRateios`
    (paginação, erro), `listSnapshots`, `vwObra`.
  - Fatia `obraDetalhe`: `ocorrenciasRepo`, `obraLocalizacoesRepo`,
    `aditivosContratoRepo`, `bmsPrevistasRepo`, `cronogramaMarcosRepo`,
    `rdoEfetivoRepo`, `rdoOcorrenciasRepo`, `importValidationRunsRepo`.

## Validação

- `bunx vitest run src/lib/__tests__/integracao/repositories-contrato-e` → **26/26**.
- Suíte completa: **509/509 verdes** (baseline pós-TST-002.d era 483).
- `bunx tsgo --noEmit` → verde.

## Cobertura acumulada de TST-002

| Lote | Repositories/fatias                                                                                             | Casos |
| ---- | --------------------------------------------------------------------------------------------------------------- | ----- |
| A    | `obrasRepo`, `fornecedoresRepo`                                                                                 | 9     |
| B    | `clientesRepo`, `insumosRepo`                                                                                   | 14    |
| C    | `boardListasRepo`, `cardChecklistItensRepo`, `cardMembrosRepo`, `cardSetoresRepo`                               | 16    |
| D    | `cronogramaRepo`, `cronogramaItemRevisoesRepo`, `requisicoesRepo`                                               | 16    |
| E    | `financeiroRepo`, fatia `obraDetalhe`                                                                           | 26    |

**Total:** 81 casos sobre 13 repositórios/fatias.

Restantes (lotes F+): `cards`, `cardsExtra` (anexos/recursos), `dp`,
`dpHolerite`, `inspecoes`, `planejamento`, `rdo`, `suprimentos`,
`boardCamposRepo`, `boardMembrosRepo`, `cronogramaCenariosRepo`,
`cronogramaCenarioItensRepo`.

## Próximo

Lote F — `cards` + `cardsExtra` (maior acoplamento com DS-011/DS-016 e com
os fluxos de Kanban que serão padronizados na Onda 6).