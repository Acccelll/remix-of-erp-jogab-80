# ARC-003.c.4 — `cronograma_itens`, `cronograma_item_revisoes`, `cronograma_cenarios`, `cronograma_cenario_itens`

## Escopo
Zerar chamadas diretas às tabelas de cronograma fora do repositório `cronograma.ts`.

## Extensões no repositório
- `cronogramaRepo.listIdsPorObra(obraId)`.
- Novo `cronogramaItemRevisoesRepo` — `insertMany`, `removeByRevisao`, `removeByRevisaoIds`.
- Novo `cronogramaCenariosRepo` — `remove`.
- Novo `cronogramaCenarioItensRepo` — `insertMany`.

## Call-sites refatorados (12)
- `CronogramaPrincipalTab.tsx` — insert/delete item + snapshot revisões + update pct.
- `CenariosSheet.tsx` — insert cenário-itens + delete cenário.
- `LimparImportadosButton.tsx` — delete revisões por ids.
- `RevisoesTab.tsx` — update item, insert snapshots, delete snapshots ao reverter.
- `CronogramaImporter.tsx` — bulk insert de itens.
- `CronogramaSemanalImporter.tsx` — insert snapshots + leitura de ids existentes.

## Verificação
- `rg 'supabase.from\("(cronograma_itens|cronograma_item_revisoes|cronograma_cenarios|cronograma_cenario_itens)"\)' src/` retorna apenas ocorrências dentro do próprio `cronograma.ts`.
- `tsgo --noEmit` verde.

## Pendente ARC-003.c.5
Módulo `cards/*` (board_*, card_*) — ~30 call-sites; próximo lote fecha `ARC-003`.
