# ARC-002 · slice-15 — Migração de consumidores restantes `useAppRead()` (batch 5)

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Escopo

Batch 5 da retirada gradual de leituras de domínio de `useAppRead()`/`useApp()`.

## Entregue

- `src/components/frotas/FrotaCustosTabs.tsx` — `obras` via `useObrasContext()`; `useApp()` mantido apenas para `hasAccess`.
- `src/components/layout/CommandPalette.tsx` — `obras` via `useObrasContext()` e `colaboradores` via `useColaboradoresContext()`; `useApp()` mantido apenas para `hasAccess`.
- `src/pages/contratos/Fornecimento.tsx` — `obras` via `useObrasContext()`; `useApp()` mantido apenas para `hasAccess`.
- `src/pages/crm/CRMDashboard.tsx` — `oportunidades` via `useClientesContext()`; removido `useAppRead()`.
- `src/pages/dp/PontoImportar.tsx` — `colaboradores` via `useColaboradoresContext()`; `useApp()` mantido apenas para `currentPlayer`.

## Próximo

`ARC-002.slice-16` — migrar páginas operacionais restantes que ainda leem domínio por `useAppRead()` (`AprovacaoFinanceira`, `ControleDespesas`, `InspecoesAgenda`, `InspecoesQR`, `MobilizacaoProvisoria`, `Orcamento`) em batch controlado.