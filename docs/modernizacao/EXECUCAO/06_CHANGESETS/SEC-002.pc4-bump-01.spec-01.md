# SEC-002 · pc4-bump-01 · spec-01 (R152)

**Onda:** 2 (OPEN) · **Apply-map:** v2026-07-12.4 · **Gate:** restaura PC1..PC7 = green após apply

## Objetivo
Autorizar o bump da constante `EXPECTED_INDEX_ROWS` de `33` → `34` em `scripts/sec002/verify-wave-2-preconditions.mjs`, refletindo o self-register de R151 (linha 34 = `batch-B-apply-01`).

## Write set físico autorizado (R153 — 1 arquivo)
1. `scripts/sec002/verify-wave-2-preconditions.mjs` — alteração pontual da linha 10:
   - antes: `const EXPECTED_INDEX_ROWS = 33;`
   - depois: `const EXPECTED_INDEX_ROWS = 34;`

Nenhuma outra alteração autorizada em R153 (nem comentário, nem import, nem lógica de PC1/PC2/PC3/PC5/PC6/PC7, nem outra ocorrência de `33`).

## Invariantes (PC4B-1 … PC4B-8)
- **PC4B-1** Diff = exatamente 1 linha alterada (linha 10), 0 adições, 0 remoções fora dela.
- **PC4B-2** Nenhuma outra constante numérica alterada no arquivo.
- **PC4B-3** Lógica de PC1, PC2, PC3, PC5, PC6, PC7 preservada byte-a-byte.
- **PC4B-4** Loop `for (let i = 1; i <= EXPECTED_INDEX_ROWS; i += 1)` inalterado; apenas o limite superior varia por consequência.
- **PC4B-5** `Apply-map: v2026-07-12.4` preservado no cabeçalho do `INDEX.md`.
- **PC4B-6** `.github/sec002-evidence/INDEX.md` NÃO é tocado por R153.
- **PC4B-7** Após R153: `node scripts/sec002/verify-wave-2-preconditions.mjs --json` retorna `ok: true` para PC1..PC7.
- **PC4B-8** Manifest `pc4-bump-01/manifest.json` registrado em R153 com `schema_version: v2026-07-12.4`, `expected_index_rows_before: 33`, `expected_index_rows_after: 34`, `auto_applied: false`, `verifier_gate: on` após bump.

## Invariantes transversais (TVBP-1 … TVBP-8)
- **TVBP-1** Sem tocar `supabase/migrations/`.
- **TVBP-2** Sem tocar `.github/sec002-evidence/INDEX.md`, `batch-B-apply-01/*`, `batch-B-apply-01-self-register-01/*`.
- **TVBP-3** Sem tocar `scripts/sec002/_lib/`, `helpers/`, `batches/`.
- **TVBP-4** Sem tocar `src/`, `tests/`, workflows.
- **TVBP-5** Sem tocar apply-map ou outros manifests.
- **TVBP-6** Sem segredos, chaves ou tokens.
- **TVBP-7** Nenhuma execução real contra DB.
- **TVBP-8** Self-register do bump (linha 35) fica delegado a rodadas posteriores (R154+); R153 é escopo mínimo.

## Estado esperado pós-R153
- `scripts/sec002/verify-wave-2-preconditions.mjs` linha 10 = `const EXPECTED_INDEX_ROWS = 34;`.
- Verificador PC1..PC7 = green.
- Onda 2 permanece OPEN; nenhum artefato de dados/produção alterado.
