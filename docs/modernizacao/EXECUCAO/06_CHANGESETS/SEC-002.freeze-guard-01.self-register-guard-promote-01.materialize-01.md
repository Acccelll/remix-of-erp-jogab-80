# SEC-002.freeze-guard-01.self-register-guard-promote-01.materialize-01 — Materialização canônica do `sec002-freeze-guard` com F15

**Onda:** 2 (bloqueada — depende de `SEC-001` fechar e da Onda 2 ser formalmente aberta)
**Data:** 2026-07-12
**Depende de:**
- `SEC-002.freeze-guard-01.parser-bump-01.materialize-01` (R55) — guard data-driven com F9–F11;
- `SEC-002.freeze-guard-01.evidence-validator-bump-01.materialize-01` (R65) — guard com F12–F14 e `--dry-run`;
- `SEC-002.validate-evidence-index-01.self-register-guard-01.tests-01.materialize-01` (R75) — runner T1–T5 canônico;
- `SEC-002.evidence-index-01.self-register-guard-bump-01.materialize-01` (R77) — `INDEX.md` sem órfãos conhecidos;
- `SEC-002.validate-evidence-index-01.self-register-guard-promote-01.materialize-01` (R79) — workflow canônico com o runner na posição 14;
- `SEC-002.freeze-guard-01.self-register-guard-promote-01.spec-01` (R80) — contrato desta materialização (F15 / `E_FREEZE_F15`).

**Escopo:** materializar, em conteúdo canônico versionado neste documento, `scripts/sec002/freeze-guard.mjs` com a nova tabela `SELF_REGISTER_GUARD_TEST_MAP` e a invariante **F15** (código `E_FREEZE_F15`) protegendo `scripts/sec002/test-validate-evidence-index-self-register-guard.mjs`. O apply-map permanece `v2026-07-12.4`; nenhum `protected_paths[]` é alterado. O workflow `sec002-freeze-guard.yml` permanece exatamente como em R65. **Nenhum arquivo de produção é gravado nesta etapa.** Gravação física permanece bloqueada até a abertura formal da Onda 2.

## 1. Invariantes cobertas

| Regra | Descrição executável |
|---|---|
| F1–F8 | mantidas conforme contrato original: leitura de diff, saída `OK`/`FAIL`, `exit 0/1`, sem writes, sem rede e sem dependências externas |
| F9 | todo caminho em `map.protected_paths[]` é avaliado e as tabelas F12–F14 são validadas contra o apply-map |
| F10 | `scripts/sec002/parse-apply-id.mjs` só editável sob `parse-apply-id-01` |
| F11 | `.github/sec002-evidence/parse-apply-id-01/**` só editável sob `parse-apply-id-01` |
| F12 | `scripts/sec002/validate-evidence-index.mjs` só editável sob `validate-evidence-index-01` |
| F13 | `.github/sec002-evidence/validate-evidence-index-01/**` só editável sob `validate-evidence-index-01` |
| F14 | `.github/sec002-evidence/INDEX.md` só editável sob `evidence-index-01` ou `validate-evidence-index-01` |
| **F15** | `scripts/sec002/test-validate-evidence-index-self-register-guard.mjs` só editável sob `validate-evidence-index-self-register-guard-tests-01` |

F15 é overlay de integridade restrito. O arquivo protegido **não** entra em `map.protected_paths[]` nesta versão do apply-map; o loop de arquivos alterados passa a avaliar overlays específicos mesmo quando `hits.length === 0`, garantindo que uma edição fora do apply proprietário produza `E_FREEZE_F15`.

## 2. Artefato 1 — `scripts/sec002/freeze-guard.mjs`

**Caminho:** `scripts/sec002/freeze-guard.mjs`
**Encoding:** UTF-8, LF, sem BOM. ESM puro. Node ≥ 20. Sem deps externas.

```js
#!/usr/bin/env node
import { execSync } from "node:child_process";
import { loadApplyMap, getApply } from "./load-apply-map.mjs";

const NAME = "sec002/freeze-guard";
const DRY_RUN = process.argv.includes("--dry-run");
const fail = [];
const push = (code, msg) => fail.push(`${code} ${msg}`);

const map = loadApplyMap();

const matchProtected = (file, pattern) => {
  if (pattern.endsWith("/**")) return file.startsWith(pattern.slice(0, -2));
  return file === pattern;
};

const PARSER_MAP = [
  { pattern: "scripts/sec002/parse-apply-id.mjs", allowed: ["parse-apply-id-01"], code: "E_FREEZE_F10" },
  { pattern: ".github/sec002-evidence/parse-apply-id-01/**", allowed: ["parse-apply-id-01"], code: "E_FREEZE_F11" }
];

const EVIDENCE_VALIDATOR_MAP = [
  { pattern: "scripts/sec002/validate-evidence-index.mjs", allowed: ["validate-evidence-index-01"], code: "E_FREEZE_F12" },
  { pattern: ".github/sec002-evidence/validate-evidence-index-01/**", allowed: ["validate-evidence-index-01"], code: "E_FREEZE_F13" },
  { pattern: ".github/sec002-evidence/INDEX.md", allowed: ["evidence-index-01", "validate-evidence-index-01"], code: "E_FREEZE_F14" }
];

const SELF_REGISTER_GUARD_TEST_MAP = [
  {
    pattern: "scripts/sec002/test-validate-evidence-index-self-register-guard.mjs",
    allowed: ["validate-evidence-index-self-register-guard-tests-01"],
    code: "E_FREEZE_F15"
  }
];

const SPECIFIC_MAP = [...PARSER_MAP, ...EVIDENCE_VALIDATOR_MAP, ...SELF_REGISTER_GUARD_TEST_MAP];

for (const entry of EVIDENCE_VALIDATOR_MAP) {
  if (!map.protected_paths.includes(entry.pattern)) {
    push("E_FREEZE_F9", `${entry.pattern} ausente de map.protected_paths[]`);
  }
}

let changed = [];
if (!DRY_RUN) {
  try {
    const out = execSync("git diff --name-only origin/main...HEAD", { encoding: "utf8" });
    changed = out.split("\n").map(s => s.trim()).filter(Boolean);
  } catch (e) {
    console.error(`[${NAME}] FAIL - não foi possível obter diff (${e.message})`);
    process.exit(1);
  }
}

const APPLY_ID = process.env.APPLY_ID || "";
let apply = null;
if (APPLY_ID) {
  try { apply = getApply(APPLY_ID); } catch { apply = null; }
}

const covers = (file) => {
  if (!apply) return false;
  return (apply.allowed_paths || []).some(p => matchProtected(file, p));
};

const specificRuleFor = (file) => SPECIFIC_MAP.find(entry => matchProtected(file, entry.pattern));

for (const file of changed) {
  const hits = map.protected_paths.filter(p => matchProtected(file, p));
  const specific = specificRuleFor(file);
  if (hits.length === 0 && !specific) continue;

  const code = specific?.code || "E_FREEZE_F9";

  if (!APPLY_ID) { push(code, `${file} protegido mas PR sem APPLY_ID`); continue; }
  if (!apply) { push(code, `${file} protegido e APPLY_ID=${APPLY_ID} inválido`); continue; }

  if (specific && !specific.allowed.includes(APPLY_ID)) {
    push(code, `${file} só editável sob ${specific.allowed.join(" ou ")} (APPLY_ID=${APPLY_ID})`);
    continue;
  }

  if (hits.length > 0 && !covers(file)) push(code, `${file} fora de allowed_paths[] de ${APPLY_ID}`);
}

if (fail.length === 0) { console.log(`[${NAME}] OK`); process.exit(0); }
console.error(`[${NAME}] FAIL`);
for (const v of fail) console.error(`  - ${v}`);
process.exit(1);
```

Contratos:

- `stdout` em sucesso: única linha `[sec002/freeze-guard] OK`; `exit 0`.
- `stderr` em falha: `[sec002/freeze-guard] FAIL` + uma violação por linha com código `E_FREEZE_F<n>`; `exit 1`.
- `--dry-run` não lê diff Git; valida apenas a consistência da tabela F12–F14 contra `map.protected_paths[]`; F15 continua estaticamente presente via `SELF_REGISTER_GUARD_TEST_MAP`, sem exigir presença em `protected_paths[]`.
- F15 não faz rede, não escreve em disco e não lê o arquivo do runner; usa apenas o diff Git.
- Nunca escreve em disco, nunca faz rede, nunca lê `.github/sec002-apply-map.json` direto — apenas via `loadApplyMap()`.
- Nunca reimplementa parsing de `APPLY_ID`; consome `process.env.APPLY_ID` populado pelo pré-gate `parse-apply-id`.

## 3. Artefato 2 — `.github/workflows/sec002-freeze-guard.yml`

**Inalterado em relação a R65.** Nenhum step é adicionado, removido ou reordenado; `concurrency`, `permissions: read-only`, `timeout-minutes: 5`, `fetch-depth: 0` e pré-gate único `parse-apply-id` continuam idênticos.

## 4. Compatibilidade

| Consumidor / Guard | Efeito |
|---|---|
| `sec002-consumers-guard` | continua exigindo R1–R7 de `freeze-guard.mjs` |
| `sec002-pr-checklist` | inalterado; o runner permanece na posição 14 conforme R79 |
| Apply-map `v2026-07-12.4` | `protected_paths[]` preservado; F15 é overlay diagnóstico, não bump |
| `validate-evidence-index.mjs` | inalterado; S1–S6 continuam dentro do validador |
| `test-validate-evidence-index-self-register-guard.mjs` | passa a ter proteção de integridade F15 contra alteração fora do apply proprietário |

## 5. Procedimento de gravação futura

1. Gravar `scripts/sec002/freeze-guard.mjs` (§2).
2. Rodar localmente:

```bash
node scripts/sec002/validate-apply-map.mjs
node scripts/sec002/check-consumers.mjs
node scripts/sec002/freeze-guard.mjs --dry-run
node scripts/sec002/test-validate-evidence-index-self-register-guard.mjs
```

Saída esperada:

```text
[sec002/validate-apply-map] OK
[sec002/consumers-guard] OK
[sec002/freeze-guard] OK
[sec002/test-validate-evidence-index-self-register-guard] OK
```

## 6. Evidência obrigatória

`.github/sec002-evidence/freeze-guard-self-register-guard-promote-01/manifest.json`:

```json
{
  "sec002_apply_id": "freeze-guard-self-register-guard-promote-01",
  "schema_version": "v2026-07-12.4",
  "source": "docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.freeze-guard-01.self-register-guard-promote-01.materialize-01.md",
  "changed_files": [
    "scripts/sec002/freeze-guard.mjs"
  ],
  "checks": [
    "node scripts/sec002/validate-apply-map.mjs",
    "node scripts/sec002/check-consumers.mjs",
    "node scripts/sec002/freeze-guard.mjs --dry-run",
    "node scripts/sec002/test-validate-evidence-index-self-register-guard.mjs"
  ],
  "hashes": {
    "scripts/sec002/freeze-guard.mjs": "<sha256>"
  },
  "refs": ["R55", "R65", "R72", "R73", "R75", "R77", "R78", "R79", "R80", "R81"]
}
```

## 7. Critérios de rejeição futura

- Alteração de `schema_version`, apply-map, loader ou validador do apply-map.
- `SELF_REGISTER_GUARD_TEST_MAP` ausente, com `pattern`, `allowed[]` ou `code` diferentes de F15.
- Loop de arquivos alterados ignorando overlays específicos quando `hits.length === 0`.
- F15 permitindo editar o runner sob `pr-checklist-workflow-self-register-guard-promote-01` ou qualquer outro apply não proprietário.
- Guard reimplementando parsing de título/corpo de PR em vez de consumir `$APPLY_ID`.
- Guard lendo `.github/sec002-apply-map.json` sem passar por `loadApplyMap()`.
- Qualquer alteração em `.github/workflows/sec002-freeze-guard.yml`, `.github/workflows/sec002-pr-checklist.yml`, `.github/sec002-evidence/INDEX.md`, `.github/sec002-apply-map.json`, `src/**`, `supabase/functions/**` ou `supabase/migrations/**`.
- Código `E_FREEZE_F15` ausente ou emitido em `stdout` em falha.

## 8. Impacto em produção

**Nenhum.** Documento de materialização canônica. Não grava scripts/workflows executáveis nesta etapa, não altera dados, não aplica migração, não muda função, não muda frontend e não altera política RLS.
