# SEC-002 · R182 · pc4-bump-10 · compact-01 · materialize-01

**Apply-map:** v2026-07-12.4 (preservado)
**Apply-id:** wave-2-formal-open-01
**Ref:** R182
**Política:** aceleração — round compacto atômico

## 1. Motivação

O ciclo `pc4-bump-*` vinha consumindo 3 rounds por unidade de linha do `INDEX.md` (spec self-register, materialize self-register, bump verifier), com residual permanente de 1 linha e 1 gate em aberto. R182 substitui esse padrão por um round único atômico que:

1. Registra o bump anterior (`pc4-bump-09`, R181) como linha 43 do `INDEX.md`.
2. Registra a si mesmo (`pc4-bump-10`) como linha 44 do `INDEX.md`.
3. Faz bump `EXPECTED_INDEX_ROWS: 42 → 44` no verificador, fechando ambos os gates simultaneamente.
4. Publica manifest determinístico único.

Resultado: **1 round = 2 linhas absorvidas, 0 gates residuais**, contra o padrão anterior de 3 rounds por linha.

## 2. Write set (4 arquivos)

1. `.github/sec002-evidence/INDEX.md` — append linhas 43 e 44; `Última atualização: 2026-07-12T20:55:00Z`.
2. `scripts/sec002/verify-wave-2-preconditions.mjs` — linha 10: `EXPECTED_INDEX_ROWS = 42 → 44`.
3. `.github/sec002-evidence/pc4-bump-10/manifest.json` — manifest determinístico.
4. `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-001.README.md` — linha R182.

## 3. Invariantes PCC10-1…PCC10-8

- PCC10-1: linhas 1–42 do `INDEX.md` preservadas byte-a-byte (`sha256: ea9d5af0…3e26dd`).
- PCC10-2: linha 43 aponta para `pc4-bump-09` (manifest + materialize de R181).
- PCC10-3: linha 44 aponta para `pc4-bump-10` (este round).
- PCC10-4: única linha alterada no verificador é a 10; delta = `42 → 44`.
- PCC10-5: `SCHEMA_VERSION` e `DEFAULT_APPLY_ID` inalterados.
- PCC10-6: manifest declara `self_registers` e `auto_applied: true`.
- PCC10-7: nenhum arquivo fora do write set tocado.
- PCC10-8: `node verify-wave-2-preconditions.mjs --json` → `ok: true` restaurando PC1..PC7 verdes.

## 4. TVBP18-1…TVBP18-8

Sem DB, secrets, tokens, execução em produção, migrations, edge functions, RLS, grants, `src/`, testes, workflows ou apply-map tocados.

## 5. Verificação

`node scripts/sec002/verify-wave-2-preconditions.mjs --json` → `{ ok: true, checked: [PC1..PC7] }`.
`INDEX.md` = 44 linhas úteis; `EXPECTED_INDEX_ROWS = 44`; gates `E_WAVE2_PC4 42 vs 43` e `E_WAVE2_PC4 43 vs 44` **fechados atomicamente**.

## 6. Política de aceleração daqui em diante

- Próximos rounds seguem o padrão `compact-N`: cada round absorve seu antecessor + a si mesmo em uma única transação.
- Se o próximo trabalho útil for fora do treadmill (ex.: SEC-002 lote B/C/D), o compact-round encerra este eixo antes de mudar de frente.

## 7. Não tocado

`_lib/`, `migrations/`, `src/`, `supabase/`, `helpers/`, `batches/`, `apply-map`, `workflows`, `.env`, banco.
