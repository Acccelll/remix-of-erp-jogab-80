# UX-002 — NOTA-FIM (retroativa)

**Onda:** 7 · **Achado:** UX-002 — Pesquisa global (⌘K) limitada
**Slices:** `UX-002.slice-01`
**Status:** ✅ ENCERRADO

## Escopo entregue

`src/components/layout/CommandPalette.tsx` — Command Palette (Ctrl/⌘+K) agora
indexa **5 domínios** via contextos globais já providos pelo `Layout`, sem
novas queries:

1. **Telas** — navegação de rotas (pré-existente).
2. **Obras** — código + nome (pré-existente).
3. **Colaboradores** — nome + CPF (pré-existente).
4. **Patrimônios** — código + nome → `/patrimonios/lista`.
5. **Veículos** — código + nome + tipo → `/veiculos`.
6. **Clientes** — razão social + fantasia + CNPJ → `/crm/clientes`.

Cada grupo limita 50 itens; `value` composto no `CommandItem` permite matching
por qualquer campo. Placeholder do `CommandInput` reflete os novos escopos.

## Validação

- `bunx tsgo --noEmit` limpo no slice-01.
- Zero impacto em dados/backend; consumo puro de contextos existentes.

## Débitos residuais (não bloqueiam o achado)

- Indexar **cards/oportunidades CRM**, **riscos** e **ordens de compra** (exigem
  query dedicada — hoje não estão em contexto global).
- Ranking por relevância / histórico de uso (`cmdk` já ordena por fuzzy match).
- Atalhos secundários (ex.: `g o` para "ir para obras") — decoração, fora do
  achado original.

## Rastreabilidade

- Achado: `docs/modernizacao/00_INVENTARIO/UX-002` (Onda 7).
- Slice único: `UX-002.slice-01.md`.
- Registro de conclusão: `05_ONDAS/ONDA_07/10_Checklist_Conclusao.md`.
