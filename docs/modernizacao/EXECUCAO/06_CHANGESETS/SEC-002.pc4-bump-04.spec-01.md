# SEC-002 · pc4-bump-04 · spec-01 (R164)

**Onda:** 2 (OPEN) · **Apply-map:** v2026-07-12.4 · **Gate:** restaura PC1..PC7 = green após apply

## Objetivo
Autorizar o bump da constante `EXPECTED_INDEX_ROWS` de `36` → `37` em `scripts/sec002/verify-wave-2-preconditions.mjs`, refletindo o self-register de R163 (linha 37 = `pc4-bump-03`).

## Write set físico autorizado (R165 — 2 arquivos)
1. `scripts/sec002/verify-wave-2-preconditions.mjs` — linha 10:
   - antes: `const EXPECTED_INDEX_ROWS = 36;`
   - depois: `const EXPECTED_INDEX_ROWS = 37;`
2. `.github/sec002-evidence/pc4-bump-04/manifest.json` — manifest (`schema_version: v2026-07-12.4`, `expected_index_rows_before: 36`, `expected_index_rows_after: 37`, `auto_applied: false`, `verifier_gate: on`).

## Invariantes PC4B4-1…PC4B4-8
- **PC4B4-1** Diff no verificador = 1 linha alterada (linha 10), 0 fora dela.
- **PC4B4-2** Nenhuma outra constante numérica alterada.
- **PC4B4-3** Lógica PC1/PC2/PC3/PC5/PC6/PC7 preservada byte-a-byte.
- **PC4B4-4** Loop `for (let i = 1; i <= EXPECTED_INDEX_ROWS; …)` inalterado.
- **PC4B4-5** `Apply-map: v2026-07-12.4` preservado.
- **PC4B4-6** `INDEX.md` NÃO é tocado por R165.
- **PC4B4-7** Após R165: `node verify-wave-2-preconditions.mjs --json` → `ok: true` (PC1..PC7).
- **PC4B4-8** Manifest `pc4-bump-04/manifest.json` gravado.

## Invariantes transversais TVBP7-1…TVBP7-8
- **TVBP7-1** Sem tocar `supabase/migrations/`.
- **TVBP7-2** Sem tocar `INDEX.md`, batch-B/pc4-bump-01/pc4-bump-02/pc4-bump-03 artefatos.
- **TVBP7-3** Sem tocar `scripts/sec002/_lib/`, `helpers/`, `batches/`.
- **TVBP7-4** Sem tocar `src/`, `tests/`, workflows.
- **TVBP7-5** Sem tocar apply-map ou outros manifests.
- **TVBP7-6** Sem segredos, chaves ou tokens.
- **TVBP7-7** Nenhuma execução real contra DB.
- **TVBP7-8** Self-register (linha 38) fica delegado a R166+.

## Nota de auto-recorrência
Ciclo `pc4-bump-N` mantido conforme padrão canônico; nenhuma política alterada em R164.

## Estado esperado pós-R165
- Verificador linha 10 = `const EXPECTED_INDEX_ROWS = 37;`.
- Gate PC1..PC7 = green.
- Onda 2 permanece OPEN; nenhum artefato de dados/produção alterado.
