# ARC-013 · slice-23 — Encapsular `cardBoardPosicaoRepo` em `useCards`

## Escopo
Migrar `cardBoardPosicaoRepo` (`src/lib/repositories/cardsExtra.ts`) da chamada
direta em componentes para hooks/queryFns canônicos em `src/hooks/useCards.ts`.

## Alterações

### `src/hooks/useCards.ts`
- Import interno de `cardBoardPosicaoRepo`.
- `cardsQueryFns.maxPosBoardPosicaoNaLista(listaId)` — leitura da próxima posição livre.
- Helper local `invalidateBoardItems(qc, boardId)` (invalidação de `["board-items", boardId]`).
- Novos hooks de mutação com invalidação em `onSettled`:
  - `useInsertCardBoardPosicao`
  - `useUpdateCardBoardPosicaoPosicao`
  - `useUpdateCardBoardPosicaoListaEPosicao`
  - `useMoverCardBoardPosicaoParaBoard` (invalida origem e destino)
  - `useCriarCardBoardAtomico`

### `src/components/cards/MoverCopiarCardMenu.tsx`
- Removido import direto de `cardBoardPosicaoRepo`.
- `maxPosNaLista` → `cardsQueryFns.maxPosBoardPosicaoNaLista`.
- `mover()` usa `useMoverCardBoardPosicaoParaBoard.mutateAsync`.
- `copiar()` insere a posição via `useInsertCardBoardPosicao.mutateAsync`.

### `src/pages/QuadroBoard.tsx`
- Removido import direto de `cardBoardPosicaoRepo`.
- DnD `updatePosicao`/`updateListaEPosicao` → `useUpdateCardBoardPosicao*` (mutateAsync).
- Quick-add `criarCardAtomico` → `useCriarCardBoardAtomico.mutateAsync`.

## Validação
- `tsgo --noEmit` limpo.
- `rg cardBoardPosicaoRepo src` → apenas `useCards.ts` (canônico), repo em
  `cardsExtra.ts` e teste de contrato.
- Semântica preservada: mesmas queries de invalidação (`["board-items", boardId]`),
  mesmos payloads e sequência de operações no `mover()`/`copiar()`.
