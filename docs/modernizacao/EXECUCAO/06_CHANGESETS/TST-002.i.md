# TST-002.i — Contratos de boards e cronograma

**Onda:** 6 (bloqueada — NO-GO D-9)  
**Data:** 2026-07-12  
**Escopo:** travar contratos das superfícies do Kanban (`boards`) e do planner (`cronograma`) antes das refatorações estruturais da Onda 6.

## Alvo

- `src/lib/repositories/boards.ts` — `boardListasRepo`, `boardCamposRepo`, `boardMembrosRepo`.
- `src/lib/repositories/cronograma.ts` — `cronogramaRepo`, `cronogramaItemRevisoesRepo`, `cronogramaCenariosRepo`, `cronogramaCenarioItensRepo`.

## Arquivo criado

- `src/lib/__tests__/integracao/repositories-contrato-i.test.ts` — 26 casos.

## Contratos travados

- `board_listas`: `create/insertMany/update/updatePosicao` com no-op para arrays vazios e `eq('id', ...)` em updates.
- `board_campos`/`board_membros`: tabela e verbo (`insert`/`delete().eq('id', ...)`).
- `cronograma_itens` (leituras): `listByObra` usa `CRONOGRAMA_ITENS_COLUNAS` + filtro `obra_id` + ordenação `data_inicio`; `listDashboard` usa `CRONOGRAMA_COLUNAS_DASHBOARD`; `listResumoPacotes` usa projeção `id, descricao, obra_id`; `listIdsPorObra` usa `select('id').eq('obra_id',...)`. Todos devolvem `[]` em `data null`.
- `cronograma_itens` (mutações): `create/insertMany/update/remove/removeByObra/removeMany` com no-op vazio e uso de `in('id', ids)` em batch.
- `cronograma_item_revisoes` / `cronograma_cenarios` / `cronograma_cenario_itens`: verbos, tabelas e filtros (`revisao_id`, `id`) travados.

## Resultado

- Lote I: 26/26 ✔
- Suite total: **597/597** ✔
- `tsgo --noEmit`: verde (nenhuma mudança em código de produção)

## Acumulado TST-002

9 lotes · 33 superfícies · **169 casos**.

## Próximo

`TST-002.j` — `clientes` + `fornecedores` (cadastros mestres antes das refatorações de compras/financeiro).
