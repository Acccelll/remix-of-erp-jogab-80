# ARC-003.c.5 — Cards e Boards (fecha ARC-003)

## Escopo
Zerar chamadas diretas a tabelas `card_*`, `board_*` e `cards` (exceto `src/lib/cards/trello-import.ts`, exceção documentada).

## Novo repositório
`src/lib/repositories/cardsExtra.ts` com subrepos: `cardAnexosRepo`, `cardChecklistItensRepo`, `cardMembrosRepo`, `cardSetoresRepo`, `cardRecursosRepo`, `cardLabelsRepo`, `cardLabelLinksRepo`, `cardComentariosRepo`, `cardAtividadesRepo`, `cardLocalRepo`, `cardCustomFieldValoresRepo`, `cardBoardPosicaoRepo`, `cardGruposNegociacaoRepo`, `cardViewsSalvasRepo`, `cardsExtraRepo` (getFull/setStatus).

## Extensões em `boards.ts`
- `boardListasRepo.updatePosicao`.
- Novos `boardCamposRepo` (insert/remove) e `boardMembrosRepo` (insert).

## Call-sites refatorados (~40)
- `QuadroBoard.tsx`, `QuadroKanban.tsx`, `QuadroViewsSalvas.tsx`.
- `CommentEditor.tsx`, `CardRecursoDialog.tsx`, `AdicionarRecursoDialog.tsx`, `CardGenericoDialog.tsx`, `CardLocalSection.tsx`, `MoverCopiarCardMenu.tsx`.
- `BoardConfigDialog.tsx`.

## Exceção
`src/lib/cards/trello-import.ts` (utilitário isolado de importação Trello) mantém acesso direto — não é caminho crítico e cria uma quantidade grande de tabelas transacionalmente. Registrado na checklist da Onda 3.

## Verificação
- `rg 'supabase.from\("(card_[a-z_]+|board_[a-z_]+|cards)"\)' src/` retorna zero fora de `src/lib/repositories/` e `src/lib/cards/trello-import.ts`.
- `tsgo --noEmit` verde.

## Fecha ARC-003
Todos os módulos de UI/páginas passaram a acessar Supabase via repositórios.
