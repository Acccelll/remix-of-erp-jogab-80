# ARC-002.slice-36 · Onda 6 — Extração de `patrimonios` para `PatrimoniosStateProvider`

## Escopo
- Novo `src/contexts/patrimonios/PatrimoniosStateContext.tsx`: domicilia `usePatrimoniosState` + query `usePatrimonios` com `useSyncQueryToState`; expõe `patrimonios`, `setPatrimonios`, CRUDs, `patrimoniosLoaded`, `getPatrimonios` (getter estável).
- `AppContext.tsx`: consome `usePatrimoniosStateContext()`; usa o `getPatrimonios` do provider como `getPatrimoniosStable` para `useMobilizacoes`. `dataLoaded` passa a compor `patrimoniosLoaded`.
- `useAppQueries.ts`: remove `usePatrimonios` e `setPatrimonios` (agora vivem no provider).
- `App.tsx`: `<PatrimoniosStateProvider>` promovido para acima do `<AppProvider>` (dentro de `<ColaboradoresStateProvider>`).

## Validação
- `tsgo --noEmit`: limpo.
- Consumidores do `PatrimoniosProvider` (fachada) e `useApp().patrimonios/...` continuam funcionando via bridge.

## Próximo
`ARC-002.slice-37` — mesma extração para `veiculos` (`useVeiculosState` + query `useVeiculos`).
