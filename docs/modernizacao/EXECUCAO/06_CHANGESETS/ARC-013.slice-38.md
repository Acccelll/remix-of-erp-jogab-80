# ARC-013 · slice-38 — financeiro sem imports diretos de cronograma

## Alterações
1. **`src/pages/financeiro/FinObraDetalhe.tsx`**: import de `@/lib/repositories/cronograma` substituído por `@/hooks/useCronograma` com aliases para `cronogramaFns` e `cronogramaRevisoesFns`.
2. **`src/pages/financeiro/FinDashboard.tsx`**: import de `cronogramaRepo` substituído por `cronogramaFns` via hook canônico.

## Escopo intencional
`PacotesTrabalho`, `CenariosSheet` e libs de domínio (`recalcular-cpm.ts`, `persistir-calendarios.ts`) permanecem para slices dedicadas: ainda combinam tipos/repos auxiliares ou fluxo de domínio fora do financeiro.

## Validação
- `bunx tsgo --noEmit`: limpo.
- `rg 'from "@/lib/repositories/cronograma"' src/pages/financeiro`: sem ocorrências.