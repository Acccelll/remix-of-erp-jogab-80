# PRO-012 · slice-24 — Badge de direção (piora/melhora) na célula de tendência

## Escopo
Destacar linhas em deterioração ou melhora na coluna "Tendência ign." da
tabela principal, independentemente da ordenação ativa (slice-23).

## Alterações
- `src/pages/suprimentos/ContagensCiclicas.tsx`
  - `rowsOrdenadas.map` passa a desestruturar também `delta`.
  - Badge inline após o total: `ArrowUp` (piora, `bg-destructive/10`),
    `ArrowDown` (melhora, `bg-emerald-500/10`), `ArrowUpDown` (estável).
  - `title` e `aria-label` descrevem a variação e o valor (`+N`, `−N`, `0`).
  - Renderizada apenas quando `serie.length >= 2` (evita ruído em séries curtas).

## Contrato de execução
- UI-only, sem Cloud/RLS/schema. Reaproveita `delta` já calculado no slice-23.

## Validação
- `bunx tsgo --noEmit` sem erros.
- Manual: contagens com piora exibem seta vermelha `+N`; melhora exibe seta
  verde `−N`; séries de 0/1 pontos permanecem sem badge.

## Próximo passo sugerido
`PRO-012.slice-25` — filtro rápido "Somente em piora" acima da tabela,
combinável com o filtro de local existente.
