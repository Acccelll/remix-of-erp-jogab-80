# ARC-013 · slice-39 — PacotesTrabalho via useCronograma

## Escopo
Migrar `src/pages/planejamento/PacotesTrabalho.tsx` para consumir `cronogramaFns` de `@/hooks/useCronograma` em vez de importar `cronogramaRepo` diretamente de `@/lib/repositories/cronograma`.

## Alterações
- `src/pages/planejamento/PacotesTrabalho.tsx`:
  - Runtime: `import { cronogramaFns as cronogramaRepo } from "@/hooks/useCronograma"`.
  - Tipo: `import type { CronogramaItemRow } from "@/lib/repositories/cronograma"` (type-only, sem custo de runtime nem violação do contrato ARC-013 que mira dependências de execução em UI).
  - Corpo intacto (uso em `cronogramaRepo.list(...)`).

## Validação
- `tsgo --noEmit`: limpo.
- `rg "from \"@/lib/repositories/cronograma\"" src/pages/planejamento/PacotesTrabalho.tsx`: só o `import type`.

## Fora de escopo (documentado)
- `src/components/obra/CenariosSheet.tsx` — próxima slice (requer expor `cronogramaCenariosFns` e `cronogramaCenarioItensFns`).
- `src/lib/cronograma/*.ts` e `src/lib/__tests__/**` — camada de domínio/teste, fora do escopo ARC-013 (UI → hook).
