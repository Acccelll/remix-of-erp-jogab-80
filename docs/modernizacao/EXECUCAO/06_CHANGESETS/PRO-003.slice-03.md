# PRO-003.slice-03 — Trava de importação por competência fechada

**Onda:** 07 · **Achado:** PRO-003 · **Slice:** 03/n

## Entrega
- `src/components/dp/ImportHoleriteDialog.tsx`
  - Após parse do XLS, extrai as competências presentes no arquivo.
  - Se qualquer uma estiver fechada (`useDpFechamentoCompetencia.estaFechada`),
    aborta antes do `mutateAsync` e exibe erro amigável com responsável e data
    do fechamento — orienta reabrir pelo Fopag.
  - Reutiliza `Alert` de erros já existente no diálogo (nenhuma cor hardcoded).

## Regras respeitadas
- Único ponto de mutação DP (importação de Holerite/Adiantamento) fica travado;
  telas de HE/Adiantamento/HoleriteDetail já são read-only.
- Regra pura reaproveitada (`competenciaValida` + `estaFechada`).
- Nenhuma alteração de contrato de dados nem migração.

## Não escopo
- Trava no backend (slice-04): tabela `dp_fechamento_competencia`, RPC de
  fechar/reabrir, RLS e auditoria em `audit_logs`.
- Bloqueio equivalente em rotinas de recálculo (não existem hoje).

## Validação
- `bunx tsgo --noEmit` — OK.
- Fluxo manual: importar XLS de competência fechada → mensagem clara com
  motivo/responsável; reabrir na barra do Fopag → import volta a funcionar.
