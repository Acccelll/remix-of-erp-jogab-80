# PRO-009 — NOTA-FIM (retroativa)

**Onda:** 7 · **Achado:** PRO-009 — Suprimentos: fluxo cotação vencedora → OC não conduzido
**Slices:** `PRO-009.slice-01`, `PRO-009.slice-02`
**Status:** ✅ ENCERRADO

## Escopo entregue

- **slice-01** — one-click **Cotação fechada + proposta escolhida → OC (rascunho)** direto do mapa comparativo.
  - `src/lib/repositories/suprimentos.ts` · `gerarOcDeCotacao(cotacaoId)`: valida `status=fechada` + proposta `escolhida`; cria `ordens_compra` (`rascunho`/`pendente`) com fornecedor + obra; insere item em `ordem_compra_itens` com `requisicao_id`, descrição do `orcamento_itens` (fallback observação), qtd × preço da proposta; atualiza `valor_total`; muda `requisicoes.status='atendida'`.
  - `src/pages/suprimentos/Cotacoes.tsx`: botão **Gerar OC** no `DialogFooter` (só quando cotação `fechada` + proposta `escolhida`); após sucesso, fecha diálogo, recarrega e navega para `/suprimentos/ordens-compra`.
- **slice-02** — reversão automática ao cancelar OC de rascunho.
  - `src/lib/repositories/suprimentos.ts` · `cancelarOcRascunho(ocId)`: valida `rascunho` + `emitida_em IS NULL`; reabre requisições vinculadas (`atendida → cotando`) de forma idempotente; marca OC `cancelada`; retorna `{ revertidas }`.
  - `src/pages/suprimentos/OrdensCompra.tsx`: handler `cancelar` chama o repositório; toast informa reaberturas.

## Fluxo consolidado

```text
Requisição (aberta)
  └─► Cotação (aberta) ──[Escolher + Fechar]──► Cotação (fechada + vencedora)
        └─[Gerar OC]──► OC (rascunho / pendente) ──► alçadas padrão
              └─► Requisição = atendida
                    └─[Cancelar OC rascunho]──► Requisição = cotando (reaberta)
```

## Validação

- `bunx tsgo --noEmit` limpo em ambos os slices.

## Débitos residuais (não bloqueiam o achado)

- **PRO-010** — envio de OC/RFP ao fornecedor por PDF/e-mail (achado próprio, separado).
- Consolidação de múltiplas cotações em uma única OC multi-item.
- Reversão pós-emissão (exige cancelamento formal + estorno de committed cost).

## Rastreabilidade

- Achado: `docs/modernizacao/00_INVENTARIO/PRO-009`.
- Slices: `PRO-009.slice-01.md`, `PRO-009.slice-02.md`.
- Registro de conclusão: `05_ONDAS/ONDA_07/10_Checklist_Conclusao.md`.
