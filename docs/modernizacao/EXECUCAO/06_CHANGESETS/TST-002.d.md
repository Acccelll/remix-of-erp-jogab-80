# TST-002.d — Cobertura de `cronograma` e `requisicoes`

**Onda:** pré-condição da Onda 6 (Stage Gate: TST-002 em curso, ver D-9).
**Achado:** TST-002 — lote D.

## Contexto

Repositórios do domínio de obras que ARC-005 vai reestruturar na Onda 6
(cronograma alimenta AnaliseTab/EVM; requisições atravessam o fluxo
suprimentos ↔ obra ↔ compras). Fixar o contrato aqui antes da cirurgia.

## Mudanças

- Novo `src/lib/__tests__/integracao/repositories-contrato-d.test.ts` com 16 casos:
  - `cronogramaRepo`: `listByObra` (colunas + eq + order; `data null`; erro),
    `listDashboard` (projeção de dashboard), `insertMany` (short-circuit),
    `update`, `removeByObra`, `removeMany` (short-circuit + `in`).
  - `cronogramaItemRevisoesRepo`: `removeByRevisao` (propagação de erro),
    `removeByRevisaoIds` (short-circuit).
  - `requisicoesRepo`: `listAll` (colunas padrão + order desc; erro),
    `listResumoObra` (limit default 500), `updateStatus`, `update`, `create`.

## Validação

- `bunx vitest run src/lib/__tests__/integracao/repositories-contrato-d` → **16/16**.
- Suíte completa: **483/483 verdes** (baseline pós-TST-002.c era 467).
- `bunx tsgo --noEmit` → verde.

## Cobertura acumulada de TST-002

| Lote | Repositories                                                                                                    | Casos |
| ---- | --------------------------------------------------------------------------------------------------------------- | ----- |
| A    | `obrasRepo`, `fornecedoresRepo`                                                                                 | 9     |
| B    | `clientesRepo`, `insumosRepo`                                                                                   | 14    |
| C    | `boardListasRepo`, `cardChecklistItensRepo`, `cardMembrosRepo`, `cardSetoresRepo`                               | 16    |
| D    | `cronogramaRepo`, `cronogramaItemRevisoesRepo`, `requisicoesRepo`                                               | 16    |

**Total:** 55 casos sobre 11 repositórios/fatias.

Restantes (lotes E+): `cards`, `cardsExtra` (anexos/recursos), `dp`,
`dpHolerite`, `financeiro`, `inspecoes`, `obraDetalhe`, `planejamento`,
`rdo`, `suprimentos`, `boardCamposRepo`, `boardMembrosRepo`,
`cronogramaCenariosRepo`, `cronogramaCenarioItensRepo`.

## Próximo

Lote E — `financeiroRepo` + `obraDetalheRepo` (fecha os dois repositórios de
maior superfície agregada; abre caminho para atender a sub-onda 5.F quando
suas pré-condições forem entregues).
