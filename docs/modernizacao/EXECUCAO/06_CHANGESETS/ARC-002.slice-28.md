# ARC-002 · slice-28

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Escopo
1. Novo `src/contexts/auth/AuthContext.tsx` — `AuthProvider` domicilia o estado `currentPlayer` (com hidratação de `go_player`/`migratePlayers` que antes era feita no `AppProvider`).
2. `src/contexts/AppContext.tsx` — deixa de declarar `useState<Player | null>` e passa a consumir `useAuthState()` como bridge. Imports de `loadLocal`/`migratePlayers` removidos.
3. `src/contexts/auth/useAuth.ts` — `currentPlayer` lido direto do `AuthContext`; `login`/`logout` seguem via `useApp()` nesta fatia (dependem de `players`/`dataLoaded`; migração para fatia futura).
4. `src/App.tsx` — `AuthProvider` envolve `AppProvider` (dentro do `UIProvider`).

## Motivação
Segunda extração de **estado** real do God-context `AppContext` (após `sidebarOpen` na slice-27). `currentPlayer` — leitura mais quente do app — deixa de reidratar a árvore inteira via `useApp()`; consumidores modernos (`useAuth`) lêem de um contexto pequeno e focado.

## Validação
- `tsgo --noEmit`: sem erros.
- `AppContext` continua expondo `currentPlayer` no seu value (fachada) para não quebrar consumidores legados de `useApp()`; o dado é o mesmo, agora domiciliado no `AuthContext`.

## Próximo passo
`ARC-002.slice-29` — mover `login`/`logout` e o efeito de invalidação de sessão para o `AuthProvider`. Requer expor `players`/`dataLoaded` por um `PlayersProvider` real (candidato natural após esta fatia).
