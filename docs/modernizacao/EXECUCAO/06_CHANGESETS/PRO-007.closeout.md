# PRO-007 · closeout — Workflow ativo de Não Conformidades

**Onda:** 07 · **Achado:** PRO-007 · **Status:** concluído.

## Slices entregues
- **slice-01** — Núcleo puro do workflow (`NCState`, `NCEvent`, `transition`, cálculo de vencimento) com estados formais `aberta`, `em_tratativa`, `aguardando_reinspecao`, `resolvida` e `cancelada`.
- **slice-02** — Mapper puro `NCState ↔ nao_conformidades`, preservando o contrato legado e representando `aguardando_reinspecao` por `status='em_tratativa'` + flag dedicado.
- **slice-03** — Migration do workflow em `public.nao_conformidades`, com colunas de responsável, prazo, tratativa, reinspeção, encerramento, índices, constraints, RLS e `GRANT`s explícitos.
- **slice-04** — Repositório de NCs usando mapper/FSM (`getWorkflowState`, `setWorkflowState`, `updateWorkflowState`, `applyTransition`) e espelhamento com campos legados 5W2H.
- **slice-05** — UI de `NaoConformidades.tsx` consumindo ações formais de workflow para atribuição, tratativa, solicitação/aprovação/reprovação de reinspeção e cancelamento.
- **slice-06** — Integração com a central de notificações (`PRO-030`): prazo novo `prazo`, fallback legado `quando_planejado` e alerta prioritário para `aguardando_reinspecao`.

## Aceite (Etapa 2)
> _"NC sem workflow ativo (sem responsável/prazo/reinspeção/notificação)"_

Coberto: responsável e prazo persistidos, tratativa obrigatória antes de reinspeção, ciclo de reinspeção aprovado/reprovado, notificação por prazo/reinspeção e compatibilidade com NCs legadas.

## Fora do escopo (não regride)
- Geração automática de restrição no M4 e card no M2 — pertence ao próximo achado `PRO-008`, desbloqueado por este closeout.
- Relatório/board consolidado de NCs vencidas por responsável.
- E-mail/push server-side; a entrega permanece na central de notificações local conforme decisão consolidada em `PRO-030.closeout`.
- Remoção imediata dos campos legados 5W2H; mantidos para compatibilidade até migração funcional completa das telas consumidoras.

## Validação
- `nc-workflow`, `nc-mapper`, repositório e `useNCPendentes` possuem testes dedicados registrados nas slices 01, 02, 04 e 06.
- `bunx tsgo --noEmit` foi executado limpo nas slices 01, 02, 04, 05 e 06.
- Migration da slice-03 foi aplicada com `GRANT` explícito, RLS habilitado e schema confirmado.

## Próximo achado
`PRO-008.slice-01` — NC passa a gerar restrição operacional (M4) e card automático (M2), usando o workflow `PRO-007` como pré-condição já concluída.