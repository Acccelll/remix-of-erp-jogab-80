# PRO-031 · slice-25 — Nº do contrato nas notificações do sino

## Escopo
Fechamento das lacunas remanescentes de Nº parametrizado em superfícies
que expunham contratos apenas pelo `locacaoServico` / id truncado.

## Alterações
- `src/components/layout/NotificationBell.tsx`
  - Novo helper local `numeroContrato(id)` reutilizando
    `getDocumentoNumeroLocal(id, "contrato")` com fallback `CTR-{últ.6}`.
  - Bloco **Contratos e reajustes**: prefixo `Nº` (mono, muted) antes do
    nome do contrato em cada item.
  - Bloco **Documentos de contratos**: prefixo `Nº` no rodapé, antes do
    nome do contrato / versão / validade.
  - Bloco **Alertas silenciados**: prefixo `Nº` antes do nome do contrato.

## Fora de escopo (confirmado sem lacuna)
- Exports XLSX (`handleExport`, cronogramas por contrato) já incluíam `Nº`
  (slices 20 e 23).
- Cronograma "Por obra × mês" é agregado por obra, não expõe contratos
  individualmente.
- `ContratoProfileDialog` já traz o badge `Nº` no header (slice 20).
- `CardGenericoDialog` e `CascataPrazosBar` cobertos na slice 24.
- Dashboards de portfólio/planejamento operam sobre `valor_contrato`
  (valor financeiro), não sobre identidade documental.

## Validação
- Tipagem: alteração puramente de apresentação; imports resolvidos.
- Visual: sino continua carregando via hooks existentes; contratos sem
  numeração configurada exibem fallback `CTR-XXXXXX`.

## Contrato de Execução
- Achado: PRO-031 (Nº documental parametrizado em contratos).
- Onda: 07.
- Status do achado após esta slice: **coberto** — todas as saídas
  identificadas exibem/exportam Nº com fallback consistente.

## Próximo passo
`PRO-031` encerrado nas superfícies mapeadas. Próxima slice avança para o
próximo achado da Onda 07 conforme roadmap.
