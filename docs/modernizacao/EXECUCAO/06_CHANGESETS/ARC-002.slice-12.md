# ARC-002 · slice-12 — Migração `obras`/`clientes` para contexts de domínio (batch 2)

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Entregue

- `src/pages/Patrimonios.tsx` — `obras` via `useObrasContext()`.
- `src/pages/Contratos.tsx` — `obras` via `useObrasContext()`.
- `src/pages/Veiculos.tsx` — `obras` via `useObrasContext()`.
- `src/pages/Obras.tsx` — `clientes` via `useClientesContext()` (a leitura escopada de obras já vinha de `useObrasEscopo`).

Todas as páginas mantêm `useApp()` apenas para `hasAccess`/`currentPlayer` (auth/perms).

## Restantes (próximos batches)

- Batch 3: `GM.tsx`, `rh/Colaboradores.tsx`, `rh/Funcoes.tsx`, `crm/CRMFunil.tsx`, `financeiro/FinObraDetalhe.tsx`.
- Batch 4: dialogs (`ContratoProfileDialog`, `PatrimonioProfileDialog`, `EmployeeProfileDialog`, `VeiculoProfileDialog`, `ClienteProfileDialog`).

## Próximo

`ARC-002.slice-13` — batch 3 acima.
