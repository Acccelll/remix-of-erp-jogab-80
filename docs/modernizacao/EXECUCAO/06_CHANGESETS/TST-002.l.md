# TST-002.l — Contratos de insumos, requisicoes e planejamento

**Onda:** 6 (bloqueada — NO-GO D-9)  
**Data:** 2026-07-12  
**Escopo:** fechar a bateria pré-Onda 6 travando os últimos repositórios grandes de suprimentos leves e planejamento.

## Alvo

- `src/lib/repositories/insumos.ts` — `insumosRepo`.
- `src/lib/repositories/requisicoes.ts` — `requisicoesRepo`.
- `src/lib/repositories/planejamento.ts` — `riscosRepo`, `licoesAprendidasRepo`, `pacotesTrabalhoRepo`, `restricoesRepo`, `pacoteRestricoesRepo`, `compromissosSemanaisRepo`, `causasNaoConclusaoRepo`.

## Arquivo criado

- `src/lib/__tests__/integracao/repositories-contrato-l.test.ts` — 24 casos.

## Contratos travados

- `insumos`: `listAtivos` com `eq('ativo', true)` + ordem `descricao`, projeções mínimas em `listPrecos`/`listResumoPrecos`, `create/update/toggleAtivo` corretos.
- `requisicoes`: `listAll` com colunas padrão + `order('created_at', desc)` + override; `listResumoObra` com `limit(500)` padrão e configurável; `create/update/updateStatus`.
- `riscos` e `licoes_aprendidas`: projeções fixas de dashboard/resumo; `upsert` alterna insert/update conforme id; `remove` via `delete().eq('id',...)`.
- `pacotes_trabalho`: três projeções (`listDescricao`, `listResumoObra`, `listObra`); `upsert` e `updateStatus`.
- `restricoes`: `listResumo`/`listDashboard`/`listCurto` com projeções distintas; `upsert` alterna verbo; `updateStatus`.
- `pacote_restricoes`: `listComData` inclui `created_at`; `listDashboard` sem.
- `compromissos_semanais.insertRaw` e `causas_nao_conclusao.listChaveLabel` travados.

## Resultado

- Lote L: 24/24 ✔
- Suite total: **654/654** ✔
- `tsgo --noEmit`: verde (nenhuma mudança em código de produção)

## Acumulado TST-002

12 lotes · 46 superfícies · **226 casos**.

## Próximo

`TST-002.m` — cobertura das últimas fatias remanescentes (`obras`, `patrimonios`, `veiculos`) para consolidar TST-002 como pré-condição atendida e permitir revisão do NO-GO da Onda 6.
