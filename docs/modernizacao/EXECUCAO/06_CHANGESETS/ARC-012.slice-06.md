# ARC-012.slice-06 — Hook canônico `useClientes`

**Onda:** 6 · **Achado:** ARC-012 · **Slice:** 06

## Alterações
- **`src/hooks/useClientes.ts`** (novo): encapsula `clientesRepo` com hooks TanStack Query.
  - Leituras: `useClientes` (lista completa), `useClientesResumo` (`id,nome`), `useClienteObrasVinculadas` (contagem).
  - Mutations: `useCreateCliente`, `useUpdateCliente`, `useDeleteCliente` com toast centralizado e invalidação em `onSettled` das chaves `list` + `resumo`.
  - Chaves em `clientesKeys` sob root já registrado `"clientes"` (`list`, `resumo`, `obras-vinculadas/<id>`).
- **`src/contexts/clientes/ClientesStateContext.tsx`**: adapta o único consumidor pré-existente à nova API — troca `useAddCliente` por `useCreateCliente`, envelope `{ id, patch }` no update e mapeia `ClienteRow → Cliente` mantendo `ativa: true` por padrão (coluna ausente na tabela).

## Comportamento
Sem alteração de runtime — nenhum consumidor migrado ainda. `clientesRepo` permanece exportado para testes de contrato e páginas legadas.

## Validação
- `tsgo --noEmit`: limpo.

## Próximo passo
`ARC-012.slice-07` — migrar `FinClientes.tsx` (CRUD principal) para `useClientes`/`useCreateCliente`/`useUpdateCliente`/`useDeleteCliente`/`useClienteObrasVinculadas`.
