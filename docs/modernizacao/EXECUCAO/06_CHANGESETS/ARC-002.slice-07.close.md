# ARC-002.slice-07.close — Encerramento do slice de Funções + Tipos de Documento

**Onda:** 6 · **Prioridade:** P1 · **Tipo:** REF/MIGRAÇÃO
**Achado:** ARC-002 (god-context)

## Consumers migrados

- `src/components/rh/EmployeeFormDialog.tsx` — `funcoes` via `useFuncoesContext()`.
- `src/components/rh/EmployeeProfileDialog.tsx` — `funcoes` + `documentosTipo` via `useFuncoesContext()`; `obras/hasAccess` permanecem em `useApp()`.

`rh/Funcoes.tsx`, `rh/ControleDocumentos.tsx` e `EmployeeDPDialog.tsx` já usavam hooks react-query (`useFuncoes`/`useDocumentoTipos`/`useDeleteDocumentoTipo`) e não dependiam de `useApp()` para funções ou tipos de documento — não requerem migração.

## Critérios preservados

- Sem alteração de UI, validação de NRs, vencimentos ou persistência.
- `useApp()` mantém compatibilidade para consumers não migrados.
- Typecheck (`tsgo`) verde.

## Próximo

Abrir `ARC-002.slice-08` com `PlayersContext` (usuários do sistema).
