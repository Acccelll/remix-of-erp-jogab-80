# PRO-012 · slice-28 — Estado vazio contextual dentro da tabela

**Onda:** 07 · Suprimentos & Contagens Cíclicas
**Contrato:** UI-only, sem alterar dados/persistência.

## Escopo
Quando `rowsExibidas.length === 0`, exibir estado vazio **dentro** do `<tbody>` (colSpan={8}), preservando toolbar (filtro/limpar) e contadores acima da tabela.

## Alterações
- `src/pages/suprimentos/ContagensCiclicas.tsx`:
  - Removida a mensagem inline "Nenhuma contagem em piora no momento." da toolbar (agora vive na tabela).
  - Nova linha de empty state no `<tbody>` com mensagem contextual:
    - Filtro "Somente em piora" ativo → mensagem específica com sugestão de ajuste.
    - Caso geral → "Nenhum registro corresponde aos critérios atuais."
  - Botão secundário "Limpar filtros e ordenação" aparece dentro do empty state quando `temPreferenciaTabela` é verdadeiro (atalho redundante ao da toolbar, mais próximo do contexto).
  - Toolbar, contadores globais (`X de Y`) e cabeçalhos da tabela permanecem visíveis.

## Validação
- `bunx tsgo --noEmit` sem erros.
- Comportamento visual: toolbar e contadores continuam renderizando; tabela mantém cabeçalhos; corpo troca linhas por mensagem contextual quando vazio.

## Próximo passo
`PRO-012.slice-29` — reset opcional do filtro "Somente em piora" via botão inline do empty state quando ele for a única causa da lista vazia.
