# PRO-012 · slice-22 — Coluna de tendência de ignoradas por contagem

## Escopo
Adicionar coluna "Tendência ign." à tabela principal de contagens cíclicas,
usando a primitiva `<Sparkline>` (slice-21) com as últimas 10 importações da
contagem.

## Alterações
- `src/pages/suprimentos/ContagensCiclicas.tsx`
  - Novo `<th>` "Tendência ign." antes de "Ação".
  - `<td>` renderiza sparkline colorido (`text-destructive` quando houver
    ignoradas; `text-muted-foreground` quando série zerada) + total agregado.
  - Fallback "—" para contagens sem auditorias de importação.
  - Ordenação e slice `-10` calculados por linha (custo desprezível).

## Contrato de execução
- UI-only, sem Cloud/RLS/schema. Reaproveita `<Sparkline>` existente.

## Validação
- `bunx tsgo --noEmit` sem erros.
- Manual: contagens com múltiplas importações exibem série; ranking visual
  facilita identificar deterioração recente.

## Próximo passo sugerido
`PRO-012.slice-23` — ordenação da tabela principal por tendência (delta entre
último e primeiro ponto) e por total de ignoradas.
