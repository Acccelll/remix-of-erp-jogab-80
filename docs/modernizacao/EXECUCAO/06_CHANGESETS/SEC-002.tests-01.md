# SEC-002.tests-01 — Enumeração de `test_id` por lote

**Onda:** 2 (preparatória — bloqueada por fechamento de `SEC-001`)  
**Ref:** R183 · **Round:** compact-01 (aceleração — 1 doc útil = 1 round)  
**Data:** 2026-07-12  
**Escopo:** enumera cada `test_id` derivado da matriz `SEC-002.inventory-02.csv` conforme convenção `SEC-002.inventory-03 §4`, com fixture, oráculo e comando de execução. **Zero migração, zero código de produção.**

## 1. Convenção (recap)

`SEC-002.T-<lote>-<tabela>-<caso>`, onde `<caso>` cobre os quatro comandos (SELECT/INSERT/UPDATE/DELETE) na mesma execução.

## 2. Fixture-base (`_lib/fixtures.sql`, já existente)

- Empresas: `E1`, `E2`.
- Obras: `O1@E1`, `O2@E2`.
- Boards: `B1@E1`, `B2@E2`; cards `C1@B1`, `C2@B2`.
- Usuários: `U_GM` (gm), `U_E1` (membro E1), `U_E2` (membro E2), `U_OBR1` (membro O1), `U_BRD1` (membro B1), `U_CRD1` (membro C1), `U_DP` (has_role dp), `U_FIN` (has_role financeiro), `U_OUT` (autenticado sem escopo), `anon`.
- JWT injection via `_lib/jwt.sql` → `set_config('request.jwt.claims', ...)`.

## 3. Enumeração por lote

### 3.1 Lote B — registries (13 tabelas)

Casos por tabela: `{gm, member, anon}`. Oráculo: `anon` retorna 0 linhas em SELECT e falha em escrita; `member` lê tudo, escreve só se `has_role(admin|setor)`; `gm` lê/escreve tudo.

Tabelas: `audit_logs`, `centros_custo_totvs`, `clientes`, `documento_tipos`, `empresas`, `formas_pagamento`, `funcoes`, `grupos_negociacao`, `insumos`, `player_credentials`, `players`, `user_empresas`, `user_roles`.

Total: `13 × 3 = 39` `test_id`.

### 3.2 Lote C — escopo empresa/obra (24 tabelas)

Casos: `{gm, member, outsider, anon}`. Oráculo: `member` de `E1` só lê/escreve linhas de `E1`; `outsider` (`U_E2`) lê 0 linhas de `E1`; `gm` bypass; `anon` negado.

Tabelas: `aditivos_contrato`, `bms_previstas`, `bms_redistribuicao`, `colaboradores`, `colaboradores_documentos`, `compromissos_semanais`, `contratos`, `cronograma_atividades`, `cronograma_baseline`, `cronograma_marcos`, `documentos`, `licoes_aprendidas`, `mobilizacoes`, `movimentacoes_patrimonio`, `obras`, `obra_membros`, `oportunidades`, `pacotes_trabalho`, `patrimonios`, `rdos`, `restricoes`, `veiculos`, `veiculos_custos`, `vinculos_colaborador_obra`.

Total: `24 × 4 = 96` `test_id`.

### 3.3 Lote D — DP + Financeiro (14 tabelas)

Casos: `{gm, member, outsider, anon, segregation}`. `segregation` verifica que solicitante ≠ aprovador em `solicitacoes_financeiras` e `controle_despesas`.

Tabelas: `controle_despesas`, `custo_colaborador_competencia`, `decimo_terceiro`, `dp_holerite`, `financeiro_entradas`, `financeiro_lancamentos`, `financeiro_saidas`, `fopag_entries`, `historico_salarial`, `horas_extras`, `provisoes`, `solicitacoes_financeiras`, `snapshots_financeiros`, `formas_pagamento_alcadas`.

Total: `14 × 5 = 70` `test_id`.

### 3.4 Lote E — boards/cards (22 tabelas)

Casos: `{gm, owner, member, outsider, anon}`. `owner` é o criador; `member` é vinculado por `board_membros`/`card_membros`; `outsider` é autenticado sem vínculo.

Tabelas: `boards`, `board_campos`, `board_listas`, `board_membros`, `cards`, `card_anexos`, `card_atividades`, `card_board_posicao`, `card_campos_valores`, `card_checklist_itens`, `card_comentarios`, `card_custom_field_valores`, `card_grupos_negociacao`, `card_label_links`, `card_labels`, `card_local`, `card_membros`, `card_membros_externos`, `card_recursos`, `card_secoes_visiveis`, `card_setores`, `card_views_salvas`.

Total: `22 × 5 = 110` `test_id`.

## 4. Consolidado

| Lote | Tabelas | Casos/tabela | Total `test_id` |
|------|---------|--------------|-----------------|
| B    | 13      | 3            | 39              |
| C    | 24      | 4            | 96              |
| D    | 14      | 5            | 70              |
| E    | 22      | 5            | 110             |
| **Total** | **73** | —          | **315**         |

Confere com matriz: 73/73 tabelas cobertas, 0 residuais.

## 5. Formato do arquivo executável

Para cada `test_id`, o próximo changeset (`SEC-002.tests-02`) gera um arquivo pgTAP em `supabase/tests/sec-002/batch-<lote>/<tabela>__<caso>.sql` com o esqueleto:

```sql
BEGIN;
SELECT plan(4);  -- SELECT, INSERT, UPDATE, DELETE
\i _lib/fixtures.sql
\i _lib/jwt.sql
SELECT set_jwt_claims_for('<caso>');  -- helper existente
-- 4 asserts (um por comando), com oráculo do caso
SELECT * FROM finish();
ROLLBACK;
```

Os arquivos já existentes em `supabase/tests/sec-002/batch-{B,C,D,E}/*_rls.sql` cobrem parcialmente lotes B/C/E via testes agregados por tabela; `SEC-002.tests-02` decompõe em `<tabela>__<caso>.sql` para diagnóstico determinístico.

## 6. Ordem de execução recomendada (dependência de helpers)

1. `SEC-002.helpers-03` — implementar `user_in_empresa`, `user_in_obra`, `user_in_board`, `user_in_card` (se ainda ausentes; `helpers-02` cobre parte).
2. `SEC-002.tests-02` — decomposição por caso.
3. `SEC-002.batch-B.migration` — políticas B (base para dropdowns globais).
4. `SEC-002.batch-C.migration` — políticas C (empresa/obra).
5. `SEC-002.batch-D.migration` — políticas D (DP/financeiro; segregação de duty).
6. `SEC-002.batch-E.migration` — políticas E (boards/cards).
7. `SEC-002.gate-01` — script `verify-sec-002-permissive.mjs` (0 políticas `USING(true)` remanescentes).

## 7. Riscos e mitigações

| Risco | Mitigação |
|-------|-----------|
| `user_in_card` recursa por `card_membros` | função `SECURITY DEFINER` com `set search_path = public`, sem RLS interna |
| `segregation` em `solicitacoes_financeiras` esbarra em fluxo antigo | teste marcado `TODO` até `SEC-002.batch-D` definir coluna `aprovador_id` distinta |
| Contagem 315 subir com colunas de dados sensíveis não modeladas hoje | matriz é iterativa; `inventory-05` reabre se surgir tabela nova |

## 8. Impacto em produção

**Nenhum.** Enumeração documental. Nenhuma migração, nenhum arquivo de teste `.sql` novo criado neste changeset (fica para `SEC-002.tests-02`).

## 9. Não tocado

`_lib/`, `migrations/`, `src/`, `supabase/functions/`, `supabase/migrations/`, `.env`, banco, verifier `verify-wave-2-preconditions.mjs`, `INDEX.md`, apply-map.

---

**ONDA ATUAL:** WAVE 2 — OPEN; `INDEX.md`=44 linhas; PC1..PC7 verdes; treadmill `pc4-bump-*` encerrado; matriz SEC-002 completa (73 tabelas, 315 `test_id` planejados).  
**PRÓXIMO PASSO:** R184 — `SEC-002.helpers-03` (implementar helpers `user_in_*` faltantes) OU `SEC-002.tests-02` (decompor pgTAP por `test_id`). Decisão: começar por `helpers-03` porque os testes de lote C/E dependem dos helpers estarem no schema.
