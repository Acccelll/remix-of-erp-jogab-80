# ARC-004.slice-05 — Veículos migrados para paradigma único (TanStack Query mutations)

**Onda:** 6 · **Achado:** ARC-004

## Escopo
Migra `veiculos` do CRUD imperativo para mutations TanStack Query.

## Mudanças
- **`src/hooks/useVeiculos.ts`**: adiciona `useAddVeiculo`, `useUpdateVeiculo`, `useDeleteVeiculo` com padrão canônico (`onMutate` snapshot → `onError` restore → `onSettled` invalidate).
- **`src/contexts/veiculos/VeiculosStateContext.tsx`**: reescrito — `veiculos = useVeiculos().data ?? []`; `setVeiculos` vira wrapper `qc.setQueryData` compatível com `React.Dispatch<SetStateAction>` para preservar mobilizações.
- **`src/contexts/app/useVeiculosState.ts`**: removido.

## Nota
Regra de vínculo `motoristaId → obraAtualId` e histórico automático de flags permanece no provider por depender de colaboradores/currentPlayer e do snapshot atual do cache.

## Validação
- `tsgo --noEmit` limpo.

## Próximo
`ARC-004.slice-06` — aplicar o mesmo padrão a `contratos` (`useContratos` + mutations otimistas; `ContratosStateContext` consumindo cache; `useContratosState` removido).