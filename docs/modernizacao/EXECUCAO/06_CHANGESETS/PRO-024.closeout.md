# PRO-024 · closeout — Ativos: manutenção preventiva programada

**Onda:** 07 · **Marco:** M13 · **Status:** concluído em 3 slices.

## Entregas
- **slice-01** — Regra pura `statusPreventiva` (dimensões km, horímetro, dias) com casos `desconhecido/ok/proxima/vencida` e testes verdes.
- **slice-02** — Badge visual (`proxima`/`vencida`) no `VeiculoProfileDialog`, com tooltip listando os motivos.
- **slice-03** — Seção "Preventivas de frota" no `NotificationBell`, somada ao badge, com link para `/veiculos` e gated por `hasAccess("frotas","visualizar")` + `isVisivel(...)`.

## Aceite (Etapa 5.5)
> _"veículos/patrimônios sinalizam preventivas próximas/vencidas por km, horímetro e data."_

Coberto: regra pura auditável, visibilidade no perfil do ativo e alerta na central de notificações.

## Fora do escopo (não regride)
- Push/e-mail (segue no roadmap de `PRO-030` + canais).
- Exibição na tabela principal `Veiculos.tsx` (evitar N+1 fetch).
- Programação automatizada de ordens de manutenção.

## Validação
- `bunx tsgo --noEmit` — OK em todos os slices.
- `bunx vitest run src/lib/frotas/__tests__/preventiva.test.ts` — verde.

## Próximo achado
`PRO-025.slice-03` — Lean: lookahead → compromissos manuais; repetir semana anterior (já iniciado na Onda 07).
