# SEC-002.tests-02.teardown-01.materialize-01 — Materialização do teardown da suíte SEC-002

**Onda:** 2 (OPEN)  
**Ref:** R196' · **Round:** compact-01  
**Data:** 2026-07-12  
**Escopo:** materializa o contrato `SEC-002.tests-02.teardown-01.spec-01` (R195') em `supabase/tests/sec-002/_lib/teardown.sql`, com manifest `.github/sec002-evidence/tests-02-teardown-01/manifest.json`. Impacto em produção: nenhum.

## 1. Write set físico (2 arquivos)

| # | Caminho | Ação |
|---|---------|------|
| 1 | `supabase/tests/sec-002/_lib/teardown.sql` | substituição controlada do teardown antigo (8 linhas) por teardown idempotente completo para fixtures transitórias R192b |
| 2 | `.github/sec002-evidence/tests-02-teardown-01/manifest.json` | manifest com hash baseline/after, refs e invariantes TDR01-1…TDR01-10 |

## 2. Hashes

| Arquivo | Baseline | After |
|---------|----------|-------|
| `_lib/teardown.sql` | `48591eff4bfa67447b332039b29f1f5ec2e5593ad0b538acfc517af8efb9b986` | `d3bb3e2db7af2c19a7eb8d66ee411c843f17f3f58f1349664efe1e1e165370b0` |

## 3. Limpezas materializadas

Ordem canônica implementada:

1. `solicitacoes_financeiras` fixture `SF1`, protegida por `information_schema.columns`.
2. `user_setores` para `U_DP/U_FIN`, protegida por `information_schema.tables`.
3. `card_membros` de `C1/U_CRD1` e `C2/*`.
4. `card_board_posicao` de `C2`.
5. `cards` fixture `C2`.
6. `board_membros` de `B1/U_BRD1`.
7. `obra_membros` de `U_OBR1/O1`.
8. `user_roles.gm` para `U1/U2/UGM` (baseline preservado semanticamente).
9. `profiles` dos 6 users adicionais R192b.

Fixtures base R132 (`E1/E2`, `O1/O2`, `B1/B2`, `C1`, `U1/U2/UGM`) permanecem preservadas, exceto o papel transitório `gm` já removido pelo teardown antigo.

## 4. Invariantes verificadas (TDR01-1…TDR01-10)

| ID | Resultado |
|----|-----------|
| TDR01-1 | ✅ Idempotência estática: todas as deleções são filtradas por chaves/UUIDs fixos; tabelas opcionais têm guards. |
| TDR01-2 | ✅ Zero `create`, `alter`, `drop`, `grant`, `revoke`, `set role`, `set_config`, `security definer`. |
| TDR01-3 | ✅ Todo `delete` tem `where` por UUID canônico SEC-002. |
| TDR01-4 | ✅ `user_setores` e `solicitacoes_financeiras` protegidas por `information_schema`. |
| TDR01-5 | ✅ Ordem dependências→entidades implementada. |
| TDR01-6 | ✅ Cleanup de `user_roles.gm` para `U1/U2/UGM` preservado. |
| TDR01-7 | ✅ Fixtures base R132 preservadas. |
| TDR01-8 | ✅ Manifest registra SHA-256 antes/depois e invariantes. |
| TDR01-9 | ✅ `node scripts/sec002/verify-wave-2-preconditions.mjs --json` retornou `{ ok:true }`. |
| TDR01-10 | ✅ Arquivo restrito à suíte pgTAP SEC-002; impacto em produção nulo. |

## 5. Não tocado

`src/`, `supabase/migrations/`, edge functions, workflows, apply-map, `INDEX.md`, `scripts/sec002/*`, batches B/C/D/E e banco real.

## 6. Habilitação a jusante

O teardown completo remove um bloqueio de isolamento para execução repetida da suíte `tests-02`. O gerador dos 315 pgTAP continua bloqueado por D-6; helpers-03 continua bloqueado por D-3 + helpers-02 deployado.

## 7. Impacto em produção

**Nenhum.** Alteração apenas em arquivo auxiliar de testes. PC1..PC7 permanecem verdes; `INDEX.md`=44 linhas; `EXPECTED_INDEX_ROWS=44.