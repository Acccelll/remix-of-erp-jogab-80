# TST-002.k — Contratos de rdo e inspecoes

**Onda:** 6 (bloqueada — NO-GO D-9)  
**Data:** 2026-07-12  
**Escopo:** travar contratos das leituras diárias (`rdo`) e auditorias (`inspecoes`/`nao_conformidades`) antes das refatorações do módulo de campo.

## Alvo

- `src/lib/repositories/rdo.ts` — `rdoRepo`.
- `src/lib/repositories/inspecoes.ts` — `inspecoesRepo`.

## Arquivo criado

- `src/lib/__tests__/integracao/repositories-contrato-k.test.ts` — 15 casos.

## Contratos travados

- `rdo`:
  - `listPorObra`: projeção padrão `id, data, status, clima, observacoes`, override de colunas, filtro `obra_id`, ordenação `data desc`, `[]` em `data null`, propagação de erro.
  - `getById`: 17 colunas de detalhe (climas manhã/tarde/noite, trabalhavel_*, owner_id) + `eq('id',...)` + `.single()`.
  - `create`/`update`: verbos e `eq('id',...)`.
- `inspecoes`:
  - `list`: projeção padrão + `order('data_inspecao', desc)` + override.
  - `getById`: 19 colunas de detalhe (score, aprovado, assinatura, geo, card_id, cronograma_item_id) + `.single()`.
  - `create`/`update`.
  - `listNaoConformidades`: tabela `nao_conformidades`, projeção padrão, `order('created_at', desc)`, override.

## Resultado

- Lote K: 15/15 ✔
- Suite total: **630/630** ✔
- `tsgo --noEmit`: verde (nenhuma mudança em código de produção)

## Acumulado TST-002

11 lotes · 37 superfícies · **202 casos**.

## Próximo

`TST-002.l` — `insumos` + `planejamento` + `requisicoes` (últimos repositórios grandes antes do fechamento da bateria pré-Onda 6).
