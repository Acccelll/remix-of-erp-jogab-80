# SEC-002 · tests-02 / teardown-01 / self-register-01 / spec-01

- **Ref:** R197'
- **Tipo:** especificação (mirror; sem escrita)
- **Onda:** 2 — OPEN, gate PC1..PC7 verde
- **Apply-map:** `v2026-07-12.4` (a preservar)
- **Antecessores:** R195' (spec teardown-01), R196' (materialização teardown-01)
- **DRI:** Cappucceno (GM) / Lovable

## 1. Objetivo

Especificar o self-register do apply `tests-02-teardown-01` (R196') como **linha 45** de `.github/sec002-evidence/INDEX.md`, sob o mesmo contrato dos self-registers anteriores (R139/R140, R145/R146). Nenhum toque em produção, testes, runner, verificador, migrations, workflows, batches, helpers ou apply-map.

## 2. Write set físico (autorizado — 2 arquivos, exatos)

1. `.github/sec002-evidence/INDEX.md` — **append** de 1 (uma) linha canônica ao final da tabela (linha útil 45); `Última atualização` bumpada para o timestamp UTC do R198'; linhas 1–44 preservadas byte-a-byte (hash pré-append: `3e65273152b3e85a0e99cff4f978d3d6457626f9e7606ff3fbbb058e93e97068`).
2. `.github/sec002-evidence/tests-02-teardown-01-self-register-01/manifest.json` — manifest próprio do self-register.

Qualquer 3º arquivo tocado → **REJECT**.

## 3. Linha canônica esperada (linha 45)

```
| 45 | tests-02-teardown-01 | .github/sec002-evidence/tests-02-teardown-01/manifest.json | docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.tests-02.teardown-01.materialize-01.md | v2026-07-12.4 | R132, R183, R192b, R195', R196' |
```

## 4. Invariantes (TDR01SR-1 … TDR01SR-10)

- **TDR01SR-1** — `INDEX.md` termina com exatamente 45 linhas úteis; linhas 1–44 idênticas ao hash `3e65273152b3e85a0e99cff4f978d3d6457626f9e7606ff3fbbb058e93e97068`.
- **TDR01SR-2** — a linha 45 aponta para `tests-02-teardown-01/manifest.json` (R196'), com o conjunto de refs listado em §3.
- **TDR01SR-3** — `Apply-map: v2026-07-12.4` preservado literalmente.
- **TDR01SR-4** — `Última atualização` é o único campo do cabeçalho alterado.
- **TDR01SR-5** — manifest inclui `schema_version: v2026-07-12.4`, `apply_id: tests-02-teardown-01-self-register-01`, `ref: R198'`, `spec_ref: R197'`, `index_row: 45`, `apply_map_version_preserved: true`, `expected_index_rows_before: 44`, `expected_index_rows_after: 45`, `hashes.INDEX.md` (SHA-256 pós-append).
- **TDR01SR-6** — `scripts/sec002/verify-wave-2-preconditions.mjs` **NÃO** é tocado; bump `EXPECTED_INDEX_ROWS: 44→45` delegado a R199'.
- **TDR01SR-7** — nenhum toque em `supabase/tests/`, `_lib/teardown.sql`, `_lib/fixtures.sql`, `_lib/jwt.sql`, `_lib/asserts.sql`, helpers, `batch-*/`, runner, `src/`, edge functions, migrations, workflows, docs fora do SEC-001.README/CHANGESETS.
- **TDR01SR-8** — sem segredos, sem `service_role_key`, sem URLs de banco, sem project refs.
- **TDR01SR-9** — gate PC1..PC7 ficará temporariamente `off_until_pc4_bump` entre R198' e R199'; documentado no manifest.
- **TDR01SR-10** — manifest do apply `tests-02-teardown-01/manifest.json` (R196', hash conhecido do teardown.sql após = `d3bb3e2db7af2c19a7eb8d66ee411c843f17f3f58f1349664efe1e1e165370b0`) permanece intocado.

## 5. Fora de escopo (delegado)

- **R198'** — materialização (append linha 45 + manifest self-register).
- **R199'** — spec + materialização do bump PC4 `EXPECTED_INDEX_ROWS: 44→45`, reativando PC1..PC7.
- Deploy de `helpers-02` permanece bloqueado por GO GM (R132); nenhum vínculo aqui.
- `helpers-03` permanece bloqueado por D-3 + deploy helpers-02 (R194/R195); nenhum vínculo aqui.

## 6. Rejeição automática

PR que toque qualquer arquivo fora dos 2 do write set, altere linhas 1–44 do `INDEX.md`, mude `Apply-map`, edite o verificador, altere o manifest de R196', ou introduza rollback → **REJECT**.
