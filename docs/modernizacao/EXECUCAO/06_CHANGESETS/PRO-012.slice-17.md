# PRO-012 · slice-17 — Filtro rápido e contadores no painel de ignoradas

## Escopo

O painel expandido de cada auditoria de importação XLSX agora permite
filtrar rapidamente as linhas ignoradas por código ou motivo e exibe os
contadores por motivo daquela auditoria específica.

## Alterações

### `src/pages/suprimentos/ContagensCiclicas.tsx`
- Import `Search` de `lucide-react` para o campo de filtro.
- Helper `normalizarTextoBusca` para busca sem acento e case-insensitive.
- Helper `contarIgnoradasPorMotivo` para agregação local por auditoria.
- Novo estado `auditIgnoredFilters: Record<string, string>` para manter
  um filtro independente por auditoria expandida.
- `fecharExecucao` limpa os filtros ativos junto com expansão/destaque.
- Painel expandido passa a exibir badges com contadores por motivo.
- Tabela de ignoradas passa a filtrar por código ou motivo e apresenta
  estado vazio quando não houver correspondência.

## Contrato de execução

- Escopo restrito a `PRO-012`, UI-only.
- Sem alterações em schema, Cloud, RLS, storage ou tipos persistidos.
- Sem alteração no formato do histórico já salvo em localStorage.
- Busca normalizada apenas para visualização; não altera dados gravados.

## Validação

- `bunx tsgo --noEmit` sem erros novos.

## Próximo passo sugerido

`PRO-012.slice-18` — permitir exportar apenas as linhas ignoradas da
auditoria expandida, respeitando o filtro visual aplicado.