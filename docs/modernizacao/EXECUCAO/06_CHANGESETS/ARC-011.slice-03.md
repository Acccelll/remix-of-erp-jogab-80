# ARC-011.slice-03 — `fornecedoresRepo` marcado como `@internal`

## Escopo
Selar a fonte única de fornecedores: o repositório passa a ser explicitamente camada interna consumida apenas pelos hooks em `@/hooks/useFornecedores`.

## Alterações
- `src/lib/repositories/fornecedores.ts`: JSDoc `@internal ARC-011` acrescentado no objeto `fornecedoresRepo`, apontando os cinco hooks públicos e reconhecendo os testes de contrato como única exceção.

## Validação
- `tsgo --noEmit` limpo (sem mudança de tipos).
- Sem alteração de comportamento em runtime.

## Encerramento ARC-011
Domínio Fornecedores alinhado ao padrão ARC-010 (Obras): leitura + mutações via TanStack Query, invalidação centralizada, repositório selado como `@internal`.

## Próximo
ARC-012.slice-01 — replicar o padrão em outro domínio de alta reincidência (`clientesRepo` / `insumosRepo`), começando pelo mapeamento de consumidores.
