# ARC-003.c.1 — Repositório `planejamento` (riscos + lições aprendidas)

## Escopo
Centralizar chamadas diretas a `supabase.from("riscos")` e `supabase.from("licoes_aprendidas")` no novo `src/lib/repositories/planejamento.ts`.

## Novo repositório
`src/lib/repositories/planejamento.ts`:
- `riscosRepo.listDashboard()` — projeção usada em DashboardLean.
- `riscosRepo.listResumoDescricao()` — projeção usada em LicoesAprendidas.
- `riscosRepo.upsert(id, payload)` / `riscosRepo.remove(id)`.
- `licoesAprendidasRepo.upsert(id, payload)` / `licoesAprendidasRepo.remove(id)`.

## Call-sites refatorados (10)
- `src/components/obra/RiscosTab.tsx` (salvar/excluir).
- `src/components/obra/OcorrenciasTab.tsx` (bloco lições — salvar/remover).
- `src/pages/planejamento/DashboardLean.tsx` (leitura riscos).
- `src/pages/planejamento/LicoesAprendidas.tsx` (leitura riscos + excluir lição).
- `src/pages/planejamento/Riscos.tsx` (excluir risco).

## Verificação
- `rg 'supabase.from\("(riscos|licoes_aprendidas)"\)' src/` retorna apenas ocorrências dentro do próprio `planejamento.ts`.
- `tsgo --noEmit` verde.

## Pendente ARC-003.c.2
`cards/*`, `board_*`, `cronograma_*`, `pacotes_trabalho`, `pacote_restricoes`, `restricoes`, `compromissos_semanais`, `ocorrencias`, `obra_localizacoes`, `aditivos_contrato`, `bms_previstas`, `causas_nao_conclusao`, `rdo_efetivo`, `rdo_ocorrencias`, `import_validation_runs`.
