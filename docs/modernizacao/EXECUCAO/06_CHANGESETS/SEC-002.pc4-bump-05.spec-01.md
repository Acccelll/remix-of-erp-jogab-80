# SEC-002 · pc4-bump-05 · spec-01 (R168)

**Onda:** 2 (OPEN) · **Apply-map:** v2026-07-12.4 · **Gate:** restaura PC1..PC7 = green após apply

## Objetivo
Autorizar o bump da constante `EXPECTED_INDEX_ROWS` de `37` → `38` em `scripts/sec002/verify-wave-2-preconditions.mjs`, refletindo o self-register de R167 (linha 38 = `pc4-bump-04`).

## Write set físico autorizado (R169 — 2 arquivos)
1. `scripts/sec002/verify-wave-2-preconditions.mjs` — linha 10:
   - antes: `const EXPECTED_INDEX_ROWS = 37;`
   - depois: `const EXPECTED_INDEX_ROWS = 38;`
2. `.github/sec002-evidence/pc4-bump-05/manifest.json` — manifest (`schema_version: v2026-07-12.4`, `expected_index_rows_before: 37`, `expected_index_rows_after: 38`, `auto_applied: false`, `verifier_gate: on`).

## Invariantes PC4B5-1…PC4B5-8
- **PC4B5-1** Diff no verificador = 1 linha alterada (linha 10), 0 fora dela.
- **PC4B5-2** Nenhuma outra constante numérica alterada.
- **PC4B5-3** Lógica PC1/PC2/PC3/PC5/PC6/PC7 preservada byte-a-byte.
- **PC4B5-4** Loop `for (let i = 1; i <= EXPECTED_INDEX_ROWS; …)` inalterado.
- **PC4B5-5** `Apply-map: v2026-07-12.4` preservado.
- **PC4B5-6** `INDEX.md` NÃO é tocado por R169.
- **PC4B5-7** Após R169: `node verify-wave-2-preconditions.mjs --json` → `ok: true` (PC1..PC7).
- **PC4B5-8** Manifest `pc4-bump-05/manifest.json` gravado.

## Invariantes transversais TVBP9-1…TVBP9-8
- **TVBP9-1** Sem tocar `supabase/migrations/`.
- **TVBP9-2** Sem tocar `INDEX.md`, batch-B/pc4-bump-01/pc4-bump-02/pc4-bump-03/pc4-bump-04 artefatos.
- **TVBP9-3** Sem tocar `scripts/sec002/_lib/`, `helpers/`, `batches/`.
- **TVBP9-4** Sem tocar `src/`, `tests/`, workflows.
- **TVBP9-5** Sem tocar apply-map ou outros manifests.
- **TVBP9-6** Sem segredos, chaves ou tokens.
- **TVBP9-7** Nenhuma execução real contra DB.
- **TVBP9-8** Self-register (linha 39) fica delegado a R170+.

## Nota de auto-recorrência
Ciclo `pc4-bump-N` mantido conforme padrão canônico; nenhuma política alterada em R168.

## Estado esperado pós-R169
- Verificador linha 10 = `const EXPECTED_INDEX_ROWS = 38;`.
- Gate PC1..PC7 = green.
- Onda 2 permanece OPEN; nenhum artefato de dados/produção alterado.