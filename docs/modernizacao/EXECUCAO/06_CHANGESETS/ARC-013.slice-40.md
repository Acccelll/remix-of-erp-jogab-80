# ARC-013 · slice-40 — CenariosSheet via useCronograma

## Escopo
Encerrar consumidores de UI de `@/lib/repositories/cronograma` migrando `CenariosSheet.tsx`.

## Alterações
- `src/hooks/useCronograma.ts`: expõe `cronogramaCenariosFns` e `cronogramaCenarioItensFns` (spread dos repos correspondentes).
- `src/components/obra/CenariosSheet.tsx`: import trocado para `@/hooks/useCronograma` com aliases (`cronogramaCenariosFns as cronogramaCenariosRepo`, `cronogramaCenarioItensFns as cronogramaCenarioItensRepo`). Corpo intacto.

## Validação
- `tsgo --noEmit`: limpo.
- `rg "@/lib/repositories/cronograma" src/components src/pages`: só o `import type` de `CronogramaItemRow` em `PacotesTrabalho` (slice-39).

## Fora de escopo
- `src/lib/cronograma/*.ts` (recalcular-cpm, persistir-calendarios) — camada de domínio.
- `src/lib/__tests__/**` — testes de contrato do repo.
