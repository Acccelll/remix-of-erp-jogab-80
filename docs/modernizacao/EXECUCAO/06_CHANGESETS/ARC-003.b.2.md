# ARC-003.b.2 — Canalizar tabelas restantes de suprimentos via `suprimentosRepo`

**Onda:** 3 · **Prioridade:** P1 · **Tipo:** LOTE (parte B.2 de ARC-003)

## Escopo

Remover as 26 chamadas `supabase.from()` remanescentes em `src/pages/suprimentos/*` para as tabelas `cotacoes`, `cotacao_propostas`, `ordem_compra_itens`, `orcamento_itens`, `composicoes`, `composicao_itens`, `estoque_saldos`, `alcadas_aprovacao` e `card_grupos_negociacao`.

## Mudanças

### `src/lib/repositories/suprimentos.ts` — novos métodos

- **cotacoes:** `listCotacoes`, `createCotacao`, `fecharCotacao`.
- **cotacao_propostas:** `listPropostas`, `createProposta`, `setPropostaEscolhida` (transação leve zera todas + escolhe), `deleteProposta`.
- **ordem_compra_itens:** `createOcItem`, `deleteOcItem`.
- **orcamento_itens:** `listOrcamentoItens(cols)`, `createOrcamentoItem`, `deleteOrcamentoItem`.
- **composicoes/composicao_itens:** `listComposicoesAtivas`, `listComposicoesTodas(cols)`, `updateComposicao`, `toggleComposicaoAtiva`, `createComposicaoReturningId`, `listComposicaoItens(cols)`, `insertComposicaoItens`, `deleteComposicaoItensByComposicao`.
- **estoque_saldos:** `listEstoqueSaldos(cols)`.
- **alcadas_aprovacao:** `listAlcadas`, `createAlcada`, `deleteAlcada`.
- **card_grupos_negociacao:** `listGruposNegociacao`, `createGrupoNegociacao`, `renameGrupoNegociacao`, `deleteGrupoNegociacao`, `listCardsGrupoNegociacao` (contagem em `cards` para gate de exclusão).

### Call-sites refatorados

- `pages/suprimentos/Cotacoes.tsx` — 7 chamadas → `suprimentosRepo.*`.
- `pages/suprimentos/OrdensCompra.tsx` — 6 chamadas → `suprimentosRepo.*` + `requisicoesRepo.listAll`.
- `pages/suprimentos/GruposNegociacao.tsx` — 4 chamadas → `suprimentosRepo.*`.
- `pages/suprimentos/Estoque.tsx` — 1 chamada.
- `pages/suprimentos/CurvaABC.tsx` — 2 chamadas.
- `pages/suprimentos/Orcamento.tsx` — 4 chamadas.
- `pages/suprimentos/Composicoes.tsx` — 4 chamadas.
- `pages/suprimentos/Alcadas.tsx` — 3 chamadas.

Padrão para `Promise.all` preservado: repo retorna dados; adaptador `.then((data) => ({ data, error: null as any }))` mantém o destructuring atual dos consumidores.

## Validação

- `rg 'supabase\.from\(' src/pages/suprimentos` → zero.
- `tsgo --noEmit` verde.

## Próximo (ARC-003.c)

Lote C — `components/cards/*`, `components/obra/*` e `pages/planejamento/*` restantes cobrindo `card_*`, `cronograma_*`, `riscos`, `pacotes_trabalho`, `licoes_aprendidas`, `ocorrencias`, `obra_localizacoes`, etc.
