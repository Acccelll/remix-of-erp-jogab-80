# ARC-013 · Slice 22 — Encapsular `cardCustomFieldValoresRepo` em `useCards`

## Escopo
Migrar o uso direto de `cardCustomFieldValoresRepo` para hook canônico em `src/hooks/useCards.ts`, removendo o import direto em `CardGenericoDialog`.

## Alterações
1. **`src/hooks/useCards.ts`**
   - Import interno de `cardCustomFieldValoresRepo`.
   - `cardsKeys.customFieldValores(cardId)` (`["card-custom-field-valores", cardId]`).
   - Helper `invalidateCustomFieldValores`.
   - Mutation `useRemoveCardCustomFieldValoresByCard` para limpeza da seção `campos_custom`.

2. **`src/lib/query-keys.ts`**
   - Registro do prefixo `card-custom-field-valores` para uso com `queryKey()` tipado.

3. **`src/components/cards/CardGenericoDialog.tsx`**
   - Remove `cardCustomFieldValoresRepo` do import de `cardsExtra`.
   - Remoção dos valores customizados passa a usar `useRemoveCardCustomFieldValoresByCard`.

## Validação
- `tsgo --noEmit`: limpo.
- `rg cardCustomFieldValoresRepo src`: apenas `useCards.ts` (canônico), repo em `cardsExtra.ts` e teste de contrato (`repositories-contrato-f.test.ts`).
- Semântica preservada.

## Próximo passo
`ARC-013.slice-23` — encapsular `cardBoardPosicaoRepo` em hooks/queryFns canônicos e migrar componentes que movem/copiam cards entre quadros.