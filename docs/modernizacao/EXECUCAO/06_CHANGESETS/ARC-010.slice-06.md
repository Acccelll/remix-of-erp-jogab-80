# ARC-010.slice-06 — Depreciação pública de `obrasRepo.listResumo*`

## Escopo
Sinalizar formalmente no repositório que as três variantes `listResumo*` estão descontinuadas em runtime; a fonte única passa a ser `useObras()`.

## Alterações
- `src/lib/repositories/obras.ts`: JSDoc `@deprecated ARC-010` acrescentado em `listResumo`, `listResumoComCodigo` e `listResumoComCodigoNome`, com ponteiro para `@/hooks/useObras`. Nenhuma alteração de comportamento — apenas metadado para IDEs e code review.

## Motivação
Após slices 01–05, nenhum consumidor de runtime chama esses métodos; permanece apenas cobertura de contrato em `src/lib/__tests__/integracao/repositories-contrato*.test.ts`. A anotação evita reintrodução acidental em novas páginas.

## Validação
- `tsgo --noEmit` limpo.
- `rg "obrasRepo.listResumo" src/ | grep -v __tests__` → sem ocorrências.

## Próximo
ARC-010.slice-07 — auditar/decidir migração dos testes de contrato; remover métodos `listResumo*` do `obrasRepo` após confirmação com governança da Onda 6.
