# ARC-013 · Slice 20 — Encapsular `cardComentariosRepo` em `useCards`

## Escopo
Migrar leituras/inserts de `cardComentariosRepo` para hooks/queryFns canônicos em `src/hooks/useCards.ts`, removendo imports diretos em `CardGenericoDialog`, `CardRecursoDialog` e `QuadroKanban`.

## Alterações
1. **`src/hooks/useCards.ts`**
   - Import interno de `cardComentariosRepo`.
   - `cardsKeys.comentarios(cardId)` (`["card-coments", cardId]`).
   - `cardsQueryFns.listComentariosByCard`, `listComentariosByCardFull`.
   - Helper `invalidateComentarios`. Mutation `useInsertCardComentario`.

2. **`src/components/cards/CardGenericoDialog.tsx`**
   - Remove `cardComentariosRepo` do import de `cardsExtra`.
   - Query usa `cardsKeys.comentarios` + `cardsQueryFns.listComentariosByCard`.
   - `enviarComentario` usa `useInsertCardComentario`.

3. **`src/components/cards/CardRecursoDialog.tsx`**
   - Remove import direto; leitura via `cardsQueryFns.listComentariosByCardFull`; inserts (`adicionarComentario`, `solicitarAtualizacaoCompras`) via `useInsertCardComentario`.

4. **`src/components/cards/QuadroKanban.tsx`**
   - Remove import direto; toast action "Solicitar ao Compras" usa `useInsertCardComentario`.

## Validação
- `tsgo --noEmit`: limpo.
- `rg cardComentariosRepo src`: apenas `useCards.ts` (canônico), o repo em `cardsExtra.ts` e o teste de contrato (`repositories-contrato-f.test.ts`).
- Semântica preservada: mesmos payloads Supabase + `refetch` local + invalidação de cache canônica.

## Próximo passo
`ARC-013.slice-21` — encapsular `cardAtividadesRepo` (listByCard/insert) em hooks/queryFns canônicos e migrar `CardGenericoDialog`.
