# TST-002.j — Contratos de clientes e fornecedores

**Onda:** 6 (bloqueada — NO-GO D-9)  
**Data:** 2026-07-12  
**Escopo:** travar contratos dos cadastros mestres antes das refatorações de compras/financeiro.

## Alvo

- `src/lib/repositories/clientes.ts` — `clientesRepo`.
- `src/lib/repositories/fornecedores.ts` — `fornecedoresRepo`.

## Arquivo criado

- `src/lib/__tests__/integracao/repositories-contrato-j.test.ts` — 18 casos.

## Contratos travados

- `clientes`:
  - `list`: colunas explícitas completas (26 campos, incluindo alíquotas ISS/INSS/CBS/IBS e prazos de pagamento) + `order('nome')`, `[]` em `data null`, propagação de erro.
  - `listIdNome`: projeção `id,nome`.
  - `create`/`update`/`remove`: verbos e `eq('id', ...)`.
  - `countObrasVinculadas`: tabela `obras` com `select('id', { count: 'exact', head: true })` + `eq('cliente_id', ...)`, retorno `0` em count nulo.
- `fornecedores`:
  - `listResumo` (`id, razao_social`) e `listResumoAtivos` (`id, razao_social, ativo`), ambos `order('razao_social')`.
  - `create`/`update`: `insert`/`update().eq('id', ...)` com propagação de erro.

## Resultado

- Lote J: 18/18 ✔
- Suite total: **615/615** ✔
- `tsgo --noEmit`: verde (nenhuma mudança em código de produção)

## Acumulado TST-002

10 lotes · 35 superfícies · **187 casos**.

## Próximo

`TST-002.k` — `rdo` + `inspecoes` (leituras diárias e auditorias antes das refatorações do módulo de campo).
