# SEC-002 · batch-B / migration / apply-01 / materialize-01

- **Ref:** R149
- **Spec:** R148
- **Tipo:** materialização
- **Apply-map:** `v2026-07-12.4` (preservado)
- **Onda:** 2 — OPEN, gate PC1..PC7 verde

## Write-set (2 arquivos)

1. `.github/sec002-evidence/batch-B-apply-01/candidate.sql` — SQL canônico do lote B, 361 linhas, transação única, 13 blocos alfabéticos, 27 `create policy`, 13 `alter table … enable rls`, `notify pgrst 'reload schema'` antes do `commit`. Grants padrão em 12 tabelas; `user_roles` com `grant select` restrito a `authenticated` (§6 da migration.spec).
2. `.github/sec002-evidence/batch-B-apply-01/manifest.json` — manifest com `verifier_gate: on`, `auto_applied: false`, `target_deploy_path` (timestamp diferido), `requires` (helpers-02 aplicada + tests-02 verde + snapshot + janela DRI), SHA-256 do candidate.

## Hashes

- `candidate.sql`: `174e445ae6c57d839c797aca93d21cb07dc706f2b18ee5b1e82c4b4c72b04e0f`
- `INDEX.md` (intocado): `6310f4f16bcbcbbb2a753d44c3eb824d4e7ee1545a4ab17431f23f7d56ac763b`

## Invariantes AB1–AB16 — ✅

Ordem alfabética (AB1), 1 tx (AB2), shape drop→create→enable→revoke→grants (AB3), textos §3/§6 (AB4), sem `user_in_*`/`has_role` fora do padrão (AB5), cabeçalho SEC-002/DRI/refs (AB6), sem DDL/DML de dados (AB7), manifest completo (AB8), zero toque em `supabase/migrations/`/testes/scripts/`src/`/workflows/apply-map/`INDEX.md`/verificador (AB9), `Apply-map: v2026-07-12.4` literal (AB10), sem segredos (AB11), PC1..PC7 verde (AB12), rollback companion fora de escopo (AB13), deploy real fora (AB14), self-register delegado R150/R151 (AB15), bump PC4 delegado R152 (AB16).

## Gate

`verify-wave-2-preconditions.mjs --json` continua `ok: true` — nenhum arquivo do gate tocado.
