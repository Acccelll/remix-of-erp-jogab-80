# ARC-002 · Onda 6 · slice-34 — OportunidadesStateProvider real (CRM)

## Alterações
- `src/contexts/clientes/OportunidadesStateContext.tsx` (novo): roda
  `useOportunidadesState` + queries `useOportunidades` e `useFunilEstagios`,
  sincroniza estados. Expõe `oportunidades`+CRUDs, `moverEstagio`,
  `converterOportunidade`, `funilEstagios` e `crmLoaded`.
- `src/contexts/AppContext.tsx`: substitui `useOportunidadesState()` e o
  `useState<FunilEstagio[]>` por bridge `useOportunidadesStateContext()`.
  Remove `setOportunidades`/`setFunilEstagios` de `useAppQueries`;
  `dataLoaded` inclui `crmLoaded`.
- `src/contexts/app/useAppQueries.ts`: enxugado — remove `useOportunidades`,
  `useFunilEstagios`, tipos `Oportunidade`/`FunilEstagio`, seus setters e
  contribuições em `dataLoaded`.
- `src/App.tsx`: `<OportunidadesStateProvider>` montado acima do
  `<AppProvider>`, dentro de `<ClientesStateProvider>`.

## Validação
- `tsgo --noEmit` sem erros.
- Fachada `AppContext` e `ClientesContext` continuam expondo os mesmos
  símbolos via bridge; consumidores intactos.
