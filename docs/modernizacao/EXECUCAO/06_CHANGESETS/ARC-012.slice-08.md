# ARC-012.slice-08 — Consumidores auxiliares e selamento de `clientesRepo`

**Onda:** 6 · **Achado:** ARC-012 · **Slice:** 08

## Alterações
- **`src/components/obra/EditObraDialog.tsx`**: `useQuery(["clientes-edit-obra"], clientesRepo.list)` → `useClientes({ enabled: open })`.
- **`src/pages/financeiro/FinObras.tsx`**: `useQuery(["clientes"], clientesRepo.list)` → `useClientes()`.
- **`src/pages/financeiro/FinFluxo.tsx`**: remove `clientesRepo.listIdNome()` do `useQuery(["fluxo"])`; passa a consumir `useClientesResumo()` fora do agregado. Render de `<SelectItem>` usa `clientesResumo` no lugar de `data.clientes`.
- **`src/hooks/financeiro/useObrasFinanceiro.ts`**: remove hook local `useClientes` (duplicava leitura) e re-exporta o canônico `useClientes` de `@/hooks/useClientes`. `import clientesRepo` eliminado.
- **`src/lib/repositories/clientes.ts`**: JSDoc `@internal ARC-012` no `clientesRepo` listando os hooks públicos como única fachada permitida (testes de contrato exceptuados). Sem alteração de runtime.

## Comportamento
Sem regressão funcional. Todas as leituras/mutações de clientes passam agora pelas mesmas chaves de cache (`["clientes","list"]`, `["clientes","resumo"]`, `["clientes","obras-vinculadas",<id>]`).

## Validação
- `tsgo --noEmit`: limpo.
- `rg "clientesRepo" src/` fora de `hooks/useClientes.ts`, `lib/repositories/clientes.ts` e `__tests__/`: sem ocorrências.

## Encerramento
Trilha `clientes` de ARC-012 encerrada. Padrão ARC-010/ARC-011/ARC-012-insumos replicado.

## Próximo passo
`ARC-013.slice-01` — mapear próximo repositório de alta reincidência para o mesmo padrão (candidatos: `funcoesRepo`, `patrimoniosRepo`, `frotaRepo` — priorizar por número de consumidores runtime que ainda invocam repos diretamente).
