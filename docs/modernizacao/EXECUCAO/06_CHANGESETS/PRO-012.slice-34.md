# PRO-012 · slice-34 — Reuso de TrendSummary com recorte temporal (28 dias)

**Onda:** 07 · Suprimentos & Contagens Cíclicas
**Contrato:** UI-only.

## Escopo
Validar a extração do slice-32 reaproveitando `TrendSummary` na mesma tela com um segundo recorte de dados (últimas 4 semanas) e configuração distinta (sem toggle, rótulos personalizados).

## Alterações
- `src/pages/suprimentos/ContagensCiclicas.tsx`:
  - Novo `useMemo` `resumoTendencia28d`: filtra `rowsTendencia` por `c.dataPlanejada >= now - 28d` e agrega piora/estável/melhora.
  - Segundo `<TrendSummary>` renderizado logo abaixo do primeiro com `label="Últimas 4 semanas"` e `totalLabel="planejadas"`, sem `onTogglePiora` (informativo).
  - Comportamento do primeiro resumo (com toggle `apenasPiora`) inalterado.

## Validação
- `bunx tsgo --noEmit` sem erros.
- Componente `TrendSummary` provado como reutilizável: mesma API cobre chip clicável (primeiro uso) e chip informativo (segundo uso).

## Próximo passo
`PRO-012.slice-35` — permitir que `TrendSummary` renderize um placeholder discreto quando `total === 0` no modo "informativo" (opt-in via prop `showWhenEmpty`), para manter a moldura visual estável do recorte de 28 dias mesmo sem dados.
