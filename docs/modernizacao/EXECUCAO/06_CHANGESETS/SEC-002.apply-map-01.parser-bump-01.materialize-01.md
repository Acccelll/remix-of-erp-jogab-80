# SEC-002.apply-map-01.parser-bump-01.materialize-01 — Materialização canônica do apply-map `v2026-07-12.3`

**Onda:** 2 (bloqueada — depende de `SEC-001` fechar e da Onda 2 ser formalmente aberta)
**Data:** 2026-07-12
**Depende de:**
- `SEC-002.apply-map-01.materialize-01` (R41) — JSON + loader base;
- `SEC-002.apply-map-01.version-bump-01.materialize-01` (R47) — versão `v2026-07-12.2` (DRI);
- `SEC-002.check-consumers-01.materialize-01` (R48) — guard R1–R7;
- `SEC-002.pr-checklist-workflow-01.materialize-01` (R50) — workflow final dos 11 gates;
- `SEC-002.parse-apply-id-01.materialize-01` (R51) — parser oficial;
- `SEC-002.apply-map-01.parser-bump-01.spec-01` (R52) — contrato canônico deste bump.

**Escopo:** materializar, em conteúdo canônico versionado neste documento, o próximo delta do apply-map (`v2026-07-12.2 → v2026-07-12.3`), do loader e do validador, registrando `scripts/sec002/parse-apply-id.mjs` como consumidor auditado (categoria `parser`) e implementando as invariantes M14–M16 definidas em R52. **Nenhum arquivo de produção é gravado nesta etapa.** Gravação física permanece bloqueada até a abertura formal da Onda 2.

## 1. Racional

R51 introduziu o parser e R50 o invoca como pré-gate único do workflow. R52 fixou o contrato do bump. Falta materializar o conteúdo exato dos três arquivos afetados, para que a gravação futura seja mecânica e verificável por hash.

## 2. Delta canônico de `.github/sec002-apply-map.json`

Aplicar exatamente estes deltas sobre o JSON pós-R47.

### 2.1 Metadados

```diff
-  "schema_version": "v2026-07-12.2",
+  "schema_version": "v2026-07-12.3",
-  "previous_version": "v2026-07-12.1",
+  "previous_version": "v2026-07-12.2",
```

### 2.2 Changelog

Inserir no topo de `changelog[]`:

```json
{
  "id": "bump-2026-07-12.3",
  "summary": "Registra parse-apply-id.mjs como consumidor auditado do apply-map",
  "refs": ["R50", "R51", "R52"]
}
```

Entradas anteriores permanecem intactas.

### 2.3 Categorias

Substituir `categories[]` pela ordem canônica final:

```json
"categories": [
  "checklist",
  "go-no-go",
  "comms",
  "dri",
  "acceptance",
  "evidence",
  "errata",
  "freeze",
  "consumers",
  "parser"
]
```

### 2.4 Consumidores

Anexar no final de `consumers[]`:

```json
{
  "id": "parse-apply-id",
  "path": "scripts/sec002/parse-apply-id.mjs",
  "category": "parser",
  "invariants": ["R1", "R2", "R3", "R4", "R5", "R6", "R7"],
  "invoked_by": [".github/workflows/sec002-pr-checklist.yml#parse-apply-id"],
  "outputs": ["APPLY_ID (stdout)"]
}
```

Ordem final de `consumers[].id`: `validate-pr-checklist`, `validate-changed-files`, `validate-staging-parity`, `validate-go-no-go`, `validate-acceptance-gates`, `validate-evidence-archive`, `validate-dri-matrix`, `validate-errata`, `validate-post-rls-handoff`, `sec002-freeze-guard`, `sec002-pr-checklist`, `parse-apply-id`.

### 2.5 Caminhos protegidos

Anexar em `protected_paths[]`, exatamente uma vez cada:

```json
"scripts/sec002/parse-apply-id.mjs",
".github/sec002-evidence/parse-apply-id-01/**"
```

## 3. Delta canônico do loader

Arquivo: `scripts/sec002/load-apply-map.mjs`.

```diff
-export const SCHEMA_VERSION = "v2026-07-12.2";
+export const SCHEMA_VERSION = "v2026-07-12.3";
```

Adicionar em `VALID_CATEGORIES` (ou equivalente) o valor `"parser"`. Adicionar regra no normalizador:

```js
if (c.category === "parser" && (!Array.isArray(c.outputs) || c.outputs.length === 0)) {
  throw new Error(`E_APPLYMAP_PARSER_OUTPUTS: consumidor ${c.id} category=parser exige outputs[] não vazio`);
}
```

Nenhuma outra chave do loader muda. `getApply()` e `loadApplyMap()` mantêm assinatura.

## 4. Delta canônico do validador

Arquivo: `scripts/sec002/validate-apply-map.mjs`. Adicionar, após M13:

```js
// M14 — parse-apply-id existe exatamente uma vez com path/category corretos
{
  const hits = map.consumers.filter(c => c.id === "parse-apply-id");
  if (hits.length !== 1) fail("E_APPLYMAP_M14", `parse-apply-id deve aparecer 1x em consumers[], encontrado ${hits.length}`);
  else if (hits[0].path !== "scripts/sec002/parse-apply-id.mjs")
    fail("E_APPLYMAP_M14", `parse-apply-id.path inválido: ${hits[0].path}`);
  else if (hits[0].category !== "parser")
    fail("E_APPLYMAP_M14", `parse-apply-id.category inválido: ${hits[0].category}`);
}

// M15 — categories[] contém "parser" exatamente 1x na ordem canônica
{
  const expected = ["checklist","go-no-go","comms","dri","acceptance","evidence","errata","freeze","consumers","parser"];
  if (JSON.stringify(map.categories) !== JSON.stringify(expected))
    fail("E_APPLYMAP_M15", `categories[] fora da ordem canônica`);
}

// M16 — protected_paths[] cobre parser e evidência, 1x cada
{
  const need = ["scripts/sec002/parse-apply-id.mjs", ".github/sec002-evidence/parse-apply-id-01/**"];
  for (const p of need) {
    const n = map.protected_paths.filter(x => x === p).length;
    if (n !== 1) fail("E_APPLYMAP_M16", `protected_paths[] deve conter ${p} exatamente 1x (encontrado ${n})`);
  }
}
```

Contrato de saída do validador permanece: `[sec002/validate-apply-map] OK` em sucesso, `[sec002/validate-apply-map] FAIL` com códigos `E_APPLYMAP_M<n>` em falha.

## 5. Compatibilidade com consumidores

| Consumidor | Efeito do bump |
|---|---|
| `parse-apply-id.mjs` | passa a ser listado em `consumers[]`; `sec002-consumers-guard` valida R1–R7 contra ele |
| `sec002-freeze-guard` | passa a proteger `scripts/sec002/parse-apply-id.mjs` e `.github/sec002-evidence/parse-apply-id-01/**` |
| demais consumidores | nenhuma alteração — ordem/contagem preservadas |
| `sec002-pr-checklist.yml` | **não é tocado** neste bump (invariante do §9 de R52) |

## 6. Invariantes preservadas

- M1–M13 permanecem inalteradas.
- `totals.applies`, `totals.sum_expected_asserts` e `totals.final_rls_suite_asserts` inalterados.
- Nenhum `apply_id`, `allowed_paths[]` ou `required_evidence_categories[]` de qualquer apply é modificado.

## 7. Procedimento de gravação futura

1. Editar `.github/sec002-apply-map.json` conforme §2.
2. Editar `scripts/sec002/load-apply-map.mjs` conforme §3.
3. Editar `scripts/sec002/validate-apply-map.mjs` conforme §4.
4. Executar localmente:

```bash
node scripts/sec002/validate-apply-map.mjs
node scripts/sec002/check-consumers.mjs
```

Saída esperada:

```text
[sec002/validate-apply-map] OK
[sec002/consumers-guard] OK
```

## 8. Evidência obrigatória

Registrar `.github/sec002-evidence/apply-map-parser-bump-01/manifest.json`:

```json
{
  "sec002_apply_id": "apply-map-parser-bump-01",
  "version_before": "v2026-07-12.2",
  "version_after": "v2026-07-12.3",
  "source": "docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.apply-map-01.parser-bump-01.materialize-01.md",
  "changed_files": [
    ".github/sec002-apply-map.json",
    "scripts/sec002/load-apply-map.mjs",
    "scripts/sec002/validate-apply-map.mjs"
  ],
  "checks": [
    "node scripts/sec002/validate-apply-map.mjs",
    "node scripts/sec002/check-consumers.mjs"
  ],
  "hashes": {
    ".github/sec002-apply-map.json": "<sha256>",
    "scripts/sec002/load-apply-map.mjs": "<sha256>",
    "scripts/sec002/validate-apply-map.mjs": "<sha256>"
  },
  "refs": ["R40", "R41", "R46", "R47", "R50", "R51", "R52"]
}
```

## 9. Critérios de rejeição futura

- `schema_version` do JSON e `SCHEMA_VERSION` do loader divergirem;
- `parse-apply-id` ausente, duplicado ou com `category` diferente de `"parser"`;
- `categories[]` fora da ordem canônica do §2.3;
- qualquer caminho de §2.5 ausente ou duplicado;
- validador sem M14, M15 ou M16 (ou sem os códigos `E_APPLYMAP_M14/15/16`);
- workflow `sec002-pr-checklist.yml` alterado neste bump;
- qualquer alteração em `src/**`, `supabase/functions/**` ou `supabase/migrations/**`.

## 10. Impacto em produção

**Nenhum.** Documento de materialização canônica. Não grava arquivos executáveis nesta etapa, não altera dados, não aplica migração, não muda função, não muda frontend e não altera política RLS.
