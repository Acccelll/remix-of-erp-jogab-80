# ARC-013 · slice-33 — migrar `CronogramaPrincipalTab` para `cronogramaMutationFns`

## Alterações
1. **`src/hooks/useCronograma.ts`**: adiciona `cronogramaMutationFns` (`create`/`update`/`remove`) — wrappers imperativos para fluxos `async` custom que ainda não usam `useMutation`.
2. **`src/components/obra/CronogramaPrincipalTab.tsx`**: import de `cronogramaRepo` removido (mantido só `cronogramaItemRevisoesRepo`, escopo de slice futura); `create`/`remove`/`update` migrados para `cronogramaMutationFns`.

## Validação
- `bunx tsgo --noEmit`: limpo.
- `rg cronogramaRepo src/components/obra/CronogramaPrincipalTab.tsx`: sem ocorrências.
