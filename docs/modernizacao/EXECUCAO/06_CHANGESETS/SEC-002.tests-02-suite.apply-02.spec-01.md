# SEC-002 · tests-02-suite / apply-02 / spec-01

- **Ref:** R143
- **Tipo:** especificação
- **Onda:** 2 — OPEN, gate PC1..PC7 verde (R142)
- **Apply-map:** `v2026-07-12.4` (a preservar)
- **Antecessores:** R131/R132 (apply-01 do esqueleto), R138/R140/R142 (self-register + bump PC4)
- **DRI:** Cappucceno (GM) / Lovable

## 1. Objetivo

Especificar o **segundo apply autorizado** do write set de `tests-02-suite` — os batches funcionais **B, C, D, E** dos testes SQL de RLS/policies para SEC-002, sobre o esqueleto já materializado em R132 (`supabase/tests/sec-002/{_lib,helpers}/`). Nenhum SQL é executado nesta fase (contrato R131 preservado); a execução real depende de staging + `SEC002_ALLOW_STAGING=1` + janela DRI.

## 2. Write set físico (autorizado)

Exatamente **6 arquivos** — nenhum outro toque permitido:

1. `supabase/tests/sec-002/batch-B/boards_rls.sql` — asserts de SELECT/INSERT/UPDATE/DELETE em `boards` sob `has_role`/`user_in_board`.
2. `supabase/tests/sec-002/batch-C/cards_rls.sql` — asserts em `cards`/`card_members` via `user_in_card`.
3. `supabase/tests/sec-002/batch-D/obras_rls.sql` — asserts em `obras`/`obra_members` via `user_in_obra`.
4. `supabase/tests/sec-002/batch-E/empresas_rls.sql` — asserts em `empresas`/`empresa_members` via `user_in_empresa`.
5. `supabase/tests/sec-002/batch-E/cross_tenant_deny.sql` — negativos cross-tenant (deve falhar).
6. `.github/sec002-evidence/tests-02-suite-apply-02/manifest.json` — manifest do apply.

## 3. Invariantes (AT2-1 … AT2-14)

- **AT2-1** — cada arquivo `batch-*/*.sql` é idempotente e usa apenas helpers de `_lib/` + `helpers/`.
- **AT2-2** — nenhum `DROP`, `ALTER TABLE`, `CREATE POLICY` ou DDL de produção; apenas `SELECT`, `INSERT`, `SET LOCAL role`, `RAISE`.
- **AT2-3** — cada batch envolve o corpo em `begin;` … `rollback;` (não deixa estado).
- **AT2-4** — mínimo **12 asserts positivos + 8 asserts negativos** distribuídos nos 5 SQLs (total ≥ 20).
- **AT2-5** — `jwt.sql` reaproveitado; nenhum novo helper criado fora do write set.
- **AT2-6** — runner `scripts/db/run-sec-002-tests.sh` NÃO é alterado (já descobre `batch-*/`).
- **AT2-7** — nenhum toque em `src/`, edge functions, migrations, workflows, apply-map, `INDEX.md`.
- **AT2-8** — manifest inclui `schema_version: v2026-07-12.4`, `apply_id: tests-02-suite-apply-02`, `ref: R144`, `spec_ref: R143`, `auto_applied: false`, `verifier_gate: on`, hashes SHA-256 dos 5 SQLs.
- **AT2-9** — `SEC002_ALLOW_STAGING` continua obrigatório para execução; ausência = no-op.
- **AT2-10** — gate PC1..PC7 permanece verde antes e depois (nenhum bump de `EXPECTED_INDEX_ROWS` neste passo — self-register e bump 32→33 delegados a R145/R146/R147).
- **AT2-11** — cross-tenant deny usa dois JWTs distintos via `jwt.sign_as(user_id)`.
- **AT2-12** — comentário de cabeçalho em cada SQL cita `SEC-002`, batch, DRI, ref.
- **AT2-13** — nenhuma referência a segredos, `service_role_key` ou URLs de banco.
- **AT2-14** — apply-map `v2026-07-12.4` preservado literalmente.

## 4. Fora de escopo (delegado)

- **R144** — materialização (6 arquivos acima).
- **R145** — spec do self-register (`tests-02-suite-apply-02` → linha 33 do `INDEX.md`).
- **R146** — materialização do self-register.
- **R147** — spec + materialização do bump PC4 `EXPECTED_INDEX_ROWS: 32→33`.
- Execução real da suíte contra staging — janela DRI GM, fora deste changeset.

## 5. Rejeição automática

Qualquer PR que:
- toque `INDEX.md`, apply-map, `verify-wave-2-preconditions.mjs`, runner, `_lib/`, `helpers/`, migrations, workflows, `src/` — **REJECT**.
- omita qualquer dos 6 arquivos ou adicione um 7º — **REJECT**.
- deixe `verifier_gate` ≠ `on` ou `auto_applied` ≠ `false` — **REJECT**.
