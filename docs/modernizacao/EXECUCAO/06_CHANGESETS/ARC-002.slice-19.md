# ARC-002 · slice-19

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Escopo
Consolidação CRM em `ClientesContext`:

- `src/contexts/ClientesContext.tsx` — expõe `moverEstagio` e `converterOportunidade` além de `oportunidades`/`funilEstagios`/mutações já existentes.
- `src/pages/crm/CRMFunil.tsx` — `oportunidades`, `funilEstagios`, `moverEstagio`, `clientes` via `useClientesContext()`; `useApp()` mantém `hasAccess`/`currentPlayer`.
- `src/pages/crm/Oportunidades.tsx` — CRUD + `converterOportunidade` via `useClientesContext()`; `useApp` removido.
- `src/pages/crm/OportunidadePerfil.tsx` — `converterOportunidade` via `useClientesContext()`; `useApp` removido.
- `src/components/crm/OportunidadeFormDialog.tsx` — funil/mutações via `useClientesContext()`; `useApp` removido.

## Validação
- `tsgo --noEmit`: sem erros.
