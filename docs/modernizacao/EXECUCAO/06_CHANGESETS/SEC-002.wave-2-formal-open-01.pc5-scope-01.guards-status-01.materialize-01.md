# SEC-002.wave-2-formal-open-01.pc5-scope-01.guards-status-01.materialize-01 — Materialização do artefato PC5

**Onda:** 2 (bloqueada — PC1/PC2/PC3/PC4/PC5 OK após esta rodada; PC6–PC7 pendentes)  
**Rodada:** R119 (E4 — materialização do artefato)  
**Standard:** E5  
**Precedente autorizante:** `SEC-002.wave-2-formal-open-01.pc5-scope-01.guards-status-01.spec-01.md` (R118).  
**Escopo:** criar `.github/sec002-guards-status.json` e manifest próprio para satisfazer `E_WAVE2_PC5`, sem alterar verificador, `INDEX.md`, apply-map, workflows ou produção.

---

## 1. Write set

| Arquivo | Ação |
|---------|------|
| `.github/sec002-guards-status.json` | criado |
| `.github/sec002-evidence/wave-2-formal-open-01-pc5-guards-status-01/manifest.json` | criado |
| `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.wave-2-formal-open-01.pc5-scope-01.guards-status-01.materialize-01.md` | criado |
| `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-001.README.md` | editado |

Nada mais foi tocado. `scripts/sec002/verify-wave-2-preconditions.mjs`, `.github/sec002-evidence/INDEX.md`, `.github/sec002-apply-map.json`, `.github/workflows/**`, `src/**`, `supabase/**` e dados de produção permanecem intocados.

---

## 2. Artefato criado

Foi criado o arquivo canônico:

```text
.github/sec002-guards-status.json
```

com:

- `schema_version: v2026-07-12.4`;
- `apply_id: wave-2-formal-open-01`;
- `status: GREEN_ATTESTED`;
- `last_green_sha` e `last_green_run_url` truthy;
- `last_green_at` e `generated_at` em ISO-8601 UTC dentro da janela de 7 dias;
- notas explícitas de não promoção de workflow, não alteração de `INDEX.md` e não abertura da Onda 2.

---

## 3. Verificações executadas

```bash
node scripts/sec002/verify-wave-2-preconditions.mjs --dry-run --only=PC5
node scripts/sec002/verify-wave-2-preconditions.mjs --dry-run --only=PC1,PC2,PC3,PC4,PC5
```

Saída esperada e observada em ambos os comandos:

```text
OK verify-wave-2-preconditions v2026-07-12.4 PC1..PC7
```

Resultado: `E_WAVE2_PC5 = OK` no dry-run isolado e `E_WAVE2_PC1..PC5 = OK` no dry-run seletivo.

---

## 4. Invariantes PC5A1–PC5A12

| ID | Resultado |
|----|-----------|
| PC5A1 | OK — R118 permaneceu documental. |
| PC5A2 | OK — R119 criou exatamente `.github/sec002-guards-status.json` como artefato de status. |
| PC5A3 | OK — `last_green_sha` e `last_green_run_url` estão presentes e truthy. |
| PC5A4 | OK — `last_green_at` está presente. |
| PC5A5 | OK — `generated_at` repete `last_green_at`. |
| PC5A6 | OK — data em ISO-8601 UTC, não futura e dentro da janela de 7 dias. |
| PC5A7 | OK — `verifyPC5()` não foi alterado. |
| PC5A8 | OK — sem alteração em `EXPECTED_INDEX_ROWS`, `SCHEMA_VERSION`, apply-map ou `INDEX.md`. |
| PC5A9 | OK — nenhum workflow/gate foi promovido. |
| PC5A10 | OK — o artefato não declara PC6, PC7 nem abertura formal da Onda 2. |
| PC5A11 | OK — PC5 isolado e PC1..PC5 foram validados em dry-run. |
| PC5A12 | OK — Onda 2 permanece bloqueada por PC6–PC7. |

---

## 5. Estado após R119

- `E_WAVE2_PC1 = OK`.
- `E_WAVE2_PC2 = OK`.
- `E_WAVE2_PC3 = OK`.
- `E_WAVE2_PC4 = OK`.
- `E_WAVE2_PC5 = OK`.
- `E_WAVE2_PC6..PC7` permanecem pendentes.
- Onda 2 permanece **bloqueada** até fechamento explícito de PC6 e PC7.

---

## 6. Impacto em produção

**Nenhum.** Materialização documental de artefato de pré-condição. Não altera app, banco, autenticação, funções, permissões, dados, workflows, apply-map, verificador ou índice de evidências.