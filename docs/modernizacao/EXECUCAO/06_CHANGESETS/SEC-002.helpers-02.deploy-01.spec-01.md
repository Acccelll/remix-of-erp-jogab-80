# SEC-002.helpers-02.deploy-01.spec-01 — Promoção do candidate.sql para supabase/migrations/

**Onda:** 2 (OPEN)
**Ref:** R193 · **Round:** compact-01
**Data:** 2026-07-12
**Escopo:** especifica a **promoção física** de `.github/sec002-evidence/helpers-02-apply-01/candidate.sql` (115 linhas, AP1..AP12 verificados em R131/R132) para `supabase/migrations/<ts>_sec_002_helpers_02.sql`, encerrando o gap detectado hoje (`ls supabase/migrations/ | grep helpers` = vazio). Sem essa promoção, `helpers-02` permanece **inaplicado no banco**, bloqueando: policies dos lotes B/C/D/E, helpers-03 (R184), execução real do runner PC9 (R189). **Nenhum byte é escrito neste changeset**; apenas contrato de deploy, timestamp, invariantes DEP01-1…DEP01-10 e ordem de operação. Materialização fica para `SEC-002.helpers-02.deploy-01.materialize-01` (R194) sob GO explícito do GM.

## 1. Estado atual (baseline)

- `.github/sec002-evidence/helpers-02-apply-01/candidate.sql` → **115 linhas**, sha256 registrável em R194.
- `.github/sec002-evidence/helpers-02-apply-01/manifest.json` → `auto_applied:false`, `target_deploy_path: "supabase/migrations/<yyyymmddhhmmss>_sec_002_helpers_02.sql"`, `timestamp_assignment: at_deploy_pr_open` (assinado em R131/R132).
- `supabase/migrations/*sec_002_helpers_02*.sql` → **ausente**.
- Nenhuma policy dos lotes B/C/D/E aplicada em produção (pré-condição §2.4 do spec fonte).

## 2. Contrato de deploy

### 2.1 Operação atômica

Uma única cópia byte-a-byte do `candidate.sql` para `supabase/migrations/<ts>_sec_002_helpers_02.sql`, com timestamp UTC do momento do materialize R194 no formato `yyyymmddhhmmss`.

```bash
cp .github/sec002-evidence/helpers-02-apply-01/candidate.sql \
   supabase/migrations/$(date -u +%Y%m%d%H%M%S)_sec_002_helpers_02.sql
```

### 2.2 Nenhuma reescrita

Zero edição de conteúdo. Diff entre `candidate.sql` e o arquivo destino em `supabase/migrations/` deve ser exatamente `0` (comparação bruta `cmp -s`).

### 2.3 Atualização de manifest

Após a cópia, `manifest.json` recebe:
- `auto_applied: false` → **inalterado** (Supabase é quem aplica).
- `deployed_path: supabase/migrations/<ts>_sec_002_helpers_02.sql` (novo campo).
- `deployed_at: <ISO 8601 UTC>` (novo campo).
- `deployed_sha256: <sha256 do candidate>` (novo campo, igual ao `candidate_sha256` já registrável).

## 3. Invariantes DEP01-1…DEP01-10

| ID | Verificação |
|----|-------------|
| DEP01-1 | `cmp -s candidate.sql supabase/migrations/<ts>_sec_002_helpers_02.sql` = exit 0 |
| DEP01-2 | Nome do arquivo destino casa `^\d{14}_sec_002_helpers_02\.sql$` |
| DEP01-3 | Timestamp UTC monotônico (> qualquer migration anterior em `supabase/migrations/`) |
| DEP01-4 | Nenhum outro arquivo `.sql` em `supabase/migrations/` alterado ou criado |
| DEP01-5 | `candidate.sql` **não** é apagado, movido ou modificado — origem preservada |
| DEP01-6 | Nenhum edit em `_lib/`, `batch-*/`, `helpers/`, `scripts/sec002/*`, `src/`, `supabase/functions/`, workflows, `INDEX.md`, apply-map |
| DEP01-7 | Manifest recebe apenas campos aditivos (`deployed_path`, `deployed_at`, `deployed_sha256`); campos preexistentes preservados byte-a-byte |
| DEP01-8 | `verify-wave-2-preconditions.mjs --json` = `{ ok:true }` PC1..PC7 antes e depois |
| DEP01-9 | Nenhum apply direto ao banco por este changeset — deploy Supabase-nativo é etapa separada (revisão do usuário via ferramenta `supabase--migration` **não** é usada aqui; migration entra no repositório e o pipeline Cloud aplica) |
| DEP01-10 | Se a Supabase Cloud recusar a migration (sintaxe, permissão), R194 emite rollback: `rm supabase/migrations/<ts>_sec_002_helpers_02.sql` e reverte manifest |

## 4. Dependências duras para materialização (R194)

- `candidate.sql` presente e íntegro (`sha256` bate com o registrado em R131/R132).
- Nenhuma migration `sec_002_helpers_02` já presente em `supabase/migrations/` (evita duplicata).
- GM assinou GO explícito para promoção (registrado no PR body).
- PC1..PC7 verdes no HEAD imediatamente anterior.

## 5. Cadeia desbloqueada por R194

1. **helpers-02 aplicado no banco** → base para todas as policies de lote.
2. **helpers-03 deploy** (R195+) desbloqueia após D-3 assinada.
3. **Policies do lote B** (13 tabelas × 3 casos) podem ser materializadas.
4. **PC9** (R189) pode rodar `run-sec-002-tests.sh` contra banco real.

## 6. Não tocado (deste changeset)

`candidate.sql`, `manifest.json` atual, `supabase/migrations/*`, `_lib/`, `batch-*/`, `helpers/`, `scripts/sec002/*`, verifier, `INDEX.md`, apply-map, `src/`, `supabase/functions/`, workflows, banco.

## 7. Impacto em produção

**Nenhum neste changeset** (apenas especificação). Após R194, `helpers-02` entra em produção via Supabase Cloud com 4 funções `security definer stable` + 5 índices `if not exists` + grants restritos a `authenticated, service_role` — **sem policies novas** e sem alteração de dados. PC1..PC7 verdes; `INDEX.md`=44 linhas; `EXPECTED_INDEX_ROWS=44`.
