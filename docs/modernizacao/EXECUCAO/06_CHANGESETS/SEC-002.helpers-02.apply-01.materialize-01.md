# SEC-002.helpers-02.apply-01.materialize-01 — Materialização do primeiro apply do write set

**Onda:** 2 (OPEN — gate PC1..PC7 = OK)
**Data:** 2026-07-12T18:05:00Z
**Spec de referência:** `SEC-002.helpers-02.apply-01.spec-01.md`
**Contrato do arquivo SQL:** `SEC-002.helpers-02.spec-01.md` §§3–6

## 1. Ação executada

Gravados **exatamente 2 arquivos** físicos:

1. `.github/sec002-evidence/helpers-02-apply-01/candidate.sql` — migração
   idempotente completa (4 funções `security definer stable`, 4 pares
   `revoke/grant`, 5 índices `if not exists`, 4 `comment on function`), em
   um único `begin;` … `commit;`. Diff físico = 118 linhas (≤ 200, AP10 OK).
2. `.github/sec002-evidence/helpers-02-apply-01/manifest.json` — manifesto do
   apply com `schema_version: v2026-07-12.4`, `apply_id`, `verifier_gate: on`,
   `expected_index_rows_at_apply: 30`, refs `R58,R95,R127,R130,R131,R132`,
   DRI `Cappucceno`/`Lovable`, `auto_applied: false`,
   `target_deploy_path: supabase/migrations/<ts>_sec_002_helpers_02.sql`.

## 2. Nota de path (AP1/AP2)

O contrato-alvo do arquivo é
`supabase/migrations/<yyyymmddhhmmss>_sec_002_helpers_02.sql`, mas o timestamp
é atribuído somente na abertura do PR de deploy (§1 do `helpers-02.spec-01`).
Nesta materialização o arquivo vive em `candidate.sql` sob o diretório de
evidência do apply, preservando bit-a-bit o conteúdo que será renomeado no
deploy. O manifest declara `target_deploy_path` e `timestamp_assignment:
at_deploy_pr_open` para trilha auditável. Nenhum byte é escrito em
`supabase/migrations/` nesta materialização — garantia dura de `auto_applied:
false` e AP12 (o INDEX/readiness não são invalidados).

## 3. Verificação das invariantes AP1–AP12

| Invariante | Evidência |
|-----------:|-----------|
| AP1  | Um único arquivo `.sql` no write set |
| AP2  | Nome canônico do target declarado no manifest; renomeio no deploy PR |
| AP3  | `begin;` na linha 8, `commit;` na linha 118, sem `savepoint` |
| AP4  | Ordem `user_in_empresa → user_in_obra → user_in_board → user_in_card` |
| AP5  | Assinaturas/`search_path`/`security definer`/`stable`/`language sql` idênticas a `SEC-002.helpers-01` §4 |
| AP6  | 4 pares `revoke all … from public, anon;` + `grant execute … to authenticated, service_role;`; zero grants a `anon`/`public` |
| AP7  | 5 `create index if not exists`; zero `unique`, zero `concurrently` |
| AP8  | 4 `comment on function` apontando §4.1–§4.4 |
| AP9  | 0 `drop function`, 0 `alter function`, 0 `create policy`, 0 DML |
| AP10 | 118 linhas físicas |
| AP11 | Manifest satisfaz schema declarado no spec §3 |
| AP12 | `INDEX.md` intocado; `EXPECTED_INDEX_ROWS = 30` permanece válido |

Pré-execução do verificador (dry-run):
`node scripts/sec002/verify-wave-2-preconditions.mjs --json` →
`PC1..PC7 = OK` (mesmo SHA da pós-materialização de R130).

## 4. Fora do escopo (não tocado)

- `src/`, edge functions, `scripts/sec002/*`, workflows, `apply-map.json`
  (versão preservada em `v2026-07-12.4`).
- `.github/sec002-evidence/INDEX.md` — o self-register do apply fica delegado
  a **R133 (spec)** e **R134 (materialize)**, análogos a R128/R129.
- `scripts/sec002/verify-wave-2-preconditions.mjs` — o bump
  `EXPECTED_INDEX_ROWS: 30 → 31` fica delegado a **R135**, análogo a R130.
- Banco de dados — nada é executado. Deploy real depende de suíte
  `SEC-002.tests-02.suite/helpers` verde e janela DRI GM.

## 5. Rollback do apply

Basta remover `candidate.sql` e `manifest.json` sob
`.github/sec002-evidence/helpers-02-apply-01/`. Nenhum efeito colateral em
INDEX, readiness, apply-map, scripts ou banco.

## 6. Estado após R132

- Onda 2: **OPEN**, gate operacional PC1..PC7 = OK.
- Write set: **1º apply materializado** (helpers-02), pendente de
  self-register e bump de verificador.
- Impacto em produção: **nenhum**.
