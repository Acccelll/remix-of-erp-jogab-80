# PRO-018 · slice-04 — UI de perfis de layout no importador BMS

**Onda:** 07 · **Achado:** PRO-018 · **Slice:** 04/n

## Entrega
- `src/components/import/BmsImporter.tsx`:
  - Lista os perfis salvos (`useBmsLayoutPerfis`) no card "BMS
    encontrados", com nome, cliente, `headerRow` e nº de colunas.
    Ação **Remover** por linha.
  - Botão **Salvar como perfil** no bloco de diagnóstico da
    pré-visualização, usando `perfilAPartirDeDiagnostico` para capturar
    o `ColMap` reconhecido pelo parser. Sugere nome padrão a partir do
    `cliente_unidade` + `numero` da aba.

## Regras respeitadas
- Sem alterar parser, sem alterar contrato do hook.
- Persistência 100% local (`gestaobra:bms:layoutPerfis`).
- Sem dependência de UI nova (usa `Button` + `window.prompt`, alinhado
  com o padrão minimalista do importador atual).

## Não escopo (próximo slice)
- Reaplicar perfil salvo nas abas ignoradas para tentar reconstruir o
  parse (slice-05).
- Editor visual drag-and-drop (slice-06).
- Sincronização remota dos perfis.

## Validação
- `bunx tsgo --noEmit` — OK, sem novos erros.
- Testes de `layoutPerfis` (slice-03) e `coverage` (slice-02) seguem
  verdes.
