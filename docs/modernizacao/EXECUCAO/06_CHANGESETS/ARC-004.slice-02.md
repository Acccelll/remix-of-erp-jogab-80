# ARC-004.slice-02 — Clientes migrados para paradigma único (TanStack Query mutations)

**Onda:** 6 · **Achado:** ARC-004

## Escopo
Replica o padrão da slice-01 para `clientes`.

## Mudanças
- **`src/hooks/useClientes.ts`**: adiciona `useAddCliente`, `useUpdateCliente`, `useDeleteCliente` (padrão canônico `onMutate/onError/onSettled`).
- **`src/contexts/clientes/ClientesStateContext.tsx`**: reescrito — consome `useClientes().data`; CRUDs delegam para mutations.
- **`src/contexts/app/useClientesState.ts`**: removido.

## Validação
- `tsgo --noEmit` limpo.
- Assinatura pública preservada.

## Próximo
`ARC-004.slice-03` — mesmo padrão para `colaboradores`.
