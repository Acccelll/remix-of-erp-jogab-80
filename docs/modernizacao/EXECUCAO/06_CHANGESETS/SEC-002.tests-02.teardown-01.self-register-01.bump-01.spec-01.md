# SEC-002 · tests-02 / teardown-01 / self-register-01 / bump-01 / spec-01

- **Ref:** R199' (spec + materialize — janela unificada, análoga a R147)
- **Tipo:** especificação + materialização (bump trivial de 1 dígito)
- **Onda:** 2 — OPEN
- **Apply-map:** `v2026-07-12.4` (preservado)
- **Antecessores:** R195' (spec teardown), R196' (materialize teardown), R197' (spec self-register), R198' (materialize self-register linha 45)

## 1. Objetivo

Bump `EXPECTED_INDEX_ROWS: 44 → 45` em `scripts/sec002/verify-wave-2-preconditions.mjs`, reativando PC1..PC7 como gate verde após R198' ter appended a linha 45 (`tests-02-teardown-01`) em `.github/sec002-evidence/INDEX.md`.

## 2. Write-set (2 arquivos)

1. `scripts/sec002/verify-wave-2-preconditions.mjs` — **linha 10**: `const EXPECTED_INDEX_ROWS = 45;`
2. `.github/sec002-evidence/tests-02-teardown-01-self-register-01-bump-01/manifest.json` — `schema_version: v2026-07-12.4`, `expected_index_rows_before: 44`, `after: 45`, `auto_applied: false`, `verifier_gate: on`, refs `R132, R183, R192b, R195', R196', R197', R198'`.

## 3. Invariantes (TDR01B-1 … TDR01B-8)

- **TDR01B-1** — bump exato 44→45 (única alteração numérica no verificador). ✅
- **TDR01B-2** — `verifyPC4()` inalterado estruturalmente; só o literal muda. ✅
- **TDR01B-3** — `verifyPC1/PC2/PC3/PC5/PC6/PC7` preservados byte-a-byte. ✅
- **TDR01B-4** — `INDEX.md` intocado (permanece com 45 rows e `Apply-map: v2026-07-12.4`). ✅
- **TDR01B-5** — suíte `tests-02` (esqueleto R132 + batches R144 + teardown R196') e runner preservados. ✅
- **TDR01B-6** — nenhum arquivo de produção tocado (`src/`, edge functions, migrations, batches, `_lib/`, workflows, apply-map, banco). ✅
- **TDR01B-7** — `node scripts/sec002/verify-wave-2-preconditions.mjs --json` retorna `ok: true` com PC1..PC7 green. ✅
- **TDR01B-8** — sem rollback do fix; helpers-02 (deploy) e helpers-03 permanecem bloqueados por GO GM e D-3 respectivamente. ✅

## 4. Verificação pós-materialize

```bash
node scripts/sec002/verify-wave-2-preconditions.mjs --json
# → { "ok": true, "apply_id": "wave-2-formal-open-01", "schema_version": "v2026-07-12.4" }
```

## 5. Delegações

- Nenhuma. Fecha o ciclo `tests-02-teardown-01` (spec R195' → materialize R196' → self-register spec R197' → self-register materialize R198' → bump R199').
- Próximos ciclos independentes: helpers-02 deploy (bloqueado por GO GM), helpers-03 apply (bloqueado por D-3 + helpers-02 deployado).
