# SEC-002 · Wave 2 Readiness 02 · Authorization Decision Queue 01 (R229')

**Tipo:** especificação (governança pura)
**Round:** R229'
**Ciclo:** documental — nenhum código de produção, migração, `src/`, `supabase/functions/`, `supabase/migrations/`, `_lib/`, verifier, workflow, apply-map, banco ou segredo é tocado.
**Consome:** R213', R214', R216', R217', R218', R219', R220', R221', R222', R223', R224', R225', R226', R227', R228'.

## 1. Objetivo

Definir a fila canônica de decisão para intakes GM/D-3 revisados por R228', sem criar ledger, sem assinar, sem preencher, sem deployar e sem aplicar. A fila é apenas regra documental de ordenação e desempate para futuro processamento formal.

## 2. Estados da fila

Cada item candidato pode existir apenas em um dos estados abaixo:

1. `NÃO ENFILEIRADO`: mensagem genérica, incompleta ou classificada como `SEM AUTORIZAÇÃO`.
2. `REJEITADO`: intake formal com motivo canônico de R219'/R220'/R226'/R227'/R228'.
3. `PENDENTE GM`: intake GM completo, candidato a abrir round futuro R213'.
4. `PENDENTE D-3`: intake D-3 completo, candidato a abrir round futuro R214'.
5. `ADIADO POR DEPENDÊNCIA`: intake D-3a válido, mas aguardando deploy efetivo de `helpers-02`.

Nenhum estado acima materializa autorização por si só.

## 3. Ordem canônica de processamento futuro

Quando houver mais de um candidato aceito documentalmente, a ordem futura deve ser:

1. Rejeições formais são encerradas primeiro, sem efeito material.
2. `PENDENTE GM` tem precedência sobre `PENDENTE D-3`, preservando R222'.
3. `PENDENTE D-3` só pode avançar para R214' se não depender de deploy ainda inexistente de `helpers-02`.
4. Empate entre dois intakes da mesma origem é resolvido por timestamp UTC ISO-8601 mais antigo, desde que ambos ainda sejam válidos por R226'.
5. Se a validade temporal expirar ou o baseline mudar antes do processamento, o item volta para `REJEITADO` e exige reenvio completo conforme R220'.

## 4. Escopo negativo

Este round **não**:

- cria `authorization-ledger.md`;
- cria `GO-SEC-002-helpers-02-deploy-01.md`;
- preenche §5 de `D-3-SEC-002.md`;
- assina qualquer decisão GM/D-3;
- promove `helpers-02` para `supabase/migrations/`;
- aplica `helpers-03`;
- altera verifier, `INDEX.md`, `EXPECTED_INDEX_ROWS`, apply-map, `_lib/`, batches, `src/`, workflows, banco;
- solicita, aceita, expõe, loga ou fabrica segredo algum.

## 5. Invariantes (AUTHQ-1…AUTHQ-9)

- **AUTHQ-1** Ciclo puramente documental; nenhum arquivo executável tocado.
- **AUTHQ-2** R213', R214', R216', R217', R218', R219', R220', R221', R222', R223', R224', R225', R226', R227' e R228' preservados byte-a-byte.
- **AUTHQ-3** `authorization-ledger.md` continua **não criado**.
- **AUTHQ-4** `GO-SEC-002-helpers-02-deploy-01.md` continua **não criado**.
- **AUTHQ-5** `D-3-SEC-002.md §5` continua **vazio**.
- **AUTHQ-6** `SEC-002.inventory-02.csv` intacto (`sha256[:12] = 8c18a694442e…d66a404ef`).
- **AUTHQ-7** `.github/sec002-evidence/INDEX.md` = 45 linhas; `EXPECTED_INDEX_ROWS = 45`; `Apply-map: v2026-07-12.4`.
- **AUTHQ-8** PC1..PC7 verdes; PC8/PC9 SKIP.
- **AUTHQ-9** Nenhum segredo/chave/senha/token solicitado, aceito, exposto, logado ou fabricado.

## 6. Write set

- **Criado:** este arquivo.
- **Editado:** `SEC-001.README.md` — 1 linha appendada (linha 225).
- Nenhum outro arquivo é tocado.

## 7. Impacto em produção

**Nenhum.**