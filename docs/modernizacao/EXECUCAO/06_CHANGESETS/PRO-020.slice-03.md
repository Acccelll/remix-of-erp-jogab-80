# PRO-020 · slice-03 — Horizonte configurável e export XLSX do cronograma

**Onda:** 08 · **Achado:** PRO-020 · **Depende de:** slice-02.

## Contexto
Cronograma obra × mês precisa de flexibilidade de janela e uma saída para consumo externo (planejamento, revisão financeira).

## Entrega
- `src/pages/Contratos.tsx`
  - Estado `horizonteMeses` (3/6/12/24) alimentando `projetarPorObraMes`.
  - Barra do `<details>` com `Select` de horizonte e botão **Exportar XLSX** — gera `cronograma_contratos_<H>m_<YYYY-MM-DD>.xlsx` (cabeçalho `Obra | meses… | Total`, linhas por obra e linha final de totais) usando `xlsx` já presente no projeto.
  - Título do summary reflete o horizonte selecionado.

## Não escopo
- Sem escrita em Financeiro (D-8).
- Sem export por contrato individual — apenas agregado por obra.
- Sem persistência da preferência de horizonte.

## Validação
- `bunx tsgo --noEmit` — OK.
