# ARC-013.slice-15 — Encapsular `cardsExtraRepo` em `useCards`

## Alterações
- **`src/hooks/useCards.ts`**
  - Import interno: `cardsExtraRepo`.
  - `cardsQueryFns.getCardGenericoDialog` e `getCardFull`.
  - `useUpdateCardById` (invalida namespace cards).
- **`src/components/cards/CardRecursoDialog.tsx`**: remove import direto; leitura via `cardsQueryFns.getCardFull`, update via `useUpdateCardById`.
- **`src/components/cards/CardGenericoDialog.tsx`**: remove import direto; leitura via `cardsQueryFns.getCardGenericoDialog`.

## Validação
- `tsgo --noEmit` limpo.
- `rg "cardsExtraRepo" src/` só retorna `useCards.ts` (canônico), definição em `cardsExtra.ts` e teste `TST-002.f`.
