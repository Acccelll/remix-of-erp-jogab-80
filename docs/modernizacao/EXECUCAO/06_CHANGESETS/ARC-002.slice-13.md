# ARC-002 · slice-13 — Migração `obras`/`clientes`/`players` (batch 3)

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Entregue

- `src/pages/rh/Colaboradores.tsx` — `obras` via `useObrasContext()`.
- `src/pages/rh/Funcoes.tsx` — `obras` via `useObrasContext()`.
- `src/pages/financeiro/FinObraDetalhe.tsx` — `obras` via `useObrasContext()` (junto com `updateObra`, evitando duplicidade).
- `src/pages/crm/CRMFunil.tsx` — `clientes` via `useClientesContext()`, `players` via `usePlayersContext()`.
- `src/pages/GM.tsx` — sem alteração: já não consome `obras`/`clientes`/`players` de `useApp()` (só `currentPlayer` para auth).

Todas as páginas mantêm `useApp()` apenas para `hasAccess/currentPlayer` e mutações não migradas.

## Próximo

`ARC-002.slice-14` — batch 4: dialogs de perfil
(`ContratoProfileDialog`, `PatrimonioProfileDialog`, `EmployeeProfileDialog`,
`VeiculoProfileDialog`, `ClienteProfileDialog`).
