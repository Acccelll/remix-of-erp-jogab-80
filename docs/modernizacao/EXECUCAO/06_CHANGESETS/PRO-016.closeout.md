# PRO-016 · closeout — Workflow de aprovação de medição

**Onda:** 07 · **Achado:** PRO-016 · **Status:** concluído.

## Slices entregues
- **slice-01** — Workflow `rascunho|em_revisao|rejeitada → enviada → aprovada|rejeitada` com carimbos `enviada_por/em`, `aprovada_por/em`, `rejeitada_por/em`, `rejeicao_motivo`. UI com transições contextuais e badge exibindo autor + data/hora.
- **slice-02** — Notificação `medicao_aguardando_aprovacao` no sino, plugada em `useNotifPreferencias`; hook `useMedicoesAguardandoAprovacao` (refresh 60s) listando pendentes por idade, gated por `hasAccess("obras_div","editar")` + `isVisivel(...)`.
- **slice-03** — Segregação de aprovação no backend (helpers `user_pode_aprovar_medicao`, novos papéis `fiscalizacao`/`qualidade`, trigger `trg_medicao_status_transition_guard`) e trilha automática em `historico_medicao` (trigger `trg_medicao_status_history_after_update`). UI expõe `canAprovar` e "Histórico de aprovação" na aba.

## Aceite (Etapa 5.5)
> _"estados de medição com aprovação registrada (quem/quando) antes do faturamento."_

Coberto: carimbos de auditoria (slice-01), notificação ativa para o aprovador (slice-02), segregação de função + histórico imutável (slice-03).

## Fora do escopo (não regride)
- Retropreenchimento de histórico para medições anteriores ao slice-03.
- Filtro por vínculo do usuário na notificação — hoje mostra todas as pendentes visíveis ao perfil (permissão + isVisivel já contêm o ruído).
- Faturamento automatizado a partir da aprovação — segue em achado próprio da Onda 07/08.

## Validação
- `bunx tsgo --noEmit` limpo em todos os slices.
- Migrations aplicadas com `GRANT` explícito e RLS habilitado na tabela nova.
- Fluxo manual: enviar → sino sinaliza para aprovador → aprovar/rejeitar grava carimbo e linha no histórico; usuários sem setor técnico não conseguem aprovar mesmo via chamada direta.

## Próximo achado
Onda 07 segue com `PRO-014` (workflow de NC) ou próximo item priorizado da fila.
