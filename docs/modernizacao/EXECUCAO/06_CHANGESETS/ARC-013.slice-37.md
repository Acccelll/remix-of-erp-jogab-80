# ARC-013 · slice-37 — importadores de cronograma sem imports diretos do repo

## Alterações
1. **`src/hooks/useCronograma.ts`**: adiciona passthrough `cronogramaBaselineItensFns`.
2. **`src/components/import/CronogramaImporter.tsx`**: import de `@/lib/repositories/cronograma` substituído por `@/hooks/useCronograma` com aliases (mantém o corpo intacto).
3. **`src/components/import/CronogramaSemanalImporter.tsx`**: idem.

## Escopo intencional
`src/lib/cronograma/recalcular-cpm.ts` (lib de domínio) segue consumindo o repo diretamente — slice separada.

## Validação
- `bunx tsgo --noEmit`: limpo.
- `rg 'from "@/lib/repositories/cronograma"' src/components/import`: sem ocorrências.
