# PRO-008 — NOTA-FIM (retroativa, fase 1)

**Onda:** 7 · **Achado:** PRO-008 — NC de inspeção não deriva Restrição (M4) / Card (M2)
**Slices:** `PRO-008.slice-01`, `PRO-008.slice-02`
**Status:** ✅ ENCERRADO (fase 1 — núcleo puro + mapper testável)

## Escopo entregue

- **slice-01** — `src/lib/inspecoes/nc-derivados.ts`: derivadores puros
  `ncParaRestricao`, `ncParaCard`, tipos `NCDerivavel`, `RestricaoPayload`,
  `CardPayload`, `isNCAtiva`, `severidadeParaTipoRestricao`. Sem I/O.
  Regras:
  - NC `aberta | em_tratativa | aguardando_reinspecao` ⇒ restrição `aberta`
    + card ativo (`aberto`/`em_andamento`).
  - NC `resolvida` ⇒ restrição `resolvida`, card `concluido`.
  - NC `cancelada` ⇒ restrição `cancelada`, card `cancelado` + arquivado.
  - Vínculo 1:1: `origem_tipo='nao_conformidade'` + `origem_id=nc.id`.
  - Severidade `alta|critica` → restrição tipo `seguranca`; demais `outro`.
- **slice-02** — `src/lib/inspecoes/nc-persistencia.ts`: `restricaoToRow`,
  `cardToRow`, `diffRestricao`, `diffCard` + whitelist
  `CARD_CAMPOS_DERIVADOS`. `diffCard` conservador (em card existente só o
  subconjunto derivado é sobrescrito; `titulo`/`descricao` editados
  preservados). Contrato `CardStatusPayload` alinhado ao enum `card_status`.
- Testes: `nc-derivados.test.ts`, `nc-persistencia.test.ts`.

## Validação

- `bunx tsgo --noEmit` limpo.
- `bunx vitest run src/lib/inspecoes/__tests__/nc-derivados.test.ts` e
  `nc-persistencia.test.ts` — verdes.

## Débitos residuais (fase 2 — onda futura)

Reconhecidos na própria slice-02; **NÃO** consumidos nesta onda por depender
de migration + fluxo backend:

- **slice-03** — migration para `restricoes` (tabela ainda ausente do schema
  público), colunas `origem_tipo`/`origem_id`, índices únicos por origem +
  `GRANT`/RLS.
- **slice-04** — repositório `ensureFromNC(ncId)` idempotente (upsert em
  `cards` e `restricoes` via os mappers já prontos).
- **slice-05** — disparo automático nas transições do workflow `PRO-007`;
  M2/M4 exibem o vínculo com a NC de origem.
- **slice-06** — regressão cruzada com `PRO-030` (notificações) e `PRO-007`
  (workflow permanece fonte da verdade).

O achado é encerrado nesta onda com débito estrutural registrado — o núcleo
determinístico está pronto para consumo assim que a fase backend for aberta
(candidata natural à **Onda 8**).

## Rastreabilidade

- Achado: `docs/modernizacao/00_INVENTARIO/PRO-008`.
- Slices entregues: `PRO-008.slice-01.md`, `PRO-008.slice-02.md`.
- Débito: registrar no backlog da Onda 8 (slices 03–06).
- Registro de conclusão: `05_ONDAS/ONDA_07/10_Checklist_Conclusao.md`.
