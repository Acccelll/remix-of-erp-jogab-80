# PRO-012 · slice-13 — Filtro por local e drilldown por motivo no painel de importações

## Escopo

Enriquecer o painel "Qualidade das importações XLSX" (slice-12) com:
1. Filtro por local (Select) restringindo todas as métricas.
2. Drilldown clicável sobre cada motivo de "ignorada", listando as
   contagens/importações que originaram aquelas linhas.

## Alterações

### `src/pages/suprimentos/ContagensCiclicas.tsx`
- Novos estados: `filtroLocalImp` (default `__all__`) e `motivoDrill`.
- Memos: `locaisComImportacao`, `rowsImpFiltrados`, `drilldownContagens`.
- `metricasImp` passa a ser calculado sobre `rowsImpFiltrados`.
- Cabeçalho do painel recebe Select com "Todos os locais" + lista dos
  locais com pelo menos 1 importação.
- Painel agora renderiza quando `locaisComImportacao.length > 0`;
  quando o filtro zera as métricas, mostra estado vazio explícito.
- Chips "Ignoradas por motivo" viram botões (`variant="outline"` /
  `variant="default"` quando ativo) e alternam `motivoDrill`.
- Nova tabela de drilldown (max-h-48, scroll) com: Local, Data planejada,
  Importado em, Arquivo, Total de ignoradas do motivo selecionado.
- Botão "limpar drilldown" quando há motivo ativo.
- Trocar de local limpa o drilldown para evitar estado incoerente.

## Contrato de execução

- Escopo restrito a `PRO-012`, apenas UI + memos derivados.
- Sem alterações no lib, no schema ou em RLS.
- Todos os dados vêm de `auditoriasImportacao[]` já persistidos.

## Validação

- `bunx tsgo --noEmit` sem erros novos.

## Próximo passo sugerido

`PRO-012.slice-14` — abrir a contagem diretamente a partir do drilldown
(clique na linha → `abrirExecucao(contagem)`), permitindo revisar
imediatamente as linhas ignoradas dentro do diálogo de execução.
