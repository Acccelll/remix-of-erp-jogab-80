# SEC-002.freeze-guard-01.parser-bump-01.materialize-01 — Materialização canônica do `sec002-freeze-guard` data-driven

**Onda:** 2 (bloqueada — depende de `SEC-001` fechar e da Onda 2 ser formalmente aberta)
**Data:** 2026-07-12
**Depende de:**
- `SEC-002.freeze-guard-01.spec-01` — contrato original (F1–F8);
- `SEC-002.apply-map-01.parser-bump-01.materialize-01` (R53) — apply-map `v2026-07-12.3` com `protected_paths[]` do parser;
- `SEC-002.parse-apply-id-01.materialize-01` (R51) — parser oficial reutilizado para extrair `APPLY_ID`;
- `SEC-002.freeze-guard-01.parser-bump-01.spec-01` (R54) — contrato desta materialização.

**Escopo:** materializar, em conteúdo canônico versionado neste documento, `scripts/sec002/freeze-guard.mjs` e `.github/workflows/sec002-freeze-guard.yml`, agora data-driven a partir de `map.protected_paths[]` e implementando F9–F11 com códigos `E_FREEZE_F9/10/11`. **Nenhum arquivo de produção é gravado nesta etapa.** Gravação física permanece bloqueada até a abertura formal da Onda 2.

## 1. Invariantes cobertas

| Regra | Descrição executável |
|---|---|
| F1–F8 | mantidas conforme spec original (parsing de diff, exigência de `APPLY_ID`, cobertura por `allowed_paths[]`, saída `OK`/`FAIL`, `exit 0/1`, sem writes, sem rede, sem deps externas) |
| F9 | todo caminho em `map.protected_paths[]` é avaliado; nenhum ignorado |
| F10 | `scripts/sec002/parse-apply-id.mjs` só editável sob `apply_id === "parse-apply-id-01"` |
| F11 | `.github/sec002-evidence/parse-apply-id-01/**` só editável sob `apply_id === "parse-apply-id-01"` |

## 2. Artefato 1 — `scripts/sec002/freeze-guard.mjs`

**Caminho:** `scripts/sec002/freeze-guard.mjs`
**Encoding:** UTF-8, LF, sem BOM. ESM puro. Node ≥ 20. Sem deps externas.

```js
#!/usr/bin/env node
// Freeze-guard data-driven: fonte única = map.protected_paths do apply-map canônico.
import { execSync } from "node:fs" in {} ? null : null; // sentinel; substituído abaixo
import { execSync as _exec } from "node:child_process";
import { loadApplyMap, getApply } from "./load-apply-map.mjs";

const NAME = "sec002/freeze-guard";
const fail = [];
const push = (code, msg) => fail.push(`${code} ${msg}`);

const map = loadApplyMap();

// 1. Diff contra origin/main
let changed = [];
try {
  const out = _exec("git diff --name-only origin/main...HEAD", { encoding: "utf8" });
  changed = out.split("\n").map(s => s.trim()).filter(Boolean);
} catch (e) {
  console.error(`[${NAME}] FAIL - não foi possível obter diff (${e.message})`);
  process.exit(1);
}

// 2. Casamento por prefixo literal / glob "**" no fim
const matchProtected = (file, pattern) => {
  if (pattern.endsWith("/**")) return file.startsWith(pattern.slice(0, -2));
  return file === pattern;
};

// 3. APPLY_ID vem de $GITHUB_ENV populado por parse-apply-id.mjs
const APPLY_ID = process.env.APPLY_ID || "";
let apply = null;
if (APPLY_ID) {
  try { apply = getApply(APPLY_ID); } catch { apply = null; }
}

const covers = (file) => {
  if (!apply) return false;
  return (apply.allowed_paths || []).some(p => matchProtected(file, p));
};

// 4. F9 — avalia todo protected_paths[]
for (const file of changed) {
  const hits = map.protected_paths.filter(p => matchProtected(file, p));
  if (hits.length === 0) continue;

  if (!APPLY_ID) { push("E_FREEZE_F9", `${file} protegido mas PR sem APPLY_ID`); continue; }
  if (!apply)    { push("E_FREEZE_F9", `${file} protegido e APPLY_ID=${APPLY_ID} inválido`); continue; }
  if (!covers(file)) push("E_FREEZE_F9", `${file} fora de allowed_paths[] de ${APPLY_ID}`);

  // F10 / F11 — caminhos do parser exigem apply específico
  if (matchProtected(file, "scripts/sec002/parse-apply-id.mjs") && APPLY_ID !== "parse-apply-id-01")
    push("E_FREEZE_F10", `${file} só editável sob parse-apply-id-01 (APPLY_ID=${APPLY_ID})`);
  if (matchProtected(file, ".github/sec002-evidence/parse-apply-id-01/**") && APPLY_ID !== "parse-apply-id-01")
    push("E_FREEZE_F11", `${file} só editável sob parse-apply-id-01 (APPLY_ID=${APPLY_ID})`);
}

if (fail.length === 0) { console.log(`[${NAME}] OK`); process.exit(0); }
console.error(`[${NAME}] FAIL`);
for (const v of fail) console.error(`  - ${v}`);
process.exit(1);
```

Contratos:

- `stdout` em sucesso: única linha `[sec002/freeze-guard] OK`; `exit 0`.
- `stderr` em falha: `[sec002/freeze-guard] FAIL` + uma violação por linha com código `E_FREEZE_F<n>`; `exit 1`.
- Nunca escreve em disco, nunca faz rede, nunca lê `.github/sec002-apply-map.json` direto — apenas via `loadApplyMap`.
- Nunca reimplementa parsing de `APPLY_ID`; consome `process.env.APPLY_ID` populado pelo pré-gate `parse-apply-id`.

## 3. Artefato 2 — `.github/workflows/sec002-freeze-guard.yml`

**Caminho:** `.github/workflows/sec002-freeze-guard.yml`

```yaml
name: sec002-freeze-guard
on:
  pull_request:
    paths:
      - "**"

concurrency:
  group: sec002-freeze-guard-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  guard:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      PR_TITLE: ${{ github.event.pull_request.title }}
      PR_BODY: ${{ github.event.pull_request.body }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: parse-apply-id
        run: |
          APPLY_ID="$(node scripts/sec002/parse-apply-id.mjs)"
          echo "APPLY_ID=${APPLY_ID}" >> "$GITHUB_ENV"
      - name: validate apply-map
        run: node scripts/sec002/validate-apply-map.mjs
      - name: freeze-guard
        run: node scripts/sec002/freeze-guard.mjs
```

Contratos:

- Único pré-gate `parse-apply-id`; nenhum re-parse posterior.
- `fetch-depth: 0` para garantir que `git diff origin/main...HEAD` funcione.
- Sem segredos, sem write permissions, sem chamadas externas.
- Independente de `sec002-pr-checklist` — falha aqui bloqueia o PR mesmo que o checklist passe.

## 4. Compatibilidade

| Consumidor / Guard | Efeito |
|---|---|
| `sec002-consumers-guard` | continua exigindo R1–R7 de `freeze-guard.mjs` (import do loader, sem leitura direta do JSON, `OK`/`FAIL`, `exit 0/1`) |
| `sec002-pr-checklist` | inalterado (11 gates) |
| Apply-map `v2026-07-12.3` | `protected_paths[]` é a única fonte de verdade — o guard reflete R53 automaticamente |

## 5. Procedimento de gravação futura

1. Gravar `scripts/sec002/freeze-guard.mjs` (§2).
2. Gravar `.github/workflows/sec002-freeze-guard.yml` (§3).
3. Rodar localmente:

```bash
node scripts/sec002/validate-apply-map.mjs
node scripts/sec002/check-consumers.mjs
APPLY_ID=parse-apply-id-01 node scripts/sec002/freeze-guard.mjs
```

Saída esperada em ausência de diff protegido:

```text
[sec002/validate-apply-map] OK
[sec002/consumers-guard] OK
[sec002/freeze-guard] OK
```

## 6. Evidência obrigatória

`.github/sec002-evidence/freeze-guard-parser-bump-01/manifest.json`:

```json
{
  "sec002_apply_id": "freeze-guard-parser-bump-01",
  "schema_version": "v2026-07-12.3",
  "source": "docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.freeze-guard-01.parser-bump-01.materialize-01.md",
  "changed_files": [
    "scripts/sec002/freeze-guard.mjs",
    ".github/workflows/sec002-freeze-guard.yml"
  ],
  "checks": [
    "node scripts/sec002/validate-apply-map.mjs",
    "node scripts/sec002/check-consumers.mjs",
    "APPLY_ID=parse-apply-id-01 node scripts/sec002/freeze-guard.mjs"
  ],
  "hashes": {
    "scripts/sec002/freeze-guard.mjs": "<sha256>",
    ".github/workflows/sec002-freeze-guard.yml": "<sha256>"
  },
  "refs": ["R51", "R53", "R54"]
}
```

## 7. Critérios de rejeição futura

- Guard mantendo lista hard-coded de caminhos protegidos.
- Guard reimplementando parsing de título/corpo de PR em vez de consumir `$APPLY_ID`.
- Guard lendo `.github/sec002-apply-map.json` sem passar por `loadApplyMap`.
- Ausência de `fetch-depth: 0` no checkout (quebra o diff).
- Workflow sem `timeout-minutes` ou sem `concurrency`.
- Códigos `E_FREEZE_F9/10/11` ausentes ou emitidos em `stdout` em vez de `stderr`.

## 8. Impacto em produção

**Nenhum.** Documento de materialização canônica. Não grava scripts/workflows executáveis nesta etapa, não altera dados, não aplica migração, não muda função, não muda frontend e não altera política RLS.
