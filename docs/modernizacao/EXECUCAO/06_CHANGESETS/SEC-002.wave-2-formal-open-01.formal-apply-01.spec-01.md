# SEC-002.wave-2-formal-open-01.formal-apply-01.spec-01 — Spec do apply formal de abertura da Onda 2

**Onda:** 2 (abertura formal — apply separado, pós-verificação)
**Data:** 2026-07-12
**Depende de:**
- `SEC-002.wave-2-formal-open-01.spec-01` (R95) — contrato de abertura formal, PC1–PC7, FO1–FO8, bloco canônico e manifest previsto;
- `SEC-002.wave-2-formal-open-01.preconditions-verifier-01.materialize-01` (R98) — verificador executável `scripts/sec002/verify-wave-2-preconditions.mjs`;
- `SEC-001.formal-closure-01.materialize-01` (R101) — satisfaz `E_WAVE2_PC1`;
- `SEC-002.wave-2-formal-open-01.pc2-scope-01.artifact-01.materialize-01` (R106) — satisfaz `E_WAVE2_PC2`;
- `SEC-002.wave-2-formal-open-01.pc3-scope-01.pair-debt-01.materialize-01` (R113) — satisfaz `E_WAVE2_PC3`;
- `SEC-002.wave-2-formal-open-01.pc4-scope-01.verifier-patch-01.materialize-01` (R116) — satisfaz `E_WAVE2_PC4`;
- `SEC-002.wave-2-formal-open-01.pc5-scope-01.guards-status-01.materialize-01` (R119) — satisfaz `E_WAVE2_PC5`;
- `SEC-002.wave-2-formal-open-01.pc6-scope-01.dri-package-01.materialize-01` (R122) — satisfaz `E_WAVE2_PC6`;
- `SEC-002.wave-2-formal-open-01.pc7-scope-01.decision-package-01.materialize-01` (R125) — satisfaz `E_WAVE2_PC7`.

**Escopo:** especificar, de forma canônica e verificável, o **apply formal separado** que executará a transição de `.github/sec002-readiness/wave-2.md` de `BLOCKED` para `OPEN`, conforme bloco canônico definido em R95 §3. Este documento é **spec** — a materialização (R127) só pode ser emitida após esta spec ser registrada como linha 119 do `SEC-001.README.md` e após o verificador PC1–PC7 sair `OK` sobre `main`. Nenhum arquivo de produção, script, workflow, apply-map ou `INDEX.md` é alterado por esta spec.

## 1. Contrato herdado de R95

Esta spec **não redefine** PC1–PC7, FO1–FO8, bloco canônico ou manifest previsto — herda-os byte-a-byte de R95. Toda divergência é rejeição automática. Esta spec apenas:

1. Congela o **apply-id** operacional `wave-2-formal-open-01` como escopo desta ação isolada.
2. Fixa o **write set físico** do apply.
3. Fixa a **pré-execução obrigatória** do verificador R98.
4. Fixa o **manifest físico** a ser gravado em R127.
5. Reafirma que o apply é o movimento operacional autorizado por **FS7** do selo R94.

## 2. Invariantes específicas (FA1–FA12)

| Regra | Descrição executável |
|-------|----------------------|
| FA1 | Write set físico do apply R127 é exatamente `{".github/sec002-readiness/wave-2.md", ".github/sec002-evidence/wave-2-formal-open-01/manifest.json"}` — nada além |
| FA2 | Bloco canônico apendado ao checklist é textualmente idêntico ao definido em R95 §3, com apenas `<YYYY-MM-DDTHH:MM:SSZ>` e `<preencher em R96>` substituídos por valores reais |
| FA3 | Campo `Status: BLOCKED` no cabeçalho do checklist muda para `Status: OPEN` no mesmo commit; nenhuma outra linha do arquivo é tocada |
| FA4 | `Apply-map: v2026-07-12.4` preservado — abertura não bumpa schema (FO3) |
| FA5 | Nenhum arquivo em `src/**`, `supabase/functions/**`, `supabase/migrations/**`, `scripts/sec002/**` ou `.github/workflows/**` é alterado (FO4/FO5) |
| FA6 | `INDEX.md` **não** é alterado no mesmo commit do apply — self-register do apply operacional é changeset separado posterior (FO6) |
| FA7 | Pré-execução obrigatória: `node scripts/sec002/verify-wave-2-preconditions.mjs --json` deve retornar exit 0 com todos os PCx = `OK` sobre `main`; SHA do commit verificado é gravado no manifest |
| FA8 | Manifest R127 declara: `schema_version: v2026-07-12.4`, `apply_id: wave-2-formal-open-01`, `source: SEC-002.wave-2-formal-open-01.formal-apply-01.spec-01.md`, `changed_files: [".github/sec002-readiness/wave-2.md"]`, `preconditions_verified: {PC1..PC7}` com anchor textual para cada evidência, `guards_green_run`, `verifier_sha`, `verifier_output_json`, `refs: [R95, R98, R101, R106, R113, R116, R119, R122, R125, R126, R127]` |
| FA9 | `DRI GM: Cappucceno` e `DRI técnico: Lovable` são os únicos valores aceitos, ambos obrigatórios e segregados (PC6) |
| FA10 | Selo R94 (FS1–FS8) preservado — este é o movimento operacional explicitamente permitido por FS7, não um novo self-register preparatório |
| FA11 | Reversão do apply exige `SEC-002.wave-2-formal-open-01.errata-01`; edição direta do checklist é rejeição (FO8) |
| FA12 | Após R127, self-register operacional do apply e do manifest é changeset separado (`SEC-002.wave-2-formal-open-01.formal-apply-01.self-register-01.spec-01` + `.materialize-01`), fora do escopo desta spec |

## 3. Sequência canônica de execução prevista em R127

1. Rodar `node scripts/sec002/verify-wave-2-preconditions.mjs --json` sobre `main` e capturar saída completa.
2. Verificar exit 0 e cada `pc[i].status === "OK"`; abortar em qualquer falha.
3. Editar `.github/sec002-readiness/wave-2.md`: trocar `Status: BLOCKED` → `Status: OPEN` no cabeçalho; apender bloco `## Abertura formal` de R95 §3 preenchido.
4. Gravar `.github/sec002-evidence/wave-2-formal-open-01/manifest.json` conforme FA8.
5. Rodar novamente `verify-wave-2-preconditions.mjs --json` sobre o working tree pós-edição para confirmar PC1–PC7 = OK e capturar `verifier_output_json`.
6. Commitar exatamente os 2 arquivos de FA1.

Cada passo é auditável por diff; qualquer desvio caracteriza rejeição de R127.

## 4. Critérios de aceitação

1. R126 é registrado no `SEC-001.README.md` como linha 119, coluna `tipo = especificação`.
2. Nenhum arquivo além do `SEC-001.README.md` é alterado por esta spec.
3. R127 permanece bloqueada até que `verify-wave-2-preconditions.mjs --json` saia `OK` sobre `main` com os 7 PCx verdes.
4. FA1–FA12 são executáveis e verificáveis por leitura do diff de R127.
5. Contrato de R95 (PC1–PC7, FO1–FO8, bloco canônico, manifest) é preservado byte-a-byte.

## 5. Critérios de rejeição

1. Alterar qualquer PCx, FOx, bloco canônico ou schema do manifest herdado de R95 (rejeição automática).
2. Materializar R127 sem que o verificador R98 tenha saído `OK` imediatamente antes sobre `main` (viola FA7).
3. Incluir qualquer arquivo fora do write set de FA1 no commit do apply (viola FA1/FA5/FA6).
4. Bumpar apply-map para `v2026-07-12.5` no mesmo apply (viola FA4/FO3).
5. Registrar linha nova no `INDEX.md` no mesmo commit do apply (viola FA6).
6. Marcar `OPEN` sem DRI técnico segregado da GM ou com valores diferentes de `Cappucceno`/`Lovable` (viola FA9/PC6).
7. Editar `.github/sec002-readiness/wave-2.md` fora de R127 ou sob errata informal (viola FA11/FO8).
8. Criar novo self-register preparatório entre R126 e R127 (viola FA10/FS1 do selo R94).

## 6. Próximo passo formal

`SEC-002.wave-2-formal-open-01.formal-apply-01.materialize-01` (R127) — apply canônico que executa a sequência §3 sob FA1–FA12, condicionado a exit 0 do verificador R98 sobre `main`. Self-register operacional do apply é changeset separado posterior a R127.
