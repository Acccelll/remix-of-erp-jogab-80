# TST-002.g — Contratos de notas fiscais e pedidos/compras

**Onda:** 6 (bloqueada — NO-GO D-9)  
**Data:** 2026-07-12  
**Escopo:** cobertura incremental antes de ARC-005 e antes da sub-onda 5.F.

## Alvo

- `src/lib/financeiro-totvs/queries.ts` — superfície real de notas fiscais:
  `fetchNotasFiscais` e `fetchRecebimentos`.
- `src/lib/repositories/suprimentos.ts` — superfície real de pedidos/compras:
  OCs, recebimentos, cotações, propostas, itens de OC/orçamento, composições,
  estoque, alçadas e grupos de negociação.

> Nota: o código atual não expõe `notasFiscaisRepo`/`pedidosComprasRepo` com
> esses nomes. O lote fixa os contratos equivalentes existentes sem criar API
> nova de produção.

## Arquivo criado

- `src/lib/__tests__/integracao/repositories-contrato-g.test.ts` — 22 casos.

## Contratos travados

- `notas_fiscais`: projeção explícita, filtro `obra_id`, ordenação por
  `data_emissao desc`, retorno `[]` em `data=null` e propagação de erro.
- `recebimentos`: projeções explícitas nas leituras financeira/suprimentos e
  ordenações por `data_prevista`/`data_recebimento`.
- `ordens_compra`: listagem com colunas mínimas e detalhe via `eq(id)` +
  `.single()`.
- `cotacoes`/`cotacao_propostas`: fechamento de cotação, escolha exclusiva de
  proposta e short-circuit em erro da primeira etapa.
- `ordem_compra_itens`, `orcamento_itens`, `composicoes`, `composicao_itens`,
  `estoque_saldos`, `alcadas_aprovacao`, `card_grupos_negociacao` e `cards`:
  tabelas, projeções, filtros e ordenações protegidos contra regressão.

## Resultado

- Lote G: 22/22 ✔
- Suite total: **552/552** ✔
- `tsgo --noEmit`: verde

## Acumulado TST-002

7 lotes · 24 repositórios/fatias · **124 casos**.

## Próximo

`TST-002.h` — `dp` + `dpHolerite` (folha/provisões/holerites antes das refatorações estruturais).