# SEC-002 · tests-02-suite / apply-02 / self-register-01 / spec-01

- **Ref:** R145
- **Tipo:** especificação
- **Onda:** 2 — OPEN, gate PC1..PC7 verde
- **Apply-map:** `v2026-07-12.4` (a preservar)
- **Antecessores:** R143 (spec apply-02), R144 (materialização apply-02)
- **DRI:** Cappucceno (GM) / Lovable

## 1. Objetivo

Especificar o self-register do apply `tests-02-suite-apply-02` (R144) como **linha 33** de `.github/sec002-evidence/INDEX.md`, sob o mesmo contrato do R139/R140. Nenhum toque em produção, testes, runner, verificador, migrations ou apply-map.

## 2. Write set físico (autorizado — 2 arquivos, exatos)

1. `.github/sec002-evidence/INDEX.md` — **append** de 1 (uma) linha canônica ao final da tabela (linha útil 33), atualização de `Última atualização` para timestamp UTC do R146; linhas 1–32 preservadas byte-a-byte.
2. `.github/sec002-evidence/tests-02-suite-apply-02-self-register-01/manifest.json` — manifest próprio do self-register.

Qualquer 3º arquivo tocado → **REJECT**.

## 3. Invariantes (TSR2-1 … TSR2-10)

- **TSR2-1** — `INDEX.md` termina com exatamente 33 linhas úteis; linhas 1–32 idênticas ao hash `413d49f9…329beb`.
- **TSR2-2** — a linha 33 aponta para `tests-02-suite-apply-02/manifest.json` (R144), refs `R58, R95, R127, R136, R137, R138, R140, R142, R143, R144`.
- **TSR2-3** — `Apply-map: v2026-07-12.4` preservado literalmente.
- **TSR2-4** — `Última atualização` é o único metadado bumped; nenhum outro campo do cabeçalho alterado.
- **TSR2-5** — manifest inclui `schema_version: v2026-07-12.4`, `apply_id: tests-02-suite-apply-02-self-register-01`, `ref: R146`, `spec_ref: R145`, `index_row: 33`, `apply_map_version_preserved: true`, `expected_index_rows_before: 32`, `expected_index_rows_after: 33`, `hashes.INDEX.md` (SHA-256 pós-append).
- **TSR2-6** — `verify-wave-2-preconditions.mjs` NÃO é tocado (bump `EXPECTED_INDEX_ROWS: 32→33` delegado a R147).
- **TSR2-7** — nenhum toque em `supabase/tests/`, runner, `_lib/`, `helpers/`, `batch-*/`, `scripts/sec002/`, `src/`, edge functions, migrations, workflows.
- **TSR2-8** — sem segredos, sem `service_role_key`, sem URLs de banco.
- **TSR2-9** — gate PC1..PC7 ficará temporariamente `off_until_pc4_bump` entre R146 e R147; documentado no manifest.
- **TSR2-10** — invariante de referência: manifest do apply `tests-02-suite-apply-02/manifest.json` (R144, hash conhecido) permanece intocado.

## 4. Fora de escopo (delegado)

- **R146** — materialização (append linha 33 + manifest).
- **R147** — spec + materialização do bump PC4 `EXPECTED_INDEX_ROWS: 32→33`, reativando PC1..PC7.

## 5. Rejeição automática

PR que toque qualquer arquivo fora dos 2 do write set, altere linhas 1–32 do `INDEX.md`, mude `Apply-map`, edite o verificador ou introduza rollback → **REJECT**.
