# ARC-002.slice-05.close — Encerramento do slice de Obras

**Onda:** 6 · **Prioridade:** P1 · **Tipo:** REF/MIGRAÇÃO
**Achado:** ARC-002 (god-context)

## Consumers migrados

- `src/pages/Obras.tsx`
- `src/pages/financeiro/FinObraDetalhe.tsx`

`admin/Empresas.tsx` e `admin/ImportarObras.tsx` não consomem mutações de obras diretamente. `useApp()` mantido apenas para `hasAccess/currentPlayer/clientes/obras (leitura)`. Agregadores (`useAppRead`, `useCatalogos`) inalterados — não expõem mutações.

## Critérios preservados

- Sem alteração de UI, sincronização Flowcast, inativação ou persistência.
- `useApp()` mantém compatibilidade para consumers não migrados.
- Typecheck (`tsgo`) verde.

## Próximo

Abrir `ARC-002.slice-06` com `ClientesContext` (CRM: clientes + funil/oportunidades).
