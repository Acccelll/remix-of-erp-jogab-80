# PRO-012 · slice-01 — Contagens cíclicas de estoque (scaffold)

## Escopo
Início do achado **PRO-012** (Onda 07) — "Suprimentos: sem inventário /
contagem cíclica de estoque". Esta slice entrega apenas o scaffold do
módulo, sem tocar em movimentações reais de estoque.

## Alterações
- `src/lib/storage/keys.ts`
  - Nova chave `estoqueContagensCiclicas` = `gestaobra:estoque:contagensCiclicas`.
- `src/lib/estoque/contagens-ciclicas.ts` **(novo)**
  - Tipos: `ContagemCiclica`, `ItemContagemCiclica`, `StatusContagemCiclica`.
  - CRUD local: `listarContagensCiclicas`, `getContagemCiclica`,
    `criarContagemCiclica`, `atualizarContagemCiclica`, `removerContagemCiclica`.
  - Helpers: `recalcularDivergencias`, `STATUS_LABEL`.
- `src/pages/suprimentos/ContagensCiclicas.tsx` **(novo)**
  - Listagem dos planos de contagem persistidos localmente.
  - Diálogo para criar um plano vazio (local, critério, responsável, data).
- `src/pages/suprimentos/EstoqueHub.tsx`
  - Nova aba **"Contagens"** carregada via `lazy`.

## Fora de escopo
- Selecionar itens automaticamente por curva ABC.
- Congelar saldo esperado a partir do repositório de estoque.
- Registrar quantidades contadas / ajustes / divergências.
- Emitir movimentos de ajuste no repositório oficial (Supabase).
- Permissões finas por perfil (usa a permissão existente de Suprimentos).

Todos esses pontos entram nas próximas slices do PRO-012.

## Validação
- Tipagem: `swallow` chamado com a assinatura correta `(scope, err, reason)`.
- Novo módulo não afeta fluxos existentes de Estoque e Recebimento.
- Persistência 100% local (localStorage), coerente com os demais módulos
  offline do sistema (contratos, RDO, empresa, notif).

## Contrato de Execução
- Achado: **PRO-012** — Suprimentos: sem inventário/contagem cíclica.
- Onda: **07**.
- Dependência: ISOLADA (nenhum bloqueio).
- Status após esta slice: **em andamento** — scaffold pronto.

## Próximo passo
`PRO-012.slice-02` — congelar `saldoEsperado` a partir do repositório de
estoque ao iniciar a contagem, e permitir lançar `saldoContado` item a
item com cálculo automático de divergência.
