# SEC-002 · pc4-bump-03 · spec-01 (R160)

**Onda:** 2 (OPEN) · **Apply-map:** v2026-07-12.4 · **Gate:** restaura PC1..PC7 = green após apply

## Objetivo
Autorizar o bump da constante `EXPECTED_INDEX_ROWS` de `35` → `36` em `scripts/sec002/verify-wave-2-preconditions.mjs`, refletindo o self-register de R159 (linha 36 = `pc4-bump-02`).

## Write set físico autorizado (R161 — 2 arquivos)
1. `scripts/sec002/verify-wave-2-preconditions.mjs` — linha 10:
   - antes: `const EXPECTED_INDEX_ROWS = 35;`
   - depois: `const EXPECTED_INDEX_ROWS = 36;`
2. `.github/sec002-evidence/pc4-bump-03/manifest.json` — manifest (`schema_version: v2026-07-12.4`, `expected_index_rows_before: 35`, `expected_index_rows_after: 36`, `auto_applied: false`, `verifier_gate: on`).

## Invariantes PC4B3-1…PC4B3-8
- **PC4B3-1** Diff no verificador = 1 linha alterada (linha 10), 0 fora dela.
- **PC4B3-2** Nenhuma outra constante numérica alterada.
- **PC4B3-3** Lógica PC1/PC2/PC3/PC5/PC6/PC7 preservada byte-a-byte.
- **PC4B3-4** Loop `for (let i = 1; i <= EXPECTED_INDEX_ROWS; …)` inalterado.
- **PC4B3-5** `Apply-map: v2026-07-12.4` preservado.
- **PC4B3-6** `INDEX.md` NÃO é tocado por R161.
- **PC4B3-7** Após R161: `node verify-wave-2-preconditions.mjs --json` → `ok: true` (PC1..PC7).
- **PC4B3-8** Manifest `pc4-bump-03/manifest.json` gravado.

## Invariantes transversais TVBP5-1…TVBP5-8
- **TVBP5-1** Sem tocar `supabase/migrations/`.
- **TVBP5-2** Sem tocar `INDEX.md`, batch-B/pc4-bump-01/pc4-bump-02 artefatos.
- **TVBP5-3** Sem tocar `scripts/sec002/_lib/`, `helpers/`, `batches/`.
- **TVBP5-4** Sem tocar `src/`, `tests/`, workflows.
- **TVBP5-5** Sem tocar apply-map ou outros manifests.
- **TVBP5-6** Sem segredos, chaves ou tokens.
- **TVBP5-7** Nenhuma execução real contra DB.
- **TVBP5-8** Self-register (linha 37) fica delegado a R162+.

## Nota de auto-recorrência
Ciclo `pc4-bump-N` mantido conforme padrão canônico; nenhuma política alterada em R160.

## Estado esperado pós-R161
- Verificador linha 10 = `const EXPECTED_INDEX_ROWS = 36;`.
- Gate PC1..PC7 = green.
- Onda 2 permanece OPEN; nenhum artefato de dados/produção alterado.
