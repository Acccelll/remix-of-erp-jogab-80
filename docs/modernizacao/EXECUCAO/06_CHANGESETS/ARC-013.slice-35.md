# ARC-013 · slice-35 — orquestrar limpeza de cronograma em hook canônico

## Alterações
1. **`src/hooks/useCronograma.ts`**:
   - Importa `cronogramaBaselineItensRepo`, `cronogramaBaselinesRepo`, `cronogramaDependenciasRepo`, `cronogramaRevisoesRepo`.
   - Expõe `cronogramaRevisoesMutationFns` (`listIdsByObra`/`removeMany`/`remove`), estende `cronogramaItemRevisoesMutationFns` com `removeByRevisao`/`removeByRevisaoIds`, adiciona `cronogramaItemRevisoesQueryFns.listSnapshotsByRevisao`.
   - Adiciona orquestrador `limparCronogramaCompletoByObra(obraId)` cobrindo revisões → baselines → dependências → itens (mesma ordem crítica anterior).
2. **`src/components/obra-detalhe/LimparImportadosButton.tsx`**: remove imports de todos os repos de cronograma; chama `limparCronogramaCompletoByObra(obraId)`. Mantém imports de `medicoes` (fora do escopo desta slice).

## Validação
- `bunx tsgo --noEmit`: limpo.
- `rg 'from "@/lib/repositories/cronograma"' src/components/obra-detalhe/LimparImportadosButton.tsx`: sem ocorrências.
