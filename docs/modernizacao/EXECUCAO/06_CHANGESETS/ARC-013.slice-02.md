# ARC-013.slice-02 — Hook canônico `useRequisicoes`

**Onda:** 6 · **Achado:** ARC-013 · **Slice:** 02

## Alteração
- **`src/hooks/useRequisicoes.ts`** (novo): encapsula `requisicoesRepo` com TanStack Query.
  - Leituras: `useRequisicoesAll(cols?)`, `useRequisicoesResumoObra(limit=500)`.
  - Mutations: `useCreateRequisicao`, `useUpdateRequisicaoStatus`, `useUpdateRequisicao`. Toasts centralizados; invalidação em `onSettled` cobre todo o root `"requisicoes"` (`invalidateAllRequisicoes`).
  - Chaves em `requisicoesKeys` sob root registrado no slice-01 (`list[/<cols>]`, `resumo-obra/<limit>`).

## Comportamento
Sem alteração de runtime — nenhum consumidor migrado ainda. `requisicoesRepo` segue exportado.

## Validação
- `tsgo --noEmit`: limpo.

## Próximo passo
`ARC-013.slice-03` — migrar `src/pages/suprimentos/Requisicoes.tsx` (CRUD principal: `listAll`, `create`, `updateStatus`) para os hooks.
