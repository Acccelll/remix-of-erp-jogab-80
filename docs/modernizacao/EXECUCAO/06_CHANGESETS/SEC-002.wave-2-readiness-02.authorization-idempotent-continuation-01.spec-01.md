# SEC-002 · Wave 2 Readiness 02 · Authorization Idempotent Continuation 01 (R230')

**Tipo:** especificação (governança pura)
**Round:** R230'
**Ciclo:** documental — nenhum código de produção, migração, `src/`, `supabase/functions/`, `supabase/migrations/`, `_lib/`, verifier, workflow, apply-map, banco ou segredo é tocado.
**Consome:** R213', R214', R216', R217', R218', R219', R220', R221', R222', R223', R224', R225', R226', R227', R228', R229'.

## 1. Objetivo

Definir a regra canônica de **continuidade idempotente** para comandos genéricos repetidos (`Seguir`, `MANTENHA O PADRÃO`, `ONDA ATUAL`, `PRÓXIMO PASSO`) enquanto não existir intake formal GM/D-3 válido. A continuidade pode avançar governança documental, mas não altera a fila de R229' nem cria efeitos materiais.

## 2. Regra de continuidade

Enquanto não houver intake formal aceito por R228', cada comando genérico deve preservar:

1. `WAVE 2 — OPEN` como onda atual;
2. D-6 fechado;
3. PC1..PC7 verdes;
4. PC8/PC9 `SKIP` por default;
5. bloqueio de `helpers-02.deploy-01` até intake GM completo;
6. bloqueio de `helpers-03.apply-01` até intake D-3 completo e deploy efetivo de `helpers-02`;
7. inexistência de `authorization-ledger.md`;
8. inexistência de `GO-SEC-002-helpers-02-deploy-01.md`;
9. §5 de `D-3-SEC-002.md` vazio.

## 3. Efeito idempotente

Repetir `Seguir` sem intake formal:

- não muda estado da fila R229';
- não converte `SEM AUTORIZAÇÃO` em `PENDENTE GM` ou `PENDENTE D-3`;
- não reabre R213' ou R214';
- não cria ledger retroativo;
- não dispensa anexos de evidência R227';
- não dispensa validade temporal R226';
- não aceita segredo, chave, senha ou token como evidência.

O único efeito permitido é registrar nova especificação documental de continuidade, se necessário, mantendo o mesmo status operacional.

## 4. Escopo negativo

Este round **não**:

- cria `authorization-ledger.md`;
- cria `GO-SEC-002-helpers-02-deploy-01.md`;
- preenche §5 de `D-3-SEC-002.md`;
- assina qualquer decisão GM/D-3;
- promove `helpers-02` para `supabase/migrations/`;
- aplica `helpers-03`;
- altera verifier, `INDEX.md`, `EXPECTED_INDEX_ROWS`, apply-map, `_lib/`, batches, `src/`, workflows, banco;
- solicita, aceita, expõe, loga ou fabrica segredo algum.

## 5. Invariantes (AUTHIC-1…AUTHIC-9)

- **AUTHIC-1** Ciclo puramente documental; nenhum arquivo executável tocado.
- **AUTHIC-2** R213', R214', R216', R217', R218', R219', R220', R221', R222', R223', R224', R225', R226', R227', R228' e R229' preservados byte-a-byte.
- **AUTHIC-3** `authorization-ledger.md` continua **não criado**.
- **AUTHIC-4** `GO-SEC-002-helpers-02-deploy-01.md` continua **não criado**.
- **AUTHIC-5** `D-3-SEC-002.md §5` continua **vazio**.
- **AUTHIC-6** `SEC-002.inventory-02.csv` intacto (`sha256[:12] = 8c18a694442e…d66a404ef`).
- **AUTHIC-7** `.github/sec002-evidence/INDEX.md` = 45 linhas; `EXPECTED_INDEX_ROWS = 45`; `Apply-map: v2026-07-12.4`.
- **AUTHIC-8** PC1..PC7 verdes; PC8/PC9 SKIP.
- **AUTHIC-9** Nenhum segredo/chave/senha/token solicitado, aceito, exposto, logado ou fabricado.

## 6. Write set

- **Criado:** este arquivo.
- **Editado:** `SEC-001.README.md` — 1 linha appendada (linha 226).
- Nenhum outro arquivo é tocado.

## 7. Impacto em produção

**Nenhum.**