# SEC-002.wave-2-formal-open-01.pc7-scope-01 — Escopo auditável de PC7

**Onda:** 2 (bloqueada — PC1..PC6 OK; PC7 pendente)
**Rodada:** R123 (E3 — spec do escopo auditável de `E_WAVE2_PC7`)
**Standard:** E5
**Precedente autorizante:** `SEC-002.wave-2-formal-open-01.pc6-scope-01.dri-package-01.materialize-01.md` (R122) — PC6 satisfeito.
**Escopo:** fixar, sem materializar, o contrato auditável de `E_WAVE2_PC7` conforme já implementado em `verifyPC7()`.

---

## 1. Contrato observado em `verifyPC7()`

Fonte: `scripts/sec002/verify-wave-2-preconditions.mjs`, linhas 223–232.

`E_WAVE2_PC7 = OK` sse todas as condições abaixo são verdadeiras:

1. Existe e é legível o arquivo `.github/sec002-decisions/wave-2-formal-open-01.md`.
2. O conteúdo contém `Autor:\s*\S+` (autor não-vazio).
3. O conteúdo contém `Data:\s*<ISO-8601 UTC válido>` (aceito por `isoDateValue`).
4. Existe uma seção `## Justificativa` (opcionalmente numerada) com no mínimo 3 linhas não-vazias que não sejam o próprio cabeçalho.

Nenhuma outra propriedade é exigida por `verifyPC7()`.

---

## 2. Estado atual auditado

Dry-run `--only=PC7`:

```
FAIL E_WAVE2_PC7 arquivo ausente: .github/sec002-decisions/wave-2-formal-open-01.md
```

Causa: registro de decisão nunca criado. PC1..PC6 seguem `OK`.

---

## 3. Invariantes PC7S1–PC7S12

| ID | Invariante |
|----|------------|
| PC7S1 | R123 é documental: não cria decisão, não altera verificador, workflows, apply-map, `INDEX.md` ou produção. |
| PC7S2 | Caminho canônico do artefato é `.github/sec002-decisions/wave-2-formal-open-01.md`. |
| PC7S3 | Registro deve conter `Autor: <nome não vazio>`. |
| PC7S4 | Registro deve conter `Data: <ISO-8601 UTC>` reconhecível por `Date.parse` e regra `isoDateValue`. |
| PC7S5 | Registro deve conter seção `## Justificativa` (ou `## N. Justificativa`) com ≥ 3 linhas úteis. |
| PC7S6 | `verifyPC7()` não é alterado por nenhuma rodada até abertura formal da Onda 2. |
| PC7S7 | R124 materializará o registro conforme write set fixado em R125-spec-do-pacote. |
| PC7S8 | Materialização de PC7 não abre a Onda 2 por si só — abertura formal exige apply próprio. |
| PC7S9 | Nenhum bump de `EXPECTED_INDEX_ROWS`, `SCHEMA_VERSION`, apply-map ou `INDEX.md` nesta cadeia PC7. |
| PC7S10 | Nenhuma promoção de workflow/gate obrigatório durante PC7. |
| PC7S11 | Autor do registro deve ser distinto de placeholders (`TBD`, `TODO`, `<...>`); GM `Cappucceno` é autor válido. |
| PC7S12 | Após materialização de PC7, dry-run `--only=PC7` e `--only=PC1..PC7` devem retornar `OK`. |

---

## 4. Fora de escopo desta rodada

- Criar `.github/sec002-decisions/wave-2-formal-open-01.md`.
- Alterar `scripts/sec002/verify-wave-2-preconditions.mjs`.
- Tocar `INDEX.md`, apply-map, workflows, `src/**`, `supabase/**`.
- Declarar Onda 2 aberta.

---

## 5. Estado esperado após cadeia PC7 (R124 + spec de pacote R125 → R126)

- `.github/sec002-decisions/wave-2-formal-open-01.md` existe e satisfaz PC7S3–PC7S5.
- `E_WAVE2_PC1..PC7 = OK` em dry-run completo.
- Onda 2 continua **fechada** até apply de abertura formal separado (fora desta cadeia).

---

## 6. Impacto em produção

**Nenhum.** Documento de especificação. Não altera app, banco, auth, funções, frontend, permissões, workflows, apply-map, `INDEX.md` ou dados.
