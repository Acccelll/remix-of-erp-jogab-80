# ARC-002.slice-08.close — Encerramento do slice de Players

**Onda:** 6 · **Prioridade:** P1 · **Tipo:** REF/MIGRAÇÃO
**Achado:** ARC-002 (god-context)

## Consumers migrados

- `src/pages/GM.tsx` — `players/addPlayer/updatePlayer/deletePlayer` via `usePlayersContext()`; `currentPlayer` permanece em `useApp()`.
- `src/components/common/ChangePasswordDialog.tsx` — `updatePlayer` via `usePlayersContext()`; `currentPlayer` permanece em `useApp()`.

`Oportunidades.tsx`, `CRMFunil.tsx`, `NaoConformidades.tsx` e `OportunidadeFormDialog.tsx` usam hooks/caminhos próprios de consulta ou ainda misturam mutações CRM; não foram movidos neste slice.

## Critérios preservados

- Sem alteração de UI, autenticação, permissões ou persistência.
- `useApp()` mantém compatibilidade para consumers não migrados.
- Typecheck (`tsgo`) verde no encerramento do slice.

## Próximo

Abrir `ARC-002.slice-09` com `DocumentosContext` para vencimentos (`getExpiringDocuments`/`getAllExpiringDocuments`).