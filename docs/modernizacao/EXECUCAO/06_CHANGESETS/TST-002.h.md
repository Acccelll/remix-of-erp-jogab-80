# TST-002.h — Contratos de DP (dpRepo) e DP Holerite

**Onda:** 6 (bloqueada — NO-GO D-9)  
**Data:** 2026-07-12  
**Escopo:** travar contratos de folha/provisões/holerites antes das refatorações estruturais da Onda 6 e da sub-onda 5.F.

## Alvo

- `src/lib/repositories/dp.ts` — `dpRepo.listHolerites` e `dpRepo.listPontoPorPeriodo`.
- `src/lib/repositories/dpHolerite.ts` — `fetchAllHolerites`, `deleteCompetencia`, `fetchLastImport`, `upsertHoleriteBatch`, `mapRow`.

## Arquivo criado

- `src/lib/__tests__/integracao/repositories-contrato-h.test.ts` — 19 casos.

## Contratos travados

- `dp_holerite` (list padrão): projeção `id, colaborador_id, competencia, liquido, bruto` e ordenação `competencia desc`. Override de colunas preserva a ordenação.
- `ponto_registros`: projeção mínima, filtros `gte/lte` por `data` e ordenação por `data`.
- `fetchAllHolerites`: colunas explícitas completas (33 campos) + dupla ordenação `competencia desc, nome_lido asc`.
- `deleteCompetencia`: `delete().eq("competencia", ...)`.
- `fetchLastImport`: encadeia `select("competencia, imported_at").order(imported_at desc).limit(1)` + segundo `select("*", { count: "exact", head: true }).eq("competencia", ...)`. Retorna `null` sem 2ª chamada quando lista vazia; converte `count` nulo em `0`.
- `upsertHoleriteBatch`: no-op quando payload vazio; caso contrário `upsert(..., { onConflict: "cpf,competencia,tipo" })`, mapeia `origem` (`holerite_xls`/`adiantamento_xls`) e coleta `cpfsNaoCadastrados`.
- `mapRow`: coerção numérica robusta (`NaN → 0`), `fator_k` numérico preservado, `verbas` sempre array.

## Resultado

- Lote H: 19/19 ✔
- Suite total: **571/571** ✔
- `tsgo --noEmit`: verde (nenhuma mudança em código de produção)

## Acumulado TST-002

8 lotes · 26 superfícies · **143 casos**.

## Próximo

`TST-002.i` — `boards` + `cronograma` (dependências diretas do Kanban e do planner antes da Onda 6).
