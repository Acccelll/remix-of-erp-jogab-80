# SEC-002 · tests-02 / gate-01 / reconcile-affirm-01 / spec-01

- **Ref:** R206' (afirmação pós-reconciliação, governança pura)
- **Tipo:** especificação (nenhum artefato executável tocado)
- **Onda:** 2 — OPEN
- **Apply-map:** `v2026-07-12.4` (preservado)
- **Antecessores:** R189 (`gate-01.spec-01`), R191, R200'..R203' (D-6 cycle), R204' (`gen-01.reconcile-affirm-01`), R205' (`runner-01.reconcile-affirm-01`)

## 1. Objetivo

Fechar o loop `reconcile → gen → runner → gate` afirmando que o contrato `SEC-002.tests-02.gate-01.spec-01` **permanece válido byte-a-byte** após D-6a (R203'), e que as precondições **PC8** e **PC9** permanecem em **SKIP** com as mesmas razões declaradas (evidence dirs `tests-02-gen-01` e `tests-02-runner-01` ainda não aplicados; `SEC002_RUN_PC9` unset).

Este changeset é **puramente declaratório**: não altera `scripts/sec002/verify-wave-2-preconditions.mjs`, não ativa PC8/PC9, não avança nenhuma dependência dura.

## 2. Write set deste changeset

| # | Caminho | Ação |
|---|---------|------|
| 1 | `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.tests-02.gate-01.reconcile-affirm-01.spec-01.md` | criação (este arquivo) |

`SEC-001.README.md` recebe append de 1 linha no índice de changesets como registro do round. Nenhum outro arquivo tocado.

## 3. Estado atual dos gates PC1..PC9

| Gate | Estado | Razão |
|------|--------|-------|
| PC1  | ✅ ok  | `SCHEMA_VERSION=v2026-07-12.4` presente |
| PC2  | ✅ ok  | `DEFAULT_APPLY_ID=wave-2-formal-open-01` presente |
| PC3  | ✅ ok  | apply-map v2026-07-12.4 loader responde |
| PC4  | ✅ ok  | freeze-guard F1..F15 verdes |
| PC5  | ✅ ok  | DRI matrix D1..D10 verdes |
| PC6  | ✅ ok  | check-consumers R1..R7 verdes |
| PC7  | ✅ ok  | INDEX.md rows=45 = EXPECTED_INDEX_ROWS |
| PC8  | ⏸ SKIP | `tests-02-gen-01` evidence não aplicado |
| PC9  | ⏸ SKIP | `tests-02-runner-01` evidence não aplicado + `SEC002_RUN_PC9` unset |

Saída JSON atual (`verify-wave-2-preconditions.mjs --json`):
```json
{ "ok": true, "checked": ["PC1".."PC7"] }
```
Continuará válida byte-a-byte enquanto PC8/PC9 permanecerem em SKIP (campo `skipped` só é emitido pela materialização R190).

## 4. Efeito de D-6a sobre PC8/PC9 (quando materializados)

- **PC8** (`gen-tests-02.mjs --check`) — quando ativado, comparará os 315 arquivos derivados do CSV reconciliado. `batch-D/solicitacao_comentarios__*.sql` **não deve existir**; `batch-C/solicitacao_comentarios__{gm,member,outsider,anon}.sql` **deve existir**. Divergência = `E_WAVE2_PC8_DIFF` com `DIFF <caminho> expected=… got=…`.
- **PC9** (`run-sec-002-tests.sh --json`) — quando ativado, exigirá `totals.files === 315 && totals.fail === 0 && totals.timeout === 0`. Sumário canônico verde: `batch-B: 39/39, batch-C: 96/96, batch-D: 70/70, batch-E: 110/110`.

Ambos os gates continuam **OFF por default** (opt-in via evidence dirs + `SEC002_RUN_PC9=1`).

## 5. Efeito sobre `gate-01.spec-01`

- **§2 Contrato dos novos gates** — sem bump; precondições de execução (evidence + env) inalteradas.
- **§3 Preservação byte-a-byte de PC1..PC7** — sem bump; hash da região PC1..PC7 preservado.
- **§4 Saída** — sem bump; contrato humano/JSON preservado.
- **Dependências duras** — inalteradas: `gen-01.materialize-01`, `runner-01.materialize-01`, `assert-01.materialize-01`, `helpers-02.materialize-01`, `helpers-03.materialize-01`.

## 6. Invariantes deste changeset (RAG-1 … RAG-8)

- **RAG-1** — `SEC-002.inventory-02.csv` **não é tocado** (baseline `8c18a694442e…d66a404ef` preservado). ✅
- **RAG-2** — `SEC-002.tests-02.gate-01.spec-01.md` **não é tocado** byte-a-byte (sem bump). ✅
- **RAG-3** — `scripts/sec002/verify-wave-2-preconditions.mjs` **não é tocado** (PC1..PC7 preservados; PC8/PC9 permanecem em SKIP). ✅
- **RAG-4** — Nenhum `.sh`, `.sql`, `.mjs`, `_lib/`, batch, migration, verifier, `src/`, workflow, edge function ou banco tocado. ✅
- **RAG-5** — Saída JSON de `verify-wave-2-preconditions.mjs --json` permanece exatamente `{ ok: true, checked: [PC1..PC7] }` (campo `skipped` ausente). ✅
- **RAG-6** — `INDEX.md` = 45 linhas e `EXPECTED_INDEX_ROWS = 45` preservados. ✅
- **RAG-7** — `SCHEMA_VERSION`, `DEFAULT_APPLY_ID` e `Apply-map: v2026-07-12.4` preservados. ✅
- **RAG-8** — `verify-wave-2-preconditions.mjs --json` permanece `{ ok: true }` com PC1..PC7. ✅

## 7. Fechamento do ciclo D-6

Com R206', o ciclo de reconciliação D-6 fica **integralmente fechado** no plano:

| Fase | Ref | Artefato | Estado |
|------|-----|----------|--------|
| Request | R200' | `reconcile-01.d6-request-01.spec-01` | ✅ concluído |
| Skeleton | R201' | `03_DECISOES/D-6.md` | ✅ concluído |
| Signature | R202' | `reconcile-01.d6-sign-01.materialize-01` | ✅ assinada D-6a |
| Materialize CSV | R203' | `reconcile-01.materialize-01` | ✅ CSV reconciliado |
| Affirm gen | R204' | `gen-01.reconcile-affirm-01.spec-01` | ✅ concluído |
| Affirm runner | R205' | `runner-01.reconcile-affirm-01.spec-01` | ✅ concluído |
| Affirm gate | **R206'** | `gate-01.reconcile-affirm-01.spec-01` | ✅ **este round** |

Próximo bloqueio material do pipeline volta às duas dependências externas originais:

1. **GO GM → `helpers-02.deploy-01`** (R193).
2. **D-3 → `helpers-03.apply-01`** (R194).

## 8. Impacto em produção

**Nenhum.** Afirmação documental exclusivamente. Banco, migrations, policies, `_lib/`, batches, scripts executáveis, workflows, `src/` e apply-map permanecem intocados.
