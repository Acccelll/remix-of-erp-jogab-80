# PRO-020 · slice-04 — Preferência de horizonte persistida

**Onda:** 08 · **Achado:** PRO-020 · **Depende de:** slice-03.

## Contexto
Slice-03 tornou o horizonte configurável (3/6/12/24), mas resetava a cada montagem. Usuários de planejamento revisam repetidamente na mesma janela e pediam persistência (mesmo padrão já adotado em `PRO-032.slice-03`).

## Entrega
- `src/pages/Contratos.tsx`
  - Estado `horizonteMeses` inicializa lendo `localStorage['gestaobra:contratos:cronograma:horizonte']` (fallback 6).
  - `useEffect` grava a chave a cada mudança, com try/catch defensivo.
  - Aceita apenas `[3, 6, 12, 24]`; valores inválidos caem em 6.

## Não escopo
- Sem escrita em Financeiro (D-8).
- Sem sync entre abas (não crítico para preferência de UI).

## Validação
- `bunx tsgo --noEmit` — OK.
