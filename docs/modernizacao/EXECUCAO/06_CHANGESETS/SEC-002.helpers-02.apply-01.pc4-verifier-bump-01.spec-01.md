# SEC-002.helpers-02.apply-01.pc4-verifier-bump-01.spec-01 — Bump PC4 após self-register do helpers-02

**Onda:** 2 (OPEN — reativação do verificador após R134)  
**Data:** 2026-07-12  
**Depende de:** R130 (`EXPECTED_INDEX_ROWS = 30`), R132 (`helpers-02-apply-01` materializado), R134 (`INDEX.md` com linha 31).

## 1. Escopo

Contratar o patch mínimo que torna `scripts/sec002/verify-wave-2-preconditions.mjs` novamente verde após o self-register R134. O único ajuste funcional permitido é atualizar a constante `EXPECTED_INDEX_ROWS` de `30` para `31`, refletindo a nova linha útil do `INDEX.md`.

## 2. Patch permitido

1. `EXPECTED_INDEX_ROWS = 31` em `scripts/sec002/verify-wave-2-preconditions.mjs`.
2. Manifest próprio em `.github/sec002-evidence/helpers-02-apply-01-pc4-verifier-bump-01/manifest.json`.
3. Registro executivo desta spec e da materialização no `SEC-001.README.md`.

Fica proibido tocar em `INDEX.md`, `candidate.sql`, manifest R132/R134, apply-map, workflows, `src/**`, funções, migrações ou banco.

## 3. Invariantes específicas (HVB1–HVB8)

| ID | Invariante |
|----|------------|
| HVB1 | `EXPECTED_INDEX_ROWS` passa exatamente de `30` para `31`. |
| HVB2 | `verifyPC4()` continua usando a constante tanto na contagem quanto no laço `1..N`. |
| HVB3 | `verifyPC2()` permanece no contrato pós-abertura (`OPEN` + bloco `Abertura formal`). |
| HVB4 | `.github/sec002-evidence/INDEX.md` não é alterado neste changeset. |
| HVB5 | `candidate.sql` e manifests `helpers-02-apply-01*` existentes permanecem byte-a-byte. |
| HVB6 | Nenhum arquivo de produção, workflow, apply-map ou migration é alterado. |
| HVB7 | `node scripts/sec002/verify-wave-2-preconditions.mjs --json` deve retornar `ok: true` com PC1..PC7. |
| HVB8 | Reversão exige errata específica; não é permitido reduzir a constante sem remover/errar a linha 31. |

## 4. Critérios de aceitação

1. R135/R136 são registrados no `SEC-001.README.md`.
2. O verificador completo retorna `OK` em modo JSON.
3. O índice permanece com 31 linhas úteis numeradas `1..31`.
4. Oda 2 permanece `OPEN` e pronta para o próximo item do write set.

## 5. Próximo passo formal

`SEC-002.helpers-02.apply-01.pc4-verifier-bump-01.materialize-01` — aplicar o patch sob HVB1–HVB8 e reativar PC1..PC7.