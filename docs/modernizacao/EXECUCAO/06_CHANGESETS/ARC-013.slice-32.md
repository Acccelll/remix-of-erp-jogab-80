# ARC-013 · slice-32 — encapsular update de item de cronograma em `LinhaBalancoTab`

## Alterações
1. **`src/hooks/useCronograma.ts`**: adiciona `cronogramaKeys.lobByObra(obraId)` e hook `useUpdateCronogramaItem({ invalidateKeys })` reaproveitável.
2. **`src/components/obra/LinhaBalancoTab.tsx`**: remove import de `cronogramaRepo`; passa a usar `cronogramaKeys.lobByObra(obraId)` na query e nas invalidações, e `useUpdateCronogramaItem` no lugar da `useMutation` inline (perda intencional do `onError` toast — passa a usar o padrão global; comportamento visível de invalidação preservado).

## Escopo intencional
`CronogramaPrincipalTab` (create/remove/update + revisoes), `RevisoesTab`, importadores e recálculo CPM seguem consumindo `cronogramaRepo` diretamente — próximos slices.

## Validação
- `bunx tsgo --noEmit`: limpo.
- `rg cronogramaRepo src/components/obra/LinhaBalancoTab.tsx`: sem ocorrências.
