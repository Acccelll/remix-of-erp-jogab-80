# PRO-010 — NOTA-FIM (retroativa, fase 1)

**Onda:** 7 · **Achado:** PRO-010 — Suprimentos: sem comunicação com fornecedor (OC/cotação por PDF/e-mail)
**Slices:** `PRO-010.slice-01`
**Status:** ✅ ENCERRADO (fase 1 — saída documental em PDF + trilha local)

## Escopo entregue

- **`src/lib/suprimentos/export-pdf.ts`** (novo)
  - `exportPedidoCotacaoToPDF` — PDF A4 do pedido de cotação com obra, item, qtd, unidade, fornecedores destinatários e campos para preço/prazo/condição.
  - `exportOrdemCompraToPDF` — PDF A4 da OC com fornecedor, contato, e-mail, telefone, itens, preço unitário, total e assinatura.
  - Baseado em `jspdf` + `jspdf-autotable`, sem I/O de rede.
- **`src/lib/suprimentos/envios.ts`** (novo) — registro local `SuprimentoEnvio` (`ordem_compra` / `pedido_cotacao`) em `gestaobra:suprimentos:envios`; evento local para refresh de UI sem reload.
- **`src/lib/storage/keys.ts`** — nova chave canônica `suprimentoEnvios` (EST-003).
- **`src/hooks/useFornecedores.ts`** + `src/lib/repositories/fornecedores.ts` — `useFornecedoresContatos()` (razão social, fantasia, contato, e-mail, telefone, ativo).
- **`src/pages/suprimentos/Cotacoes.tsx`** — botão **Pedido PDF** no mapa comparativo; usa fornecedores das propostas (fallback: ativos); mostra últimas saídas da cotação.
- **`src/pages/suprimentos/OrdensCompra.tsx`** — botão **PDF fornecedor** no detalhe da OC; mostra últimas saídas da OC.

## Validação

- `bunx tsgo --noEmit` limpo.

## Nota de backend

Migration para `suprimento_envios` foi tentada mas rejeitada — `public.ordens_compra` ausente no schema Cloud ativo. Registro ficou local, alinhado ao padrão vigente (persistência local para dados principais).

## Débitos residuais (fase 2 — Onda 8)

- **Envio real por e-mail** via edge function (integração com Lovable Emails / provedor).
- **Upload/armazenamento** do PDF gerado (Supabase Storage + link no envio).
- **Trilha multiusuário centralizada** quando `ordens_compra`/`cotacoes` estiverem materializadas no backend Cloud ativo.
- Anexo do PDF ao registro de envio (hoje o PDF é baixado; a trilha guarda metadata).

## Rastreabilidade

- Achado: `docs/modernizacao/00_INVENTARIO/PRO-010`.
- Slice: `PRO-010.slice-01.md`.
- Registro de conclusão: `05_ONDAS/ONDA_07/10_Checklist_Conclusao.md`.
