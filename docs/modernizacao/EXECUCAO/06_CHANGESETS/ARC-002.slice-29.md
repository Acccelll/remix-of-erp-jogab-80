# ARC-002 · slice-29

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Escopo
1. Novo `src/contexts/auth/AuthMethodsContext.tsx` — `AuthMethodsProvider` hospeda a chamada de `useAuthSession` e expõe `login`/`logout`. Reúne `currentPlayer` (do `AuthProvider`) e `players`/`dataLoaded` (do `AppContext`) num único ponto.
2. `src/contexts/AppContext.tsx` — removidos `login`/`logout` do `AppContextType`, do `useMemo` do value e do array de dependências. Import de `useAuthSession` removido.
3. `src/contexts/auth/useAuth.ts` — lê `login`/`logout` de `useAuthMethods()`; deixa de depender de `useApp()`.
4. `src/App.tsx` — `AuthMethodsProvider` montado imediatamente abaixo de `AppProvider` (dentro de `AuthProvider` → `AppProvider`).

## Motivação
Fecha a extração do domínio Auth do God-context. `useAuth()` agora consome **apenas** contextos dedicados (`AuthContext` + `AuthMethodsContext`) — nenhuma dependência residual de `AppContext`. `useAuthSession` continua sendo o motor, apenas movido para o lugar certo.

## Validação
- `rg "useApp\(\)" src/contexts/auth`: zero usos.
- `rg "\.login\(|\.logout\(" src` fora de `auth/`/`AppContext`/`useAuthSession`/`supabase.auth`: zero.
- `tsgo --noEmit`: sem erros.

## Próximo passo
`ARC-002.slice-30` — próximo candidato para extração real de estado do `AppContext`:
- opção A: `funcoes` + mutations (catálogo leve, poucos consumidores fora do `useApp`).
- opção B: `documentosTipo` + mutations (mesmo perfil).
Ambos permitem enxugar o `useMemo` gigante do `AppProvider` sem depender de reordenar providers.
