# ARC-013 · slice-26 — Encapsular `cardCamposValoresRepo` em `useCards`

## Objetivo
Remover acesso direto a `cardCamposValoresRepo` em componentes; expor leitura/mutação
apenas via `useCards.ts` (hooks + `cardsQueryFns` + `cardsKeys`).

## Mudanças
- `src/hooks/useCards.ts`
  - Import interno de `cardCamposValoresRepo`.
  - `cardsKeys.camposValores(cardId)` = `["card-campos-valores", cardId]`.
  - `cardsQueryFns.listCamposValoresByCard(cardId)`.
  - Helper `invalidateCamposValores(qc, cardId)`.
  - Mutation `useUpsertCardCampoValor({ cardId, campoId, valor })`.
- `src/components/cards/CardCamposCustom.tsx`
  - Remove import direto do repo e `useQueryClient`.
  - Query usa `cardsKeys.camposValores` + `cardsQueryFns.listCamposValoresByCard`.
  - `salvar()` usa `useUpsertCardCampoValor.mutateAsync`; invalidação vem do hook.

## Validação
- `tsgo --noEmit` limpo.
- `rg cardCamposValoresRepo src` restrito a `useCards.ts` (canônico) e `cardsExtra.ts`.
- Semântica preservada: mesma chave de cache e `onConflict=card_id,campo_id`.
