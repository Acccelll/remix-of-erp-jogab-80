# ARC-010.slice-02 · Planejamento → cache canônico de obras

**Onda:** 6  
**Achado-raiz:** ARC-010 (consumo direto de API fora do cache canônico)

## Escopo
- `src/pages/planejamento/Lookahead.tsx`
- `src/pages/planejamento/Restricoes.tsx`
- `src/pages/planejamento/Riscos.tsx`
- `src/pages/planejamento/PacotesTrabalho.tsx`
- `src/pages/planejamento/LicoesAprendidas.tsx`

## Mudança aplicada
- Remove `obrasRepo.listResumo()` dos carregamentos paralelos locais.
- Remove `useState<Obra[]>`/`setObras` como estado de domínio local.
- Deriva `obras` exclusivamente de `useObras().data`, mapeando apenas `{ id, nome }` para preservar a interface existente dos filtros, selects e `obraById`.
- Mantém os `load()` locais apenas para dados específicos das páginas de planejamento.

## Validação esperada
- `tsgo --noEmit` limpo.
- Zero `obrasRepo.listResumo()` remanescente nas páginas migradas neste slice.
- Comportamento preservado: filtros por obra, selects de formulários e labels por `obra_id` continuam usando a mesma estrutura `{ id, nome }`.