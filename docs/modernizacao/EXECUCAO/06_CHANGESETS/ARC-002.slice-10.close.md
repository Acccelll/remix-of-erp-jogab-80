# ARC-002 · slice-10 — MobilizacoesPendentesContext criado (compat)

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Entregue

- Novo `src/contexts/MobilizacoesPendentesContext.tsx` como camada de compat sobre `useApp()`, expondo:
  - `mobilizarColaborador`, `cancelarMobilizacaoColaborador`
  - `mobilizarPatrimonio`, `cancelarMobilizacaoPatrimonio`
  - `mobilizarVeiculo`, `mobilizarContrato`
- `MobilizacoesPendentesProvider` registrado em `src/App.tsx` na árvore interna, após `DocumentosProvider` e antes de `EmpresaProvider`.
- Nenhum consumer migrado neste slice — apenas provider + hook, seguindo o padrão dos slices anteriores.

## Ordem de providers atual

`App → Colaboradores → Patrimonios → Veiculos → Contratos → Obras → Clientes → Funcoes → Players → Documentos → MobilizacoesPendentes → Empresa`

## Próximo

`ARC-002.slice-10.close` + `slice-11`: migrar consumers reais das mobilizações (Board, QuadroPatrimonios, QuadroContratos, Veiculos, MobilizacaoProvisoria) para `useMobilizacoesPendentesContext()`, mantendo `useApp()` para o restante.
