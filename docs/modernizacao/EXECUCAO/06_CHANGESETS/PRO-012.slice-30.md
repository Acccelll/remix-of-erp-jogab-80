# PRO-012 · slice-30 — Resumo textual de tendência acima da toolbar

**Onda:** 07 · Suprimentos & Contagens Cíclicas
**Contrato:** UI-only.

## Escopo
Exibir um resumo com contadores de tendência (em piora / estáveis / em melhora) acima da toolbar da tabela, dando contexto rápido sobre o comportamento das últimas importações.

## Alterações
- `src/pages/suprimentos/ContagensCiclicas.tsx`:
  - Novo `useMemo` `resumoTendencia` que percorre `rowsTendencia` uma vez e devolve `{ piora, melhora, estavel, total }`.
  - `totalPiora` passa a derivar de `resumoTendencia.piora` (mesma semântica anterior, sem custo extra).
  - Novo bloco acima da toolbar (renderizado apenas quando `resumoTendencia.total > 0`) com três chips coloridos usando os mesmos tokens semânticos (destructive / muted / emerald) da coluna de tendência.
  - `aria-label` descritivo no container.

## Validação
- `bunx tsgo --noEmit` sem erros.
- Semântica visual alinhada aos badges de delta já existentes na tabela.

## Próximo passo
`PRO-012.slice-31` — tornar os chips do resumo clicáveis como atalhos de filtro (ex.: clicar "em piora" aplica `apenasPiora`).
