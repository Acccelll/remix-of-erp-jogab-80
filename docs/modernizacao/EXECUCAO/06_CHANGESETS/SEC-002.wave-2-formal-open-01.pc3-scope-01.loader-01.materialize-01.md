# SEC-002.wave-2-formal-open-01.pc3-scope-01.loader-01.materialize-01 — Materialização do loader do apply-map

**Onda:** 2 (bloqueada — PC1 e PC2 OK; PC3 parcial; PC4–PC7 pendentes)  
**Rodada:** R111 (E4 — materialize)  
**Standard:** E5  
**Precedente autorizante:** `SEC-002.wave-2-formal-open-01.pc3-scope-01.loader-01.spec-01.md` (R110).  
**Escopo:** gravar `scripts/sec002/load-apply-map.mjs` conforme R110 e registrar manifest de evidência.

---

## 1. Write set

| Arquivo | Ação | SHA-256 |
|---------|------|---------|
| `scripts/sec002/load-apply-map.mjs` | criado | `4a8f8942d55d1596d5fc3cd470364913fae844e235dceaeb7bfe5c9ba042f003` |
| `.github/sec002-evidence/wave-2-formal-open-01-pc3-loader-01/manifest.json` | criado | (manifest desta rodada) |

Nada mais foi tocado. `INDEX.md`, `.github/sec002-apply-map.json`, verificador, workflows, `src/**`, `supabase/**` **intocados**.

---

## 2. Contrato executável

- ESM puro (`.mjs`), somente built-ins `node:fs`, `node:path`, `node:process`.
- Exports: `SCHEMA_VERSION`, `APPLY_MAP_PATH`, `loadApplyMap`, `default`.
- `SCHEMA_VERSION === "v2026-07-12.4"`.
- `APPLY_MAP_PATH === ".github/sec002-apply-map.json"`.
- Retorno: `Object.freeze(data)` se presente e válido; `null` se ENOENT (modo soft, permite fallback do `verifyPC3` via `loader.SCHEMA_VERSION`).
- Erros com `code` estável: `E_APPLY_MAP_UNREADABLE`, `E_APPLY_MAP_JSON`, `E_APPLY_MAP_SCHEMA`.

## 3. Errata leve ao spec R110

R110 previa `E_APPLY_MAP_MISSING` para ausência. R111 substitui por comportamento **soft-null** quando `ENOENT`, mantendo `E_APPLY_MAP_UNREADABLE` para outros erros de I/O. Justificativa: o `verifyPC3` já possui fallback `loader.SCHEMA_VERSION` quando `map == null` — o soft-null permite PC3 avançar mesmo antes do bootstrap físico de `.github/sec002-apply-map.json`, sem violar L5/L6 (leitura restrita, zero escrita). Invariantes L1–L10 permanecem satisfeitas.

## 4. Verificação executada

```
$ node scripts/sec002/verify-wave-2-preconditions.mjs --dry-run --only=PC3
FAIL E_WAVE2_PC3 bump de apply-map sem par spec/materialize: SEC-002.apply-map-01.consumers-01
```

Resultado esperado: o loader passou nos requisitos (1)–(3) do `verifyPC3`. A falha atual é cláusula (4) — **pair-debt documental** de bumps antigos, não do loader:

| Stem | Presente | Faltando |
|------|----------|----------|
| `SEC-002.apply-map-01.consumers-01` | spec-01 | materialize-01 |
| `SEC-002.apply-map-01.parser-bump-01` | spec-01 | materialize-01 |
| `SEC-002.apply-map-01.evidence-validator-bump-01` | materialize-01 | spec-01 |

Fechamento dessas paridades é responsabilidade de R112 (spec) + R113 (materialize documental), sem tocar produção.

## 5. Estado após R111

- Loader materializado e read-only.
- `E_WAVE2_PC3` avança para modo *pair-debt only* — nenhuma dependência de código pendente.
- Onda 2 permanece **bloqueada** por `E_WAVE2_PC3` (pair-debt) + `E_WAVE2_PC4..PC7`.

## 6. Impacto em produção

**Nenhum.** Loader é utilitário de governança consumido apenas por validadores/CI.
