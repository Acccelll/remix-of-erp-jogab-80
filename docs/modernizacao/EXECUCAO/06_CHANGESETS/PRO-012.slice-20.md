# PRO-012 · slice-20 — Top 3 motivos ignorados no card agregado

## Escopo
Adicionar, no card "Ignoradas" do painel "Qualidade das importações XLSX", um
resumo compacto com os 3 principais motivos + contador, servindo como atalho
para o drilldown já existente.

## Alterações
- `src/pages/suprimentos/ContagensCiclicas.tsx`
  - Após o contador de motivos, renderiza `slice(0, 3)` de
    `metricasImp.ignoradasPorMotivo` como badges clicáveis.
  - Cada badge alterna `motivoDrill` (mesma origem/estado do drilldown atual),
    com `aria-pressed` refletindo seleção ativa.
  - Badge `+N` quando há mais motivos além do top 3.

## Contrato de execução
- UI-only, reaproveita métricas e estado já existentes. Sem Cloud, RLS, schema
  ou nova dependência.

## Validação
- `bunx tsgo --noEmit` sem erros novos.
- Manual: com importações que geram >3 motivos, o card mostra top 3 + `+N`;
  clique alterna drilldown; estado visual sincronizado com barra completa.

## Próximo passo sugerido
`PRO-012.slice-21` — barra de tendência (últimas N importações) de linhas
ignoradas por motivo, com sparkline no mesmo card agregado.
