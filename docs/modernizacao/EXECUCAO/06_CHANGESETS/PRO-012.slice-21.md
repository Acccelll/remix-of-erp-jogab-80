# PRO-012 · slice-21 — Sparkline de tendência por motivo (top 3)

## Escopo
Adicionar sparkline SVG minimalista ao lado dos badges do top 3 motivos
(slice-20), mostrando a tendência das últimas 10 importações filtradas.

## Alterações
- `src/components/ui/sparkline.tsx` (novo): componente SVG puro, sem
  dependências, com `role="img"`, `aria-label`, ponto final destacado e
  fallback "—" para série vazia.
- `src/pages/suprimentos/ContagensCiclicas.tsx`
  - `tendenciasPorMotivo` (`useMemo`): coleta todas as auditorias
    (`rowsImpFiltrados`), ordena por `importadoEm`, pega as últimas 10 e
    devolve `Map<motivo, number[]>` restrito ao top 3 atual.
  - Renderização do top 3: cada linha combina badge clicável + `<Sparkline>`;
    legenda "últimas N importação(ões)" ao pé.

## Contrato de execução
- UI-only, sem Cloud, RLS ou schemas. SVG inline, sem novas libs.

## Validação
- `bunx tsgo --noEmit` sem erros.
- Manual: com múltiplas importações, sparkline responde ao filtro de local
  e ao conjunto ativo de motivos.

## Próximo passo sugerido
`PRO-012.slice-22` — coluna de tendência de ignoradas por contagem na tabela
principal (mesma primitiva `<Sparkline>`), permitindo ranking por deterioração.
