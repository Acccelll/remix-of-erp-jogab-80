# PRO-022 · slice-02 — Popover "Capacidade" no header do Board

## Contexto

Continuação da slice-01 (regra pura `calcularCapacidade`). Expõe o
resumo no header do Board sem invadir o espaço horizontal das colunas,
via popover ancorado no botão "Capacidade" (com contador de alocados).

## Entrega

- `src/pages/Board.tsx` — importa `calcularCapacidade`; novo botão
  **Capacidade** no header abre um `Popover` com:
  - Totais: ativos · alocados · sem obra · especiais.
  - Lista por obra ordenada por total, com quebra por função em linha
    secundária.
  - Scroll até `60vh` para frotas grandes.

## Não escopo

- Sem cálculo de demanda planejada (ainda). Ficará para slice-03
  quando integrarmos com cronograma/lookahead.
- Sem drill-down para a coluna correspondente (link).
- Sem exportação (basta ler o painel).

## Validação

- `bunx tsgo --noEmit` — OK.
