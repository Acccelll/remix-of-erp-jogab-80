# ARC-010.slice-05 — Migração final `obrasRepo.listResumo*` fora de `planejamento/`

## Escopo
Remover as últimas chamadas de `obrasRepo.listResumo()` em runtime (fora de testes) e migrar para `useObras()`.

## Alterações
- `src/pages/GMFeatureFlags.tsx`: `useQuery(["gm-feature-flags-obras"])` substituído por `useObras()`. Consumo via `obrasQuery.data?.find/map` mantido (mesmo shape).
- `src/components/cards/ImportarTrelloDialog.tsx`: `useQuery(["obras-min"])` substituído por `useObras({ enabled: open })` + `useMemo` mapeando `{id, nome}`. Import `obrasRepo` removido.
- `src/pages/InspecaoCaptura.tsx`: `useQuery(["obras-min"])` substituído por `useObras()` + `useMemo` `{id, nome}`.

## Validação
- `tsgo --noEmit` limpo.
- `rg "obrasRepo.listResumo" src/ | grep -v __tests__` → sem ocorrências.
- Testes de contrato do repositório permanecem intactos (única referência remanescente e legítima).

## Próximo
ARC-010.slice-06 — depreciar publicamente `obrasRepo.listResumo*` no repositório (marcar `@deprecated`, apontar `useObras()`); avaliar remoção após auditoria dos testes de contrato.
