# ARC-010.slice-04 — Migração de obras em `planejamento/*` (batch final)

## Escopo
Remover `obrasRepo.listResumo()` / `listResumoComCodigoNome()` das últimas três páginas de planejamento e passar a consumir `useObras()` como fonte única, alinhando ao padrão consolidado nas slices 01–03.

## Alterações
- `src/pages/planejamento/DashboardLean.tsx`: `obrasRepo.listResumo()` removido do `Promise.all`; obras derivadas de `useObras().data` (mapeadas para `{id, nome}`). `isLoading` combina query local + `obrasQuery.isLoading`. `obraNome` passa a ler do array derivado.
- `src/pages/planejamento/RdoConsolidado.tsx`: `useQuery(["rdo-consol-obras"])` substituído por `useObras()` + `useMemo` (`Obra[] = {id, nome}`). Zero fetch local de obras.
- `src/pages/planejamento/ChecklistImport.tsx`: `useQuery(["checklist-import-obras"])` (que chamava `obrasRepo.listResumoComCodigoNome()`) substituído por `useObras()` + `useMemo` mapeando para `{id, nome, codigo}`. `resolveObraId` continua funcionando (mesmo shape `Obra`).

## Validação
- `tsgo --noEmit` limpo.
- `rg "obrasRepo" src/pages/planejamento` → sem ocorrências.

## Próximo
ARC-010.slice-05 — varrer demais módulos (`suprimentos` finais, `financeiro`, `dp`) em busca de resquícios de `obrasRepo.listResumo*` fora do domínio de obras.
