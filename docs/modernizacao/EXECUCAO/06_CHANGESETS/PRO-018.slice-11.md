# PRO-018 · slice-11 — Diff campo-a-campo com valores lado a lado

**Onda:** 07 · **Achado:** PRO-018 · **Data:** 2026-07-14

## Entrega
- `src/lib/bms/layoutPerfis.ts`: nova função pura `detalharDiffPerfis(atual,
  importado)` que retorna `BmsLayoutDiffLinha[]` (`campo`, `atual`,
  `importado`) usando `diffPerfis` e serializando cada campo de forma
  determinística (strings vazias para `cliente` ausente).
- `src/lib/bms/__tests__/layoutPerfis.test.ts`: teste cobre nome,
  `headerRow` e chave de `colunas`, garantindo ordem estável.
- `src/components/import/BmsImporter.tsx`: o preview de importação passa a
  exibir, para cada perfil em `conflito`, uma tabela `Campo · Atual ·
  Importado` (atual em vermelho, importado em verde) substituindo a
  listagem enxuta de campos.

## Regras respeitadas
- Contrato aditivo: nova função pura; `diffPerfis` e API existentes intactos.
- UI puramente informativa; a decisão continua manual (Importar/Preservar).

## Não escopo
- Merge parcial campo-a-campo (aplicar apenas subconjunto).
- Diff visual para perfis novos ou idênticos (só faz sentido em conflito).
