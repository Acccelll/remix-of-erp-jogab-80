# ARC-013.slice-01 — Mapeamento e escolha do próximo domínio (`requisicoesRepo`)

**Onda:** 6 · **Achado:** ARC-013 · **Slice:** 01

## Ranking de reincidência (`grep -rn <repo> src/` fora de testes/hooks/próprio arquivo)

| Repo               | Consumidores runtime |
| ------------------ | -------------------- |
| `suprimentosRepo`  | 60                   |
| `cronogramaRepo`   | 41                   |
| `cardsRepo`        | 26                   |
| `financeiroRepo`   | 24                   |
| `medicoesRepo`     | 14                   |
| `boardsRepo`       | 14                   |
| `pontoRepo`        | 11                   |
| `requisicoesRepo`  | 10 (5 arquivos)      |
| `rdoRepo`          | 5                    |
| `frotasRepo`       | 0                    |
| `dpRepo`           | 0                    |

## Decisão
Priorizar **`requisicoesRepo`** no próximo slice — API pequena (5 métodos: `listAll`, `listResumoObra`, `create`, `updateStatus`, `update`), consumidores runtime bem delimitados:

- `src/pages/planejamento/Restricoes.tsx` → `listResumoObra`
- `src/pages/suprimentos/Cotacoes.tsx` → `updateStatus`
- `src/pages/suprimentos/OrdensCompra.tsx` → leitura (`listAll` com colunas)
- `src/pages/suprimentos/Requisicoes.tsx` → CRUD principal (`listAll`, `create`, `updateStatus`)

Os repositórios maiores (`suprimentosRepo`, `cronogramaRepo`, `cardsRepo`) exigirão múltiplos hooks e ondas dedicadas — serão tratados após ARC-013 fechar `requisicoes` como piloto pequeno.

## Alteração de infraestrutura
- **`src/lib/query-keys.ts`**: registra o root `"requisicoes"` (ordem alfabética entre `receb` e `responsabilidades`).

## Validação
- `tsgo --noEmit`: limpo.

## Próximo passo
`ARC-013.slice-02` — criar `src/hooks/useRequisicoes.ts` (`useRequisicoesAll`, `useRequisicoesResumoObra`, `useCreateRequisicao`, `useUpdateRequisicaoStatus`, `useUpdateRequisicao`) encapsulando `requisicoesRepo`.
