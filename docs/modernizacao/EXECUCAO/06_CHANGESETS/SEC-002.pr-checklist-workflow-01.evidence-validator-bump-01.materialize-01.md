# SEC-002.pr-checklist-workflow-01.evidence-validator-bump-01.materialize-01 — Materialização canônica do `sec002-pr-checklist.yml` para o apply-map `v2026-07-12.4`

**Onda:** 2 (bloqueada — depende de `SEC-001` fechar e da Onda 2 ser formalmente aberta)
**Data:** 2026-07-12
**Depende de:**
- `SEC-002.pr-checklist-workflow-01.materialize-01` (R50) — workflow final com 11 gates operacionais;
- `SEC-002.pr-checklist-workflow-01.parser-bump-01.materialize-01` (R57) — workflow alinhado a `v2026-07-12.3`;
- `SEC-002.evidence-index-01.materialize-01` (R59) — índice canônico de evidências;
- `SEC-002.validate-evidence-index-01.materialize-01` (R61) — validador `scripts/sec002/validate-evidence-index.mjs`;
- `SEC-002.apply-map-01.evidence-validator-bump-01.materialize-01` (R63) — apply-map `v2026-07-12.4` com M17–M19;
- `SEC-002.freeze-guard-01.evidence-validator-bump-01.materialize-01` (R65) — freeze-guard com F12–F14;
- `SEC-002.pr-checklist-workflow-01.evidence-validator-bump-01.spec-01` (R66) — contrato canônico desta materialização.

**Escopo:** materializar, em conteúdo canônico versionado neste documento, o workflow `.github/workflows/sec002-pr-checklist.yml` alinhado ao apply-map `v2026-07-12.4`, preservando literalmente os 11 gates operacionais e adicionando o gate final `validate-evidence-index`. **Nenhum arquivo de produção é gravado nesta etapa.** Gravação física permanece bloqueada até a abertura formal da Onda 2.

## 1. Invariantes cobertas

| Regra | Descrição executável |
|---|---|
| W1–W11 | herdadas de R50: 11 gates operacionais, ordem canônica, read-only permissions, sem secrets e sem rede externa |
| W12–W14 | herdadas de R57: `parse-apply-id` como pré-gate único e sem reparse local de `APPLY_ID` |
| W15 | comentário de topo declara `apply-map: v2026-07-12.4` |
| W16 | `validate-evidence-index` é o último passo do job e executa exatamente `node scripts/sec002/validate-evidence-index.mjs` |
| W17 | nenhum dos 11 gates operacionais é reordenado, removido, renomeado ou tem `run:` alterado |

## 2. Artefato — `.github/workflows/sec002-pr-checklist.yml`

**Caminho:** `.github/workflows/sec002-pr-checklist.yml`
**Encoding:** UTF-8, LF, sem BOM.

```yaml
# sec002-pr-checklist
# apply-map: v2026-07-12.4
# consumers: parse-apply-id (parser) + 11 gates + validate-evidence-index (evidence)
name: sec002-pr-checklist
on:
  pull_request:
    paths:
      - "**"

concurrency:
  group: sec002-pr-checklist-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  checklist:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      PR_TITLE: ${{ github.event.pull_request.title }}
      PR_BODY: ${{ github.event.pull_request.body }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: parse-apply-id  # consumer id="parse-apply-id" category="parser"
        run: |
          APPLY_ID="$(node scripts/sec002/parse-apply-id.mjs)"
          echo "APPLY_ID=${APPLY_ID}" >> "$GITHUB_ENV"

      - name: validate-apply-map
        run: node scripts/sec002/validate-apply-map.mjs

      - name: validate-pr-checklist
        run: node scripts/sec002/validate-pr-checklist.mjs

      - name: validate-changed-files
        run: node scripts/sec002/validate-changed-files.mjs

      - name: validate-staging-parity
        run: node scripts/sec002/validate-staging-parity.mjs

      - name: validate-go-no-go
        run: node scripts/sec002/validate-go-no-go.mjs

      - name: validate-comms-log
        run: node scripts/sec002/validate-comms-log.mjs

      - name: validate-dri-matrix
        run: node scripts/sec002/validate-dri-matrix.mjs

      - name: validate-acceptance-gates
        run: node scripts/sec002/validate-acceptance-gates.mjs

      - name: validate-evidence-archive
        run: node scripts/sec002/validate-evidence-archive.mjs

      - name: sec002-consumers-guard
        run: node scripts/sec002/check-consumers.mjs

      - name: sec002-freeze-guard
        run: node scripts/sec002/freeze-guard.mjs

      - name: sec-002-tests
        run: node scripts/sec002/run-tests.mjs

      - name: validate-evidence-index  # consumer id="validate-evidence-index" category="evidence"
        run: node scripts/sec002/validate-evidence-index.mjs
```

Contratos:

- Único pré-gate `parse-apply-id`; nenhum outro step reinterpreta título/corpo do PR.
- `validate-apply-map` continua sendo o gate lógico inicial e passa a validar M17–M19 via R63.
- Os 11 gates operacionais permanecem os mesmos de R57/R50, na mesma ordem e com os mesmos comandos.
- `validate-evidence-index` é aditivo, final e sem flags, `env`, `working-directory` ou permissões extras.
- O `summary` de R57 não é emitido nesta materialização porque W16 reserva a última posição para o gate de evidência e evita sinalizar sucesso antes do último validador.

## 3. Ordem final congelada

| Ordem | Tipo | Step | Comando |
|---:|---|---|---|
| 0 | pré-gate | `parse-apply-id` | `node scripts/sec002/parse-apply-id.mjs` |
| 1 | apply-map | `validate-apply-map` | `node scripts/sec002/validate-apply-map.mjs` |
| 2 | gate operacional | `validate-pr-checklist` | `node scripts/sec002/validate-pr-checklist.mjs` |
| 3 | gate operacional | `validate-changed-files` | `node scripts/sec002/validate-changed-files.mjs` |
| 4 | gate operacional | `validate-staging-parity` | `node scripts/sec002/validate-staging-parity.mjs` |
| 5 | gate operacional | `validate-go-no-go` | `node scripts/sec002/validate-go-no-go.mjs` |
| 6 | gate operacional | `validate-comms-log` | `node scripts/sec002/validate-comms-log.mjs` |
| 7 | gate operacional | `validate-dri-matrix` | `node scripts/sec002/validate-dri-matrix.mjs` |
| 8 | gate operacional | `validate-acceptance-gates` | `node scripts/sec002/validate-acceptance-gates.mjs` |
| 9 | gate operacional | `validate-evidence-archive` | `node scripts/sec002/validate-evidence-archive.mjs` |
| 10 | gate operacional | `sec002-consumers-guard` | `node scripts/sec002/check-consumers.mjs` |
| 11 | gate operacional | `sec002-freeze-guard` | `node scripts/sec002/freeze-guard.mjs` |
| 12 | gate operacional | `sec-002-tests` | `node scripts/sec002/run-tests.mjs` |
| 13 | gate de evidência | `validate-evidence-index` | `node scripts/sec002/validate-evidence-index.mjs` |

Contagem auditável: `1 pré-gate + 1 validate-apply-map + 11 gates operacionais + 1 gate de evidência`.

## 4. Compatibilidade

| Componente | Efeito |
|---|---|
| Apply-map `v2026-07-12.4` (R63) | referenciado no cabeçalho e validado por `validate-apply-map` |
| `validate-evidence-index.mjs` (R61) | passa a bloquear merge se `INDEX.md` divergir de E1–E8 |
| `sec002-consumers-guard` | reconhece `validate-evidence-index` como consumidor `evidence` |
| `sec002-freeze-guard` (R65) | protege o validador, sua evidência e `INDEX.md` com F12–F14 |
| Workflows paralelos | `sec002-freeze-guard.yml` permanece independente; este changeset só versiona o checklist |

## 5. Procedimento de gravação futura

1. Substituir `.github/workflows/sec002-pr-checklist.yml` pelo conteúdo de §2.
2. Confirmar que `scripts/sec002/validate-evidence-index.mjs` já existe conforme R61.
3. Rodar localmente:

```bash
node scripts/sec002/validate-apply-map.mjs
node scripts/sec002/check-consumers.mjs
node scripts/sec002/validate-evidence-index.mjs
```

Saída esperada:

```text
[sec002/validate-apply-map] OK
[sec002/consumers-guard] OK
[sec002/validate-evidence-index] OK
```

## 6. Evidência obrigatória

`.github/sec002-evidence/pr-checklist-workflow-evidence-validator-bump-01/manifest.json`:

```json
{
  "sec002_apply_id": "pr-checklist-workflow-evidence-validator-bump-01",
  "schema_version": "v2026-07-12.4",
  "source": "docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.pr-checklist-workflow-01.evidence-validator-bump-01.materialize-01.md",
  "changed_files": [
    ".github/workflows/sec002-pr-checklist.yml"
  ],
  "checks": [
    "node scripts/sec002/validate-apply-map.mjs",
    "node scripts/sec002/check-consumers.mjs",
    "node scripts/sec002/validate-evidence-index.mjs"
  ],
  "hashes": {
    ".github/workflows/sec002-pr-checklist.yml": "<sha256>"
  },
  "refs": ["R50", "R57", "R59", "R61", "R63", "R65", "R66", "R67"]
}
```

## 7. Critérios de rejeição futura

- Comentário de topo divergente de `v2026-07-12.4`.
- `validate-evidence-index` ausente, fora da última posição, com flags ou com caminho diferente.
- Qualquer um dos 11 gates operacionais removido, renomeado, reordenado ou com `run:` alterado.
- Introdução de `secrets`, `permissions: write-*`, rede externa, `working-directory` ou `env` adicional no novo gate.
- Gravação física antes de R61/R63/R65 estarem materializados.
- Alteração em `src/**`, `supabase/functions/**` ou `supabase/migrations/**`.

## 8. Impacto em produção

**Nenhum.** Documento de materialização canônica. Não grava workflow executável nesta etapa, não altera dados, não aplica migração, não muda função, não muda frontend e não altera política RLS.