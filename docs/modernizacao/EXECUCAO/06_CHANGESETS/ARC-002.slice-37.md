# ARC-002.slice-37 · Onda 6 — Extração de `veiculos` para `VeiculosStateProvider`

## Escopo
- Novo `src/contexts/veiculos/VeiculosStateContext.tsx`: domicilia `useVeiculosState` + query `useVeiculos` com `useSyncQueryToState`; expõe `veiculos`, `setVeiculos`, CRUDs, `veiculosLoaded`, `getVeiculos` (getter estável). Depende de `useColaboradoresStateContext` para `getColaboradores`.
- `AppContext.tsx`: consome `useVeiculosStateContext()`; usa `getVeiculos` como `getVeiculosStable` para `useMobilizacoes`. `dataLoaded` compõe `veiculosLoaded`.
- `useAppQueries.ts`: remove `useVeiculos`/`setVeiculos` (agora no provider).
- `App.tsx`: `<VeiculosStateProvider>` promovido para acima do `<AppProvider>` (dentro de `<PatrimoniosStateProvider>`).

## Validação
- `tsgo --noEmit`: limpo.
- Consumidores do `VeiculosProvider` (fachada) e `useApp().veiculos/...` continuam funcionando via bridge.

## Próximo
`ARC-002.slice-38` — mesma extração para `contratos` (`useContratosState` + query `useContratos`).
