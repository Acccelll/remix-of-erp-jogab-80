# SEC-002.wave-2-formal-open-01.pc4-scope-01.verifier-patch-01.materialize-01 — Materialização do patch PC4

**Onda:** 2 (bloqueada — PC1/PC2/PC3/PC4 OK após esta rodada; PC5–PC7 pendentes)  
**Rodada:** R116 (E4 — materialização do patch do verificador)  
**Standard:** E5  
**Precedente autorizante:** `SEC-002.wave-2-formal-open-01.pc4-scope-01.verifier-patch-01.spec-01.md` (R115).  
**Escopo:** aplicar o patch mínimo em `scripts/sec002/verify-wave-2-preconditions.mjs` para satisfazer `E_WAVE2_PC4`, preservando `INDEX.md`, apply-map, workflows e produção.

---

## 1. Write set

| Arquivo | Ação |
|---------|------|
| `scripts/sec002/verify-wave-2-preconditions.mjs` | editado |
| `.github/sec002-evidence/wave-2-formal-open-01-pc4-verifier-patch-01/manifest.json` | criado |
| `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.wave-2-formal-open-01.pc4-scope-01.verifier-patch-01.materialize-01.md` | criado |
| `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-001.README.md` | editado |

Nada mais foi tocado. `src/**`, `supabase/**`, `.github/workflows/**`, `.github/sec002-apply-map.json`, `.github/sec002-evidence/INDEX.md`, `.github/sec002-readiness/**` e dados de produção permanecem intocados.

---

## 2. Patch aplicado

O verificador passou a declarar a constante top-level:

```javascript
const EXPECTED_INDEX_ROWS = 29;
```

`verifyPC4()` deixou de usar o literal `26` na validação de contagem e no laço de continuidade, passando a usar `EXPECTED_INDEX_ROWS` em ambos os pontos.

O contexto de erro de contagem agora inclui `expected: EXPECTED_INDEX_ROWS`, mantendo `path` e `rows`.

---

## 3. Verificação executada

```bash
node scripts/sec002/verify-wave-2-preconditions.mjs --dry-run --only=PC4
```

Saída esperada e observada:

```text
OK verify-wave-2-preconditions v2026-07-12.4 PC1..PC7
```

Resultado: `E_WAVE2_PC4 = OK` no dry-run isolado.

---

## 4. Invariantes PC4P1–PC4P12

| ID | Resultado |
|----|-----------|
| PC4P1 | OK — o único arquivo executável alterado foi `scripts/sec002/verify-wave-2-preconditions.mjs`. |
| PC4P2 | OK — `EXPECTED_INDEX_ROWS` é constante top-level, estática e igual a `29`. |
| PC4P3 | OK — `29` reflete as linhas úteis auditadas do `INDEX.md` após R99/R103/R107. |
| PC4P4 | OK — `verifyPC4()` usa a constante na contagem e no laço `1..N`. |
| PC4P5 | OK — a mensagem de erro interpola `EXPECTED_INDEX_ROWS`. |
| PC4P6 | OK — o contexto inclui `{ path, rows, expected }`. |
| PC4P7 | OK — nenhuma inferência dinâmica por filesystem, glob, git, cache ou manifests foi introduzida. |
| PC4P8 | OK — `.github/sec002-evidence/INDEX.md` permaneceu intocado. |
| PC4P9 | OK — PC1/PC2/PC3/PC5/PC6/PC7 e CLI permanecem semanticamente inalterados. |
| PC4P10 | OK — sem bump de apply-map, sem ajuste de `SCHEMA_VERSION` e sem alteração em workflows. |
| PC4P11 | OK — dry-run isolado de PC4 validado. |
| PC4P12 | OK — Onda 2 permanece bloqueada por PC5–PC7. |

---

## 5. Estado após R116

- `E_WAVE2_PC1 = OK`.
- `E_WAVE2_PC2 = OK`.
- `E_WAVE2_PC3 = OK`.
- `E_WAVE2_PC4 = OK`.
- `E_WAVE2_PC5..PC7` permanecem pendentes.
- Onda 2 permanece **bloqueada** até fechamento explícito de PC5, PC6 e PC7.

---

## 6. Impacto em produção

**Nenhum.** Patch restrito ao verificador documental de pré-condições. Não altera app, banco, autenticação, funções, permissões, dados, workflows, apply-map ou índice de evidências.