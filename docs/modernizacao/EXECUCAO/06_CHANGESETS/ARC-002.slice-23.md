# ARC-002 · slice-23

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Escopo
1. `src/hooks/useObrasEscopo.ts` deixou de depender diretamente de `useApp()` e passa a consumir `useObrasContext()` como fonte de obras.
2. `src/contexts/app/useAppRead.ts` removido após auditoria confirmar zero consumidores fora da própria definição.
3. Mantido o padrão incremental: contextos de domínio ainda funcionam como fachadas de compatibilidade sobre `AppContext`, sem alteração de contrato público das telas.

## Validação
- `rg "useAppRead\\(" src`: zero usos.
- `rg "useApp\\(" src --glob '!src/contexts/**' --glob '!src/hooks/useObrasEscopo.ts'`: zero consumidores de tela/hook direto.
- `tsgo --noEmit`: sem erros.

## Próximo passo
`ARC-002.slice-24` — revisar fachadas de domínio que ainda apenas repassam dados do `AppContext` e escolher o próximo domínio seguro para domiciliar estado real ou reduzir tipos dependentes de `ReturnType<typeof useApp>`.