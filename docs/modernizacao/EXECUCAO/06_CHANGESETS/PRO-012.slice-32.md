# PRO-012 · slice-32 — Extração do componente TrendSummary

**Onda:** 07 · Suprimentos & Contagens Cíclicas
**Contrato:** UI-only, sem alteração de comportamento em `ContagensCiclicas`.

## Escopo
Extrair o resumo de tendência (piora / estáveis / melhora) para um componente próprio, reutilizável em outras listas com histórico.

## Alterações
- `src/components/suprimentos/TrendSummary.tsx` (novo):
  - Props: `piora`, `estavel`, `melhora`, `label?`, `totalLabel?`, `onTogglePiora?`, `piorAtiva?`, `className?`.
  - Renderiza `null` quando o total é zero.
  - Chip "em piora" vira `<button>` toggle quando `onTogglePiora` é fornecido (reflete `aria-pressed={piorAtiva}`); caso contrário, é apenas informativo.
  - Estilo/tokens iguais ao bloco original (destructive / muted / emerald).
- `src/pages/suprimentos/ContagensCiclicas.tsx`:
  - Import de `TrendSummary`.
  - Substituído o bloco inline de 38 linhas por uma única invocação do componente.

## Validação
- `bunx tsgo --noEmit` sem erros.
- Sem mudança visível: markup, tokens e comportamento equivalentes ao slice-31.

## Próximo passo
`PRO-012.slice-33` — testes unitários (`vitest`) para `TrendSummary` cobrindo: render vazio, toggle ativo/inativo, `disabled` quando `piora === 0`.
