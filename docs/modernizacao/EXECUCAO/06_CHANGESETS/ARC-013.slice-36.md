# ARC-013 · slice-36 — `RevisoesTab` sem imports diretos de `@/lib/repositories/cronograma`

## Alterações
1. **`src/hooks/useCronograma.ts`**: adiciona passthroughs completos por spread — `cronogramaFns`, `cronogramaRevisoesFns`, `cronogramaItemRevisoesFns`, `cronogramaBaselinesFns`, `cronogramaDependenciasFns`. Todos os métodos dos repos são arrow-style sem uso de `this`, então o spread preserva a semântica.
2. **`src/components/obra-detalhe/RevisoesTab.tsx`**: import `@/lib/repositories/cronograma` substituído por `@/hooks/useCronograma`, mantendo os aliases (`as cronogramaRepo` etc.) para não tocar nas 15+ ocorrências no corpo do componente.

## Escopo intencional
`CronogramaImporter`, `CronogramaSemanalImporter` e `recalcular-cpm.ts` ainda importam do repo — próximas slices. `RevisoesTab` é a última consumidora de `cronogramaBaselinesRepo`/`cronogramaDependenciasRepo` fora dos importadores/orquestrador.

## Validação
- `bunx tsgo --noEmit`: limpo.
- `rg 'from "@/lib/repositories/cronograma"' src/components/obra-detalhe/RevisoesTab.tsx`: sem ocorrências.
