# ARC-003.c.3 — `ocorrencias`, `obra_localizacoes`, `aditivos_contrato`, `bms_previstas`, `cronograma_marcos`, `rdo_efetivo`, `rdo_ocorrencias`, `import_validation_runs`

## Escopo
Centralizar 8 tabelas ligadas à obra e ao RDO consolidado num novo `src/lib/repositories/obraDetalhe.ts`.

## Novo repositório
`obraDetalhe.ts` expõe: `ocorrenciasRepo`, `obraLocalizacoesRepo`, `aditivosContratoRepo`, `bmsPrevistasRepo` (inclui `deletePrevistasPorObra` e `upsertPorObraNumero`), `cronogramaMarcosRepo` (com `updateRealizado`), `rdoEfetivoRepo`, `rdoOcorrenciasRepo`, `importValidationRunsRepo`.

## Call-sites refatorados
- `OcorrenciasTab.tsx` — upsert/remover.
- `LinhaBalancoTab.tsx` — 3 mutations.
- `DesempenhoTab.tsx` — leitura marcos.
- `MarcosTab.tsx` — 3 mutations (inclusive marcar realizado).
- `AditivosTab.tsx` — leitura + insert.
- `NovaBmsPrevistaManualDialog.tsx` — insert manual.
- `PrevisaoTab.tsx` — delete previstas + upsert onConflict.
- `FinFluxo.tsx` — leitura bms com relação `obras`.
- `RdoConsolidado.tsx` — leituras `rdo_efetivo` / `rdo_ocorrencias`.
- `ChecklistImport.tsx` — insert em `import_validation_runs`.

## Verificação
- `rg` das 8 tabelas em `src/` retorna apenas ocorrências dentro do próprio `obraDetalhe.ts`.
- `tsgo --noEmit` verde.

## Pendente ARC-003.c.4
`cards/*` (board_*, card_*), `cronograma_itens` (call-sites fora do repo), `cronograma_item_revisoes`, `cronograma_cenarios`, `cronograma_cenario_itens`.
