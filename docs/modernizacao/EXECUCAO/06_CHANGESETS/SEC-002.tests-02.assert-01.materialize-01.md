# SEC-002.tests-02.assert-01.materialize-01 — Helpers assert_rls_* em _lib/assert.sql

**Onda:** 2 (OPEN)
**Ref:** R190 · **Round:** compact-01
**Data:** 2026-07-12
**Escopo:** materializa os 4 helpers `pg_temp.assert_rls_{select,insert,update,delete}` definidos em `SEC-002.tests-02.assert-01.spec-01` (R186), acrescentados ao final de `supabase/tests/sec-002/_lib/assert.sql` **sem alterar bytes existentes** (`assert_bool`/`assert_rows`/`assert_denied` preservados).

## 1. Write set físico (2 arquivos)

| # | Caminho | Ação |
|---|---------|------|
| 1 | `supabase/tests/sec-002/_lib/assert.sql` | append de 4 funções + cabeçalho de rastreio |
| 2 | `.github/sec002-evidence/tests-02-assert-01/manifest.json` | manifest de aplicação (habilita PC8/PC9 quando materializados) |

## 2. Contrato executado

- `create or replace function pg_temp.assert_rls_select(text,text,text)` — SELECT com `execute format('select count(*) from (%s) q', _sql)`, oráculo `{allow,empty,deny}`, tratamento de `insufficient_privilege`.
- `create or replace function pg_temp.assert_rls_insert(text,text,text)` — INSERT via `execute _sql` + `get diagnostics row_count`, rejeita `empty` como `E_ASSERT_EXPECT_INVALID`.
- `create or replace function pg_temp.assert_rls_update(text,text,text)` — idem, para UPDATE.
- `create or replace function pg_temp.assert_rls_delete(text,text,text)` — idem, para DELETE.

Oráculo final via `pass()`/`fail()` do pgTAP. Exceções tratadas: `insufficient_privilege`, `check_violation`.

## 3. Invariantes verificadas (ARM01-1…ARM01-6)

| ID | Verificação | Resultado |
|----|------------|-----------|
| ARM01-1 | `rg -c "assert_rls_(select\|insert\|update\|delete)"` = 4 | ✅ |
| ARM01-2 | `rg -c "security definer" _lib/assert.sql` só match em comentário (0 em corpo de função) | ✅ |
| ARM01-3 | Bytes anteriores preservados (assert_bool/assert_rows/assert_denied intactos) | ✅ |
| ARM01-4 | Nenhum DDL fora de `create or replace function pg_temp.*` | ✅ |
| ARM01-5 | Nenhum toque em `role`, `search_path`, `request.jwt.claims`, schemas protegidos | ✅ |
| ARM01-6 | `node verify-wave-2-preconditions.mjs --json` = `{ ok: true }` PC1..PC7 | ✅ |

SHA256 pós-materialize: `1ac43aa94bbfd36bf4e3888f4279952caddc55fd9b627e378a4adefbdb9d0db5`.

## 4. Habilitação de gates a jusante

O manifest `.github/sec002-evidence/tests-02-assert-01/manifest.json` com `applied:true` é pré-condição de:

- R187 (`gen-tests-02.mjs` materialize) — depende de asserts existirem para gerar `\i _lib/assert.sql`.
- R188 (`run-sec-002-tests.sh` materialize) — assume asserts presentes.
- R189 (PC8/PC9) — checkPC8/checkPC9 dependem dos artefatos gerados por R187/R188.

## 5. Não tocado

`_lib/fixtures.sql`, `_lib/jwt.sql`, `_lib/teardown.sql`, `batch-*/`, `helpers/`, `scripts/sec002/*`, `.github/sec002-evidence/INDEX.md`, `supabase/migrations/`, `src/`, `supabase/functions/`, apply-map, workflows, banco.

## 6. Impacto em produção

**Nenhum.** `_lib/assert.sql` só é lido por arquivos pgTAP dentro de `supabase/tests/`, jamais aplicado ao banco de produção. PC1..PC7 permanecem verdes; `INDEX.md`=44 linhas; `EXPECTED_INDEX_ROWS=44`.
