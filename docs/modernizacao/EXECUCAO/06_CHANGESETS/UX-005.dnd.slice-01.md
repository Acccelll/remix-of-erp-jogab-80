# UX-005 · dnd.slice-01 — Suporte a teclado e SR em DndContext

## Escopo
Endereçar a metade "DnD sem teclado" do Achado UX-005 nos 4 usos de dnd-kit do projeto.

## Alterações
1. Novo módulo `src/lib/a11y/dndAnnouncements.ts` com:
   - `dndAnnouncementsPtBR(getItemLabel?)`: `Announcements` pt-BR para `onDragStart/Over/End/Cancel`.
   - `dndScreenReaderInstructionsPtBR`: instruções pt-BR para SR.
2. Aplicação em cada `DndContext` via `accessibility={{ announcements, screenReaderInstructions }}`:
   - `src/pages/QuadroBoard.tsx` — inclui `sortableKeyboardCoordinates` no `KeyboardSensor` (usa `@dnd-kit/sortable`).
   - `src/components/board/AllocationBoard.tsx`.
   - `src/pages/planejamento/Lookahead.tsx`.
   - `src/pages/planejamento/PacotesTrabalho.tsx`.

## Validação
- `tsgo --noEmit`: limpo.
- `rg "accessibility={" src`: 4 hits (todos os DndContext do projeto).
- `rg "sortableKeyboardCoordinates" src`: aplicado onde `SortableContext` é usado (`QuadroBoard`).

## Estado UX-005
Encerrado. Ambas as partes do Achado (nomes acessíveis em botões icon-only + DnD com teclado/SR) cobertas.
