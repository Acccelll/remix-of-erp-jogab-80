# ARC-002.slice-06.close — Encerramento do slice de CRM

**Onda:** 6 · **Prioridade:** P1 · **Tipo:** REF/MIGRAÇÃO
**Achado:** ARC-002 (god-context)

## Consumers migrados

- `src/pages/Clientes.tsx` — `clientes/addCliente/updateCliente/deleteCliente/oportunidades` via `useClientesContext()`.
- `src/pages/crm/OportunidadePerfil.tsx` — `oportunidades/funilEstagios` via `useClientesContext()`.

Agregadores atualizados para servir CRM a partir do subcontexto:

- `src/contexts/app/useAppRead.ts` — `clientes/oportunidades/funilEstagios` vindos de `useClientesContext()`.
- `src/contexts/catalogos/useCatalogos.ts` — `clientes` vindo de `useClientesContext()`.

Cobre indiretamente `CRMDashboard.tsx` e `ClienteProfileDialog.tsx`, que consomem os agregadores.

## Critérios preservados

- Sem alteração de UI, funil, conversão ou persistência.
- `useApp()` mantém compatibilidade para consumers não migrados.
- Typecheck (`tsgo`) verde.

## Próximo

Abrir `ARC-002.slice-07` com `FuncoesContext` (funções + tipos de documento).
