# PRO-018 · slice-03 — Perfis de layout BMS reutilizáveis

**Onda:** 07 · **Achado:** PRO-018 · **Slice:** 03/n

## Entrega
- `src/lib/bms/layoutPerfis.ts` (puro): tipos + regras
  `criarPerfil`, `validarPerfil`, `perfilAPartirDeDiagnostico`,
  `upsertPerfil`, `removerPerfil`, `sugerirPerfilPara`.
- `src/hooks/useBmsLayoutPerfis.ts`: persistência local via
  `STORAGE_KEYS.bmsLayoutPerfis` (`gestaobra:bms:layoutPerfis`).
- `src/lib/storage/keys.ts`: nova chave canônica registrada.
- `src/lib/bms/__tests__/layoutPerfis.test.ts`: valida input,
  upsert/remove, extração a partir do diagnóstico e sugestão por
  cliente.

## Regras respeitadas
- Camada pura + hook fino sobre localStorage (mesmo padrão de PRO-028).
- Sem alteração no parser (`excel.ts`) nem no `BmsImporter`.
- Chave canônica adicionada ao inventário; nenhum acesso direto a
  `localStorage` fora do hook.

## Não escopo
- UI para gerir catálogo e aplicar perfil no importador (slice-04).
- Editor manual drag-and-drop (slice-05).
- Reaplicação automática nas abas ignoradas.

## Validação
- `bunx vitest run src/lib/bms/__tests__/layoutPerfis.test.ts` — 4/4 verdes.
- `bunx tsgo --noEmit` — sem novos erros.
