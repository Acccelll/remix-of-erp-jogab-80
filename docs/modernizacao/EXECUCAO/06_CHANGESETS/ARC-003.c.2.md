# ARC-003.c.2 — `pacotes_trabalho`, `restricoes`, `pacote_restricoes`, `compromissos_semanais`, `causas_nao_conclusao`

## Escopo
Remover chamadas diretas às tabelas de planejamento (Last Planner) canalizando via `src/lib/repositories/planejamento.ts`.

## Extensões no repositório
- `pacotesTrabalhoRepo`: `listDescricao`, `listResumoObra`, `listObra`, `upsert`, `updateStatus`.
- `restricoesRepo`: `listResumo`, `listDashboard`, `listCurto`, `upsert`, `updateStatus`.
- `pacoteRestricoesRepo`: `listComData`, `listDashboard`.
- `compromissosSemanaisRepo`: `insertRaw` (preserva `error.code` para tratar `23505`).
- `causasNaoConclusaoRepo`: `listChaveLabel`.

## Call-sites refatorados (18)
- `DashboardLean.tsx` (4 leituras).
- `PPC.tsx` (1 leitura).
- `Lookahead.tsx` (2 leituras).
- `Restricoes.tsx` (2 leituras + upsert + updateStatus).
- `PacotesTrabalho.tsx` (2 leituras + upsert + 2 inserts compromisso + updateStatus).

## Verificação
- `rg 'supabase.from\("(pacotes_trabalho|restricoes|pacote_restricoes|compromissos_semanais|causas_nao_conclusao)"\)' src/` retorna apenas ocorrências dentro do próprio `planejamento.ts`.
- `tsgo --noEmit` verde.

## Pendente ARC-003.c.3
`cards/*` (board_*, card_*), `cronograma_*`, `ocorrencias`, `obra_localizacoes`, `aditivos_contrato`, `bms_previstas`, `rdo_efetivo`, `rdo_ocorrencias`, `import_validation_runs`.
