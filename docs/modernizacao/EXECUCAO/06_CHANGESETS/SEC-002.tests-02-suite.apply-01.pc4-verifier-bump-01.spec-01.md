# SEC-002.tests-02-suite.apply-01.pc4-verifier-bump-01.spec-01 — Bump PC4 após self-register do tests-02

**Onda:** 2 (OPEN — reativação do verificador após R140)  
**Data:** 2026-07-12  
**Depende de:** R136 (`EXPECTED_INDEX_ROWS = 31`), R138 (`tests-02-suite-apply-01` materializado), R140 (`INDEX.md` com linha 32).

## 1. Escopo

Contratar o patch mínimo que torna `scripts/sec002/verify-wave-2-preconditions.mjs` novamente verde após o self-register R140. O único ajuste funcional permitido é atualizar a constante `EXPECTED_INDEX_ROWS` de `31` para `32`, refletindo a nova linha útil do `INDEX.md`.

Este documento é apenas especificação. Não altera scripts, índice, testes, runner, workflows, apply-map, migrations, produção ou banco.

## 2. Patch permitido para R142

1. `EXPECTED_INDEX_ROWS = 32` em `scripts/sec002/verify-wave-2-preconditions.mjs`.
2. Manifest próprio em `.github/sec002-evidence/tests-02-suite-apply-01-pc4-verifier-bump-01/manifest.json`.
3. Registro executivo desta spec e da materialização no `SEC-001.README.md`.

Fica proibido tocar em `.github/sec002-evidence/INDEX.md`, `supabase/tests/sec-002/**`, `scripts/db/run-sec-002-tests.sh`, manifest R138/R140, apply-map, workflows, `src/**`, funções, migrações ou banco.

## 3. Invariantes específicas (TVB1–TVB8)

| ID | Invariante |
|----|------------|
| TVB1 | `EXPECTED_INDEX_ROWS` passa exatamente de `31` para `32`. |
| TVB2 | `verifyPC4()` continua usando a constante tanto na contagem quanto no laço `1..N`. |
| TVB3 | `verifyPC2()` permanece no contrato pós-abertura (`OPEN` + bloco `Abertura formal`). |
| TVB4 | `.github/sec002-evidence/INDEX.md` não é alterado no patch R142. |
| TVB5 | Os 11 arquivos de `supabase/tests/sec-002/**`, o runner `scripts/db/run-sec-002-tests.sh` e manifests R138/R140 permanecem byte-a-byte. |
| TVB6 | Nenhum arquivo de produção, workflow, apply-map ou migration é alterado. |
| TVB7 | `node scripts/sec002/verify-wave-2-preconditions.mjs --json` deve retornar `ok: true` com PC1..PC7 após R142. |
| TVB8 | Reversão exige errata específica; não é permitido reduzir a constante sem remover/errar a linha 32. |

## 4. Estado de partida obrigatório

R142 só pode materializar esta spec se:

1. `scripts/sec002/verify-wave-2-preconditions.mjs` contém `EXPECTED_INDEX_ROWS = 31`.
2. `.github/sec002-evidence/INDEX.md` possui exatamente 32 linhas úteis numeradas `1..32`.
3. A linha 32 é `tests-02-suite-apply-01`, apontando para `.github/sec002-evidence/tests-02-suite-apply-01/manifest.json` e para `SEC-002.tests-02-suite.apply-01.materialize-01.md`.
4. O schema do índice permanece `Apply-map: v2026-07-12.4`.
5. A falha atual de PC4 é exclusivamente a defasagem `INDEX.md deve ter 31 linhas úteis; encontrado 32`.

Se qualquer item divergir, R142 deve parar e abrir nova spec corretiva, sem patch oportunista.

## 5. Manifest previsto para R142

`.github/sec002-evidence/tests-02-suite-apply-01-pc4-verifier-bump-01/manifest.json`:

```json
{
  "schema_version": "v2026-07-12.4",
  "apply_id": "tests-02-suite-apply-01-pc4-verifier-bump-01",
  "source": "docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.tests-02-suite.apply-01.pc4-verifier-bump-01.spec-01.md",
  "changed_files": [
    "scripts/sec002/verify-wave-2-preconditions.mjs"
  ],
  "checks": [
    "node scripts/sec002/verify-wave-2-preconditions.mjs --json"
  ],
  "refs": ["R58", "R95", "R127", "R136", "R137", "R138", "R139", "R140", "R141", "R142"]
}
```

## 6. Critérios de aceitação

1. R141 é registrado no `SEC-001.README.md` como linha 135, tipo `especificação`.
2. R142 altera somente o verificador, o manifest próprio e o registro executivo necessário.
3. O índice permanece com 32 linhas úteis numeradas `1..32`.
4. O verificador completo retorna `OK` em modo JSON após a materialização.
5. Oda 2 permanece `OPEN` e pronta para o próximo item do write set.

## 7. Critérios de rejeição

1. Alterar `INDEX.md`, apply-map, workflows, arquivos de produção, migrations, banco, testes R138 ou runner R138 neste bump.
2. Usar valor diferente de `32` para `EXPECTED_INDEX_ROWS`.
3. Introduzir contagem dinâmica de manifests/evidências em vez de constante estática auditável.
4. Alterar PC1, PC2, PC3, PC5, PC6 ou PC7 junto com PC4.
5. Declarar sucesso sem executar o verificador completo.

## 8. Próximo passo formal

`SEC-002.tests-02-suite.apply-01.pc4-verifier-bump-01.materialize-01` (R142) — aplicar `EXPECTED_INDEX_ROWS: 31 → 32`, criar manifest próprio e reativar PC1..PC7 sob TVB1–TVB8.