# PRO-032 · slice-03 — Seletor de horizonte no painel M12

## Escopo
Adiciona seletor 3/6/12 meses ao gráfico de movimentação mensal do painel consolidado de Contratos (`ContratosOverview`).

## Mudanças
- `src/components/contratos/ContratosOverview.tsx`:
  - Estado local `horizonte: 3 | 6 | 12`, padrão 6, persistido em `localStorage` (chave `gestaobra:contratos:painel:horizonte`).
  - `ToggleGroup` compacto (shadcn) ao lado do rótulo do gráfico com opções `3m / 6m / 12m`.
  - `serieContratosMensal(contratos, horizonte)` recalcula via `useMemo` quando o horizonte muda.
  - Rótulo "últimos N meses" acompanha a seleção.

## Fora do escopo
- Drill-down por mês (candidato slice-04).
- Novos hooks/módulos puros — reutiliza `serieContratosMensal` existente.

## Validação
- `bunx tsgo --noEmit` limpo.
- Testes existentes de `serieContratosMensal` continuam válidos (função inalterada).

## Próximo
`PRO-032.slice-04` — drill-down por mês (lista de contratos novos/encerrados do bucket clicado) OU consolidar KPIs cross-módulo.
