# SEC-002 · batch-B / migration / apply-01 / spec-01

- **Ref:** R148
- **Tipo:** especificação
- **Onda:** 2 — OPEN, gate PC1..PC7 verde (R147)
- **Apply-map:** `v2026-07-12.4` (a preservar)
- **Fonte canônica:** `SEC-002.batch-B.migration.spec-01.md` (§§ 1–10) + `SEC-002.batch-B.spec-01.md` (§§ 3, 6)
- **Antecessores:** R126 (helpers-02.apply-01 candidato), R144 (tests-02.apply-02)
- **DRI:** Cappucceno (GM) / Lovable

## 1. Objetivo

Especificar o **terceiro apply autorizado** do write set da Onda 2 — materialização do arquivo `.github/sec002-evidence/batch-B-apply-01/candidate.sql` que espelha `<ts>_sec_002_batch_b.sql` (13 tabelas registry/transversal) conforme `SEC-002.batch-B.migration.spec-01` §§ 1–10, sem tocar `supabase/migrations/`, `src/`, edge functions, workflows, apply-map ou `INDEX.md`. Deploy real fica bloqueado por: `SEC-001` R1–R10 fechado, `helpers-02.migration` aplicada, `tests-02/helpers` + `tests-02/batch-B` verdes em staging, snapshot ≤ 1 h, janela DRI GM.

## 2. Write set físico (autorizado — 2 arquivos, exatos)

1. `.github/sec002-evidence/batch-B-apply-01/candidate.sql` — SQL canônico do lote B (13 tabelas), ordem alfabética, transação única `begin;` … `commit;`, blocos `drop policy if exists` + `create policy` + `enable row level security` + `revoke anon` + grants `authenticated`/`service_role`.
2. `.github/sec002-evidence/batch-B-apply-01/manifest.json` — manifest do apply.

Qualquer 3º arquivo → **REJECT**.

## 3. Invariantes (AB1 … AB16)

- **AB1** — `candidate.sql` contém **exatamente 13 blocos de tabela** na ordem alfabética de §4 (`audit_logs, centros_custo_totvs, clientes, documento_tipos, empresas, formas_pagamento, funcoes, notificacoes, plano_contas, players, profiles, user_empresas, user_roles`).
- **AB2** — Uma única transação (`begin;` no topo, `commit;` no final); nenhum `commit`/`rollback` intermediário.
- **AB3** — Cada bloco segue a ordem estrita: `drop policy if exists` (todas as vigentes registradas em §5 do spec-01) → `create policy … for select` → `create policy … for … (write)` → `alter table … enable row level security` → `revoke all … from anon` → `grant select, insert, update, delete … to authenticated` → `grant all … to service_role`.
- **AB4** — Nomes de policies, `USING`, `WITH CHECK`, `to authenticated`, `for all/select` conforme §3 e §6 de `batch-B.spec-01` — sem divergência textual.
- **AB5** — Nenhuma referência a `user_in_*`, `has_role` fora do padrão do batch-B (funções são consumidas por C/D/E).
- **AB6** — `candidate.sql` inclui cabeçalho de comentário com `SEC-002.batch-B`, DRI, ref R148 e apontamento para `SEC-002.batch-B.migration.spec-01`.
- **AB7** — Nenhum `drop table`, `truncate`, `alter column`, `create table`, `insert`, `update`, `delete` de dados.
- **AB8** — Manifest declara: `schema_version: v2026-07-12.4`, `apply_id: batch-B-apply-01`, `ref: R148`, `spec_ref: R148`, `auto_applied: false`, `verifier_gate: on`, `target_deploy_path: supabase/migrations/<ts>_sec_002_batch_b.sql` (timestamp diferido), SHA-256 do `candidate.sql`, `requires: helpers-02.migration=applied, tests-02/{helpers,batch-B}=green`.
- **AB9** — Nenhum toque em `supabase/migrations/`, `supabase/tests/`, `_lib/`, `helpers/`, `batch-*/` (dos testes), `scripts/`, `src/`, edge functions, workflows, apply-map, `INDEX.md`, verificador.
- **AB10** — `Apply-map: v2026-07-12.4` preservado literalmente.
- **AB11** — Sem segredos, sem `service_role_key`, sem URLs de banco.
- **AB12** — Gate PC1..PC7 permanece verde antes e depois; nenhum bump PC4 neste passo.
- **AB13** — Companion de rollback (`SEC-002.batch-B.rollback.sql`) NÃO é criado aqui — delegado a apply próprio.
- **AB14** — Deploy real (mover `candidate.sql` → `supabase/migrations/`) fica explicitamente fora deste changeset.
- **AB15** — Self-register (linha 34 do `INDEX.md`) delegado a R149/R150.
- **AB16** — Bump `EXPECTED_INDEX_ROWS: 33→34` delegado a R151.

## 4. Fora de escopo (delegado)

- **R149** — materialização (2 arquivos acima).
- **R150** — spec do self-register `batch-B-apply-01` → linha 34.
- **R151** — materialização do self-register.
- **R152** — spec + materialização do bump PC4 `EXPECTED_INDEX_ROWS: 33→34`.
- Companion de rollback do lote B — apply próprio, futuro.
- Deploy real do lote B — janela DRI GM, dependente do release train.

## 5. Rejeição automática

PR que toque `supabase/migrations/`, `INDEX.md`, apply-map, verificador, runner, testes, `src/`, workflows, ou que omita/adicione qualquer arquivo fora dos 2 do write set → **REJECT**.
