# SEC-002 · tests-02 / reconcile-01 / d6-request-01 / spec-01

- **Ref:** R200' (spec — pacote de decisão)
- **Tipo:** especificação (governança, sem código)
- **Onda:** 2 — OPEN
- **Apply-map:** `v2026-07-12.4` (preservado)
- **Antecessores:** R191 (spec reconcile-01, D-6 aberta), R199' (bump PC4 44→45, PC1..PC7 verde)

## 1. Objetivo

Formalizar o **pacote de decisão D-6** para o DRI DP/Financeiro assinar antes de habilitar `SEC-002.tests-02.reconcile-01.materialize-01` (R192'') e destravar o pipeline `gen-01 → runner-01 → gate-01`. Sem essa assinatura, `gen-tests-02.mjs` emitiria 316 arquivos onde a spec exige 315, quebrando GEN01-1 e PC8 no primeiro run.

## 2. Contexto (herdado de R191)

| Lote | CSV `inventory-02` | Spec `tests-01/tests-02` | Δ |
|------|--------------------|--------------------------|---|
| B | 13 | 13 | 0 |
| C | 23 | 24 | -1 |
| D | 15 | 14 | +1 |
| E | 22 | 22 | 0 |

`sha256(inventory-02.csv) = c814355885800e6f461989925bdbd4b49baed86f2081a6e684e6ba4b628494a2` (74 linhas, preservado byte-a-byte).

## 3. Opções (recap R191 §3)

| Opção | Move | Resultado | Impacto DP/Financeiro |
|-------|------|-----------|------------------------|
| **D-6a** | `solicitacao_comentarios` D → C | C=24, D=14, total=315 | Zero — comentário social sem valor monetário nem segregation |
| D-6b | `recebimentos` D → C | C=24, D=14, total=315 | Requer confirmação de que recebimentos não carregam segregation |
| D-6c | manter CSV, ajustar spec para 316 | C×4=92, D×5=75 | Adiciona 1 caso a mais, sem valor de cobertura |

**Recomendação técnica:** D-6a (comentário social alinha com escopo obra/empresa; não há política `segregation` aplicável).

## 4. Write set deste changeset (1 arquivo)

| # | Caminho | Ação |
|---|---------|------|
| 1 | `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.tests-02.reconcile-01.d6-request-01.spec-01.md` | criação (este arquivo) |

`SEC-001.README.md` recebe append de 1 linha no índice (fora do write set físico, tratado como registro do changeset).

Nenhum `.sql`, `.mjs`, `.sh`, `_lib/`, batch, migration, verifier, `INDEX.md`, apply-map, `src/`, edge function ou banco é tocado.

## 5. Invariantes (D6R-1 … D6R-8)

- **D6R-1** — CSV `inventory-02.csv` byte-a-byte preservado (`sha256: c814355885800e6f…4b628494a2`).
- **D6R-2** — Nenhum arquivo funcional tocado (só governança).
- **D6R-3** — `INDEX.md` = 45 linhas preservado; `EXPECTED_INDEX_ROWS = 45` preservado.
- **D6R-4** — `SCHEMA_VERSION`/`DEFAULT_APPLY_ID`/`Apply-map: v2026-07-12.4` preservados.
- **D6R-5** — 3 opções (D-6a/b/c) declaradas com impacto por opção.
- **D6R-6** — Recomendação técnica explícita (D-6a) com justificativa auditável.
- **D6R-7** — DRI destinatário nomeado: **DP/Financeiro** (owner do lote D).
- **D6R-8** — `verify-wave-2-preconditions.mjs --json` = `{ ok: true }` PC1..PC7.

## 6. Ação requerida do DRI DP/Financeiro

1. Escolher entre D-6a / D-6b / D-6c.
2. Assinar em `docs/modernizacao/EXECUCAO/03_DECISOES/D-6.md` (a criar em R201' se opção ≠ status-quo).
3. Notificar retomada de R192'' (`reconcile-01.materialize-01`).

## 7. Delegações e desbloqueio

- **R201'** (condicional a D-6 assinada): materializa `SEC-002.tests-02.reconcile-01.materialize-01` — edita CSV movendo 1 tabela + registra `sha256` antes/depois.
- **R202'..R204'**: pipeline `gen-01 → runner-01 → gate-01` materializados em sequência, cada um em ciclo compact (spec+materialize+self-register+bump unificados quando triviais).

## 8. Impacto em produção

**Nenhum.** Documento de governança puro. PC1..PC7 verdes; `INDEX.md`=45; `EXPECTED_INDEX_ROWS=45`; helpers-02 deploy segue bloqueado por GO GM; helpers-03 segue bloqueado por D-3 + helpers-02 deployado; D-6 é o **terceiro** desbloqueio pendente (paralelo aos outros dois).
