# ARC-013 · slice-41 — Reexport de tipos e fechamento da camada UI

## Escopo
Centralizar os tipos de cronograma usados por UI em `@/hooks/useCronograma`, eliminando o último `import` de `@/lib/repositories/cronograma` em `src/pages` / `src/components`.

## Alterações
- `src/hooks/useCronograma.ts`: importa `CronogramaItemRow` e reexporta `CronogramaItemInsert | CronogramaItemRow | CronogramaItemUpdate`.
- `src/pages/planejamento/PacotesTrabalho.tsx`: import único a partir de `@/hooks/useCronograma` (`cronogramaFns as cronogramaRepo`, `type CronogramaItemRow`).

## Validação
- `tsgo --noEmit`: limpo.
- `rg "@/lib/repositories/cronograma" src/components src/pages src/hooks`: apenas o import interno do próprio hook (fonte canônica).

## Estado ARC-013 (UI)
Concluído. Camada de UI não referencia mais repos de cronograma diretamente. Restam consumidores de domínio:
- `src/lib/cronograma/recalcular-cpm.ts`
- `src/lib/cronograma/persistir-calendarios.ts`
- `src/lib/__tests__/integracao/repositories-contrato-*.test.ts`

Estes ficam fora do escopo ARC-013 (UI → hook) e devem ser tratados em uma iniciativa separada de camada de domínio, se aplicável.
