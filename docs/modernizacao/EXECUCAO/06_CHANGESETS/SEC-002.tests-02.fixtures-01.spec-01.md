# SEC-002.tests-02.fixtures-01.spec-01 — Complemento canônico de _lib/fixtures.sql e _lib/jwt.sql

**Onda:** 2 (OPEN)
**Ref:** R192 · **Round:** compact-01
**Data:** 2026-07-12
**Escopo:** especifica **complementos aditivos** (byte-preservados no prefixo atual) a `supabase/tests/sec-002/_lib/fixtures.sql` e `_lib/jwt.sql` para cobrir a matriz de perfis de `SEC-002.tests-02.spec-01 §3` — hoje incompletos. Sem essa complementação, o gerador (R193) produziria arquivos que referenciam users/helpers inexistentes e o runner (R194) quebraria imediatamente. **Nenhum arquivo é editado neste changeset**; apenas contrato, deltas, invariantes. Materialização fica para R192b (`fixtures-01.materialize-01`) após reconciliação D-6 (R191).

## 1. Estado atual (baseline)

`_lib/fixtures.sql` (57 linhas) contém: `U1, U2, UGM, E1, E2, O1, O2, B1, B2, C1`.
`_lib/jwt.sql` (23 linhas) contém: `pg_temp.set_jwt_user(uuid)`, `pg_temp.set_jwt_anon()`.

## 2. Faltantes vs `SEC-002.tests-02.spec-01 §3`

### 2.1 Users canônicos ausentes

| Símbolo | UUID canônico proposto | Papel |
|---------|-----------------------|-------|
| `U_OBR1` | `44444441-0000-0000-0000-000000000001` | membro só de obra (não empresa) — cobre `T-C-*-member` sem `user_in_empresa` |
| `U_BRD1` | `44444442-0000-0000-0000-000000000002` | membro só de board (não obra) — cobre `T-E-*-member` |
| `U_CRD1` | `44444443-0000-0000-0000-000000000003` | membro só de card (não board) — cobre `T-E-*-member-card` |
| `U_DP` | `55555555-0000-0000-0000-000000000001` | setor `dp` — cobre `T-D-fopag_entries-member`, `T-D-dp_holerite-member` |
| `U_FIN` | `55555555-0000-0000-0000-000000000002` | setor `financeiro` ≠ solicitante — cobre caso `segregation` |
| `U_OUT` | `66666666-0000-0000-0000-000000000001` | outsider global (nenhum vínculo) — cobre `T-C/D/E-*-outsider` |

### 2.2 Objetos adicionais

- `C2` (card já declarado como referência mas sem insert): `ddddddd2-0000-0000-0000-000000000002` (card em `B2`, `owner_id=U2`).
- `user_setores(user_id, setor)` — 2 linhas: `(U_DP,'dp')`, `(U_FIN,'financeiro')` — depende de decisão D-3 assinada (tabela existir).
- `solicitacoes_financeiras` fixture: 1 linha com `solicitante_id=U1`, `obra_id=O1` — cobre `T-D-solicitacoes_financeiras-segregation` (U_FIN tenta aprovar solicitação de U1).

### 2.3 Helpers JWT ausentes

| Helper | Corpo esperado |
|--------|----------------|
| `pg_temp.set_jwt_gm()` | `perform pg_temp.set_jwt_user(:UGM::uuid);` |
| `pg_temp.set_jwt_member(_lote text)` | switch por lote: `B→U1`, `C→U1` (empresa+obra), `D→U_DP`, `E→U_BRD1` |
| `pg_temp.set_jwt_outsider()` | `perform pg_temp.set_jwt_user(:U_OUT::uuid);` |
| `pg_temp.set_jwt_owner()` | `perform pg_temp.set_jwt_user(:U1::uuid);` (owner do B1/C1) |
| `pg_temp.set_jwt_segregation()` | `perform pg_temp.set_jwt_user(:U_FIN::uuid);` |

## 3. Preservação byte-a-byte

- `_lib/fixtures.sql` linhas 1-57 **imutáveis**. Apêndice adiciona users, `user_setores`, `C2`, `solicitacoes_financeiras` fixture — todos com `on conflict do nothing`.
- `_lib/jwt.sql` linhas 1-23 **imutáveis** (mantém `set_jwt_user(uuid)` e `set_jwt_anon()`). Apêndice adiciona 5 helpers acima.
- `\set` das novas variáveis (U_OBR1 etc.) vai **no topo do apêndice**, não no meio dos existentes, para não invalidar diffs futuros de fixture legada.

## 4. Dependências duras para materialização (R192b)

- **D-3 assinada** (`user_setores` como fonte de setor funcional).
- **D-6 assinada** (R191) — impacta apenas contagem, mas casa a matriz.
- `solicitacoes_financeiras` com colunas `solicitante_id`, `obra_id`, `valor` presentes (já verificado no inventário).

Enquanto D-3 estiver pendente, materialização das 2 linhas `user_setores` fica `commented out` no apply — helpers `set_jwt_segregation()` retornam `raise notice 'SKIP D-3 pending'`.

## 5. Invariantes (FIX01-1…FIX01-8)

| ID | Verificação |
|----|-------------|
| FIX01-1 | `_lib/fixtures.sql` linhas 1-57 sha256 preservado |
| FIX01-2 | `_lib/jwt.sql` linhas 1-23 sha256 preservado |
| FIX01-3 | Novos users com `on conflict do nothing` — idempotente |
| FIX01-4 | Novos JWT helpers apenas `pg_temp.*` — sem `security definer`, sem DDL fora de `create or replace function pg_temp.*` |
| FIX01-5 | `set_jwt_member(_lote)` cobre exatamente B/C/D/E — outro lote = `raise exception 'FIX01: unknown batch %'` |
| FIX01-6 | `set_jwt_segregation()` sem `user_setores` seed → `raise notice 'SKIP D-3'` e não falha runner |
| FIX01-7 | Nenhum `grant`, `alter role`, `set search_path` fora de helper |
| FIX01-8 | `verify-wave-2-preconditions.mjs --json` = `{ ok:true }` PC1..PC7 |

## 6. Ordem de execução resultante

1. **R192 (este):** spec.
2. **R192b:** `fixtures-01.materialize-01` — só após D-3 (opcional; sem D-3 os 2 inserts em `user_setores` ficam comentados).
3. **R193:** `gen-tests-02.mjs materialize` — depende de D-6 (R191) + fixtures completas.
4. **R194:** runner.
5. **R195:** PC8/PC9.

## 7. Impacto em produção

**Nenhum.** Apenas especificação. PC1..PC7 permanecem verdes; `INDEX.md`=44 linhas; `EXPECTED_INDEX_ROWS=44`.
