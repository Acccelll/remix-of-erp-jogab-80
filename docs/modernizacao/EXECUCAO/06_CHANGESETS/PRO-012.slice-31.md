# PRO-012 · slice-31 — Chip "em piora" clicável como atalho de filtro

**Onda:** 07 · Suprimentos & Contagens Cíclicas
**Contrato:** UI-only.

## Escopo
Transformar o chip "em piora" do resumo de tendência (slice-30) em um botão de toggle equivalente ao filtro "Somente em piora" da toolbar, oferecendo um atalho visual direto.

## Alterações
- `src/pages/suprimentos/ContagensCiclicas.tsx`:
  - Chip "em piora" agora é `<button>` com `aria-pressed={apenasPiora}`.
  - Estado visual muda entre `bg-destructive/10` (inativo) e `bg-destructive text-destructive-foreground` (ativo), com hover intermediário.
  - Desabilitado quando não há linhas em piora e o filtro está inativo.
  - Chips "estáveis" e "em melhora" seguem informativos (não há filtro correspondente).

## Validação
- `bunx tsgo --noEmit` sem erros.
- Comportamento sincronizado com o botão da toolbar (compartilham `apenasPiora`), incluindo persistência do slice-26.

## Próximo passo
`PRO-012.slice-32` — extrair o resumo de tendência para um componente próprio (`TrendSummary`) reutilizável em outras listas de contagens/importações.
