# SEC-002 · tests-02 / gen-01 / reconcile-affirm-01 / spec-01

- **Ref:** R204' (afirmação pós-reconciliação, governança pura)
- **Tipo:** especificação (nenhum artefato executável tocado)
- **Onda:** 2 — OPEN
- **Apply-map:** `v2026-07-12.4` (preservado)
- **Antecessores:** R187 (`gen-01.spec-01`), R191 (`inventory-02.csv`), R200'..R203' (D-6a request → sign → materialize)

## 1. Objetivo

Afirmar formalmente que, após a materialização de **D-6a** em R203', o contrato `SEC-002.tests-02.gen-01.spec-01` **permanece válido byte-a-byte** — sem bump, sem edição, sem novo write set executável — e que a invariante `GEN01-2` passa a ser verificável contra o CSV reconciliado sem discrepância.

Este changeset é **puramente declaratório**: não escreve `scripts/sec002/gen-tests-02.mjs`, não cria `batch-*/*.sql`, não altera `gen-01.spec-01`, e não avança nenhuma dependência dura (helpers-02/03/assert-01) — apenas fecha o ciclo de reconciliação D-6 no plano.

## 2. Write set deste changeset

| # | Caminho | Ação |
|---|---------|------|
| 1 | `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.tests-02.gen-01.reconcile-affirm-01.spec-01.md` | criação (este arquivo) |

`SEC-001.README.md` recebe append de 1 linha no índice de changesets como registro do round. Nenhum outro arquivo tocado.

## 3. Conciliação numérica

Distribuição declarada em `gen-01.spec-01 §3` e `tests-02.spec-01 §4` vs CSV reconciliado (após R203'):

| Lote | Tabelas (CSV) | Casos/tabela (spec §4) | Σ casos (spec) | Σ casos (CSV × spec) | Δ |
|------|---------------|------------------------|----------------|----------------------|---|
| B    | 13            | 3                      | 39             | 13 × 3 = 39          | 0 |
| C    | **24**        | 4                      | 96             | 24 × 4 = 96          | 0 |
| D    | **14**        | 5                      | 70             | 14 × 5 = 70          | 0 |
| E    | 22            | 5                      | 110            | 22 × 5 = 110         | 0 |
| **Σ** | **73**       | —                      | **315**        | **315**              | **0** |

**Conclusão:** CSV reconciliado ↔ spec `GEN01-2` = **casamento exato**. A divergência histórica (CSV=316 vs spec=315) foi fechada exclusivamente pela reclassificação `public.solicitacao_comentarios` D→C (D-6a).

## 4. Efeito sobre `gen-01.spec-01`

- **§3 (Saídas)** — sem bump; contagens 39/96/70/110 já refletiam o alvo D-6a e agora coincidem com o CSV.
- **§4.1 (Determinismo)** — sem bump; oráculo `solicitacao_comentarios` passa a ser gerado a partir do lote **C** (4 casos: `gm`, `member`, `outsider`, `anon`) em vez de **D** (5 casos incluindo `segregation`), consistente com D-6a (comentário social sem `segregation`).
- **§5 Invariantes** — todas preservadas; `GEN01-2` fica **verificável sem exceção**.
- **§7 Dependências duras** — inalteradas: `helpers-02.materialize-01`, `helpers-03.materialize-01` (D-3), `tests-02.assert-01.materialize-01`, fixtures canônicas.

## 5. Invariantes deste changeset (RAF-1 … RAF-8)

- **RAF-1** — `SEC-002.inventory-02.csv` **não é tocado** neste round (baseline `8c18a694442e…d66a404ef` preservado). ✅
- **RAF-2** — `SEC-002.tests-02.gen-01.spec-01.md` **não é tocado** byte-a-byte (sem bump). ✅
- **RAF-3** — `SEC-002.tests-02.spec-01.md` **não é tocado** byte-a-byte. ✅
- **RAF-4** — Nenhum `.sql`, `.mjs`, `.sh`, `_lib/`, batch, migration, verifier, `src/`, workflow, edge function ou banco tocado. ✅
- **RAF-5** — Σ casos declarada (315) = Σ derivada do CSV × spec (315). ✅
- **RAF-6** — `INDEX.md` = 45 linhas e `EXPECTED_INDEX_ROWS = 45` preservados. ✅
- **RAF-7** — `SCHEMA_VERSION`, `DEFAULT_APPLY_ID` e `Apply-map: v2026-07-12.4` preservados. ✅
- **RAF-8** — `verify-wave-2-preconditions.mjs --json` permanece `{ ok: true }` com PC1..PC7. ✅

## 6. Delegação aberta

Fechado o ciclo D-6, o próximo bloqueio material do pipeline `gen-01 → runner-01 → gate-01` volta às **duas dependências duras** originais:

1. **GO GM → `helpers-02.deploy-01`** (R193): destrava `helpers-02` aplicado.
2. **D-3 → `helpers-03.apply-01`** (R194): destrava `helpers-03` aplicado.

Ambas são externas (assinatura/aprovação); sem elas, `SEC-002.tests-02.gen-01.materialize-01` permanece `draft` mesmo com CSV reconciliado.

## 7. Impacto em produção

**Nenhum.** Afirmação documental exclusivamente. Banco, migrations, policies, `_lib/`, batches, scripts executáveis, workflows, `src/` e apply-map permanecem intocados.
