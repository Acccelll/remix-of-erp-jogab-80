# TST-002.c — Expansão dos testes de integração (Kanban/Cards)

**Onda:** pré-condição da Onda 6 (Stage Gate: TST-002 em curso, ver D-9).
**Achado:** TST-002 — lote C.

## Contexto

Continua o esforço de destravar Onda 6 (junto de D-9). Este lote foca os
repositórios do domínio Kanban/Cards, alvo direto dos Achados DS-011
(6 implementações de Kanban) e DS-016 (31 diálogos autofetchantes) da Onda 6.

## Mudanças

- Novo `src/lib/__tests__/integracao/repositories-contrato-c.test.ts` com 16 casos:
  - `boardListasRepo`: `create` (sucesso + erro), `insertMany` (vazio + com rows),
    `update`, `updatePosicao`.
  - `cardChecklistItensRepo`: `listByCard` (contrato de colunas explícitas + erro),
    `insertMany` (vazio), `setConcluido`, `removeByCard`.
  - `cardMembrosRepo`: `listUsers`, `insert`, `removeByCard`.
  - `cardSetoresRepo`: `listByCard` (colunas explícitas), `insertMany` (vazio).

## Validação

- `bunx vitest run src/lib/__tests__/integracao/repositories-contrato-c` → **16/16**.
- Suíte completa: **467/467 verdes** (baseline pós-TST-002.b era 451).
- `bunx tsgo --noEmit` → verde.

## Cobertura acumulada de TST-002

| Lote | Repositories                                                                         | Casos |
| ---- | ------------------------------------------------------------------------------------ | ----- |
| A    | `obrasRepo`, `fornecedoresRepo`                                                      | 9     |
| B    | `clientesRepo`, `insumosRepo`                                                        | 14    |
| C    | `boardListasRepo`, `cardChecklistItensRepo`, `cardMembrosRepo`, `cardSetoresRepo`    | 16    |

**Total:** 39 casos de contrato sobre 8 repositórios.

Restantes (para lotes D+): `cards`, `cardsExtra` (fatias restantes: anexos,
recursos, comentários), `cronograma`, `dp`, `dpHolerite`, `financeiro`,
`inspecoes`, `obraDetalhe`, `planejamento`, `rdo`, `requisicoes`,
`suprimentos`, `boardCamposRepo`, `boardMembrosRepo`.

## Próximo

Lote D — repositórios `cronograma` + `requisicoes` (dependências diretas do
domínio de obras que a Onda 6 vai tocar via ARC-005).
