# ARC-002 · slice-14 — Migração de dialogs de perfil para contexts de domínio

**Onda:** 6 · **Status:** ✅ concluído · **Typecheck:** `tsgo` verde

## Escopo

Batch 4 da retirada gradual de leituras de domínio de `useApp()` / `useAppRead()`.

## Entregue

- `src/components/contratos/ContratoProfileDialog.tsx` — `obras` via `useObrasContext()`; `useApp()` mantido apenas para `hasAccess`.
- `src/components/patrimonios/PatrimonioProfileDialog.tsx` — `obras` via `useObrasContext()`; `useApp()` mantido para `hasAccess/currentPlayer`.
- `src/components/rh/EmployeeProfileDialog.tsx` — `obras` via `useObrasContext()`; `useApp()` mantido apenas para `hasAccess`.
- `src/components/frotas/VeiculoProfileDialog.tsx` — `obras` via `useObrasContext()`; `useApp()` mantido apenas para `hasAccess`.
- `src/components/cliente/ClienteProfileDialog.tsx` — `obras` via `useObrasContext()` e `oportunidades` via `useClientesContext()`; removido `useAppRead()`.

## Próximo

`ARC-002.slice-15` — localizar os consumidores restantes de `useApp()`/`useAppRead()` com leituras de domínio e migrar em batches pequenos, preservando `useApp()` apenas para autenticação/permissões enquanto não houver provider dedicado.