# SEC-002.wave-2-formal-open-01.formal-apply-01.materialize-01 — Apply formal de abertura da Onda 2

**Onda:** 2 (apply formal executado — Onda 2 aberta operacionalmente)
**Data:** 2026-07-12T17:16:41Z
**Fonte:** `SEC-002.wave-2-formal-open-01.formal-apply-01.spec-01.md` (R126)
**Apply-id:** `wave-2-formal-open-01`
**DRI GM:** Cappucceno
**DRI técnico:** Lovable

## 1. Escopo executado

Transição canônica de `.github/sec002-readiness/wave-2.md` de `BLOCKED` para `OPEN`, conforme bloco canônico definido em R95 §3 e sob invariantes FA1–FA12 de R126. Write set físico:

1. `.github/sec002-readiness/wave-2.md` — `status: BLOCKED` → `status: OPEN` no frontmatter, `Status atual: BLOCKED` → `Status atual: OPEN` no corpo, e bloco `## Abertura formal` apendado ao final.
2. `.github/sec002-evidence/wave-2-formal-open-01/manifest.json` — manifest canônico conforme FA8.

Nenhum outro arquivo é tocado neste apply (FA1/FA5/FA6).

## 2. Pré-execução verificada

Comando: `node scripts/sec002/verify-wave-2-preconditions.mjs --json`
SHA verificado: `6882770bd41c3ca35de8c500085a85a661797b59`
Saída (exit 0):

```json
{
  "ok": true,
  "schema_version": "v2026-07-12.4",
  "apply_id": "wave-2-formal-open-01",
  "checked": ["PC1", "PC2", "PC3", "PC4", "PC5", "PC6", "PC7"]
}
```

Todos os PC1–PC7 saíram `OK` sobre `main` imediatamente antes do apply (FA7).

## 3. Observação pós-apply (esperada por design)

Após a materialização, `verify-wave-2-preconditions.mjs` passa a retornar `E_WAVE2_PC2` (`Status diferente de BLOCKED: OPEN`). Isto é comportamento **correto e esperado**: o verificador R98 é o gate de **abertura** — sua condição PC2 exige `BLOCKED` justamente para impedir dupla-abertura. Uma vez aberta, PC2 inverte por design. Gate operacional para os próximos applies dentro da Onda 2 (helpers-02, tests-02-suite, batch-B..E, post-rls-handoff) será contratado em changeset separado, fora do escopo desta materialização.

## 4. Invariantes preservadas

- FA1..FA12 (R126) — write set físico limitado, bloco canônico byte-a-byte, DRIs `Cappucceno`/`Lovable`, sem toque em produção/scripts/workflows/`INDEX.md`/apply-map.
- FO1..FO8 (R95) — apply-map `v2026-07-12.4` preservado, sem append em `INDEX.md` neste commit, sem alteração em `src/**`/`supabase/**`/`scripts/**`/`.github/workflows/**`.
- FS1..FS8 (R94) — este é o movimento operacional explicitamente autorizado por FS7, não um novo self-register preparatório.

## 5. Estado resultante

- Onda 2: **OPEN** (operacional).
- Apply-map: `v2026-07-12.4` (inalterado).
- `INDEX.md`: 29 linhas úteis (inalterado neste commit — self-register operacional é changeset separado posterior).
- Write set liberado: `helpers-02`, `tests-02-suite`, `batch-B`, `batch-C`, `batch-D`, `batch-E`, `post-rls-handoff`.
- Reversão exige `SEC-002.wave-2-formal-open-01.errata-01` (FA11/FO8).

## 6. Próximo passo formal

`SEC-002.wave-2-formal-open-01.formal-apply-01.self-register-01.spec-01` — spec do self-register do apply operacional como linha 30 do `INDEX.md`, seguido de materialização. Este é o único append em `INDEX.md` autorizado como consequência de R127.
