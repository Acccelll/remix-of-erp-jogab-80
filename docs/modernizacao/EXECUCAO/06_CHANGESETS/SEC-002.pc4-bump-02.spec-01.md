# SEC-002 · pc4-bump-02 · spec-01 (R156)

**Onda:** 2 (OPEN) · **Apply-map:** v2026-07-12.4 · **Gate:** restaura PC1..PC7 = green após apply

## Objetivo
Autorizar o bump da constante `EXPECTED_INDEX_ROWS` de `34` → `35` em `scripts/sec002/verify-wave-2-preconditions.mjs`, refletindo o self-register de R155 (linha 35 = `pc4-bump-01`).

## Write set físico autorizado (R157 — 2 arquivos)
1. `scripts/sec002/verify-wave-2-preconditions.mjs` — alteração pontual da linha 10:
   - antes: `const EXPECTED_INDEX_ROWS = 34;`
   - depois: `const EXPECTED_INDEX_ROWS = 35;`
2. `.github/sec002-evidence/pc4-bump-02/manifest.json` — manifest do bump (`schema_version: v2026-07-12.4`, `expected_index_rows_before: 34`, `expected_index_rows_after: 35`, `auto_applied: false`, `verifier_gate: on`).

Nenhuma outra alteração autorizada em R157.

## Invariantes PC4B2-1…PC4B2-8
- **PC4B2-1** Diff no verificador = exatamente 1 linha alterada (linha 10), 0 adições/remoções fora dela.
- **PC4B2-2** Nenhuma outra constante numérica alterada.
- **PC4B2-3** Lógica de PC1/PC2/PC3/PC5/PC6/PC7 preservada byte-a-byte.
- **PC4B2-4** Loop `for (let i = 1; i <= EXPECTED_INDEX_ROWS; …)` inalterado.
- **PC4B2-5** `Apply-map: v2026-07-12.4` preservado no cabeçalho do `INDEX.md`.
- **PC4B2-6** `.github/sec002-evidence/INDEX.md` NÃO é tocado por R157.
- **PC4B2-7** Após R157: `node scripts/sec002/verify-wave-2-preconditions.mjs --json` retorna `ok: true` (PC1..PC7).
- **PC4B2-8** Manifest `pc4-bump-02/manifest.json` gravado com os campos acima.

## Invariantes transversais TVBP3-1…TVBP3-8
- **TVBP3-1** Sem tocar `supabase/migrations/`.
- **TVBP3-2** Sem tocar `INDEX.md`, batch-B artefatos, `pc4-bump-01/*`, `pc4-bump-01-self-register-01/*`.
- **TVBP3-3** Sem tocar `scripts/sec002/_lib/`, `helpers/`, `batches/`.
- **TVBP3-4** Sem tocar `src/`, `tests/`, workflows.
- **TVBP3-5** Sem tocar apply-map ou outros manifests.
- **TVBP3-6** Sem segredos, chaves ou tokens.
- **TVBP3-7** Nenhuma execução real contra DB.
- **TVBP3-8** Self-register do bump (linha 36) fica delegado a R158+.

## Estado esperado pós-R157
- Verificador linha 10 = `const EXPECTED_INDEX_ROWS = 35;`.
- Gate PC1..PC7 = green.
- Onda 2 permanece OPEN; nenhum artefato de dados/produção alterado.
