# ARC-013 · slice-34 — encapsular `cronogramaItemRevisoesRepo.insertMany` em hook canônico

## Alterações
1. **`src/hooks/useCronograma.ts`**: adiciona `cronogramaItemRevisoesMutationFns.insertMany` (wrapper imperativo). Import interno de `cronogramaItemRevisoesRepo`.
2. **`src/components/obra/CronogramaPrincipalTab.tsx`**: remove import de `cronogramaItemRevisoesRepo`; usa `cronogramaItemRevisoesMutationFns.insertMany`. Componente agora não importa mais nada de `@/lib/repositories/cronograma`.

## Escopo intencional
`RevisoesTab`, `LimparImportadosButton` e `CronogramaSemanalImporter` seguem consumindo o repo diretamente — próximas slices (`RevisoesTab` requer wrappers de leitura `listSnapshotsByRevisao`/`removeByRevisao` e composição com `cronogramaRepo`).

## Validação
- `bunx tsgo --noEmit`: limpo.
- `rg 'cronogramaRepo|cronogramaItemRevisoesRepo' src/components/obra/CronogramaPrincipalTab.tsx`: sem ocorrências.
