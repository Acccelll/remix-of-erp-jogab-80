# TST-002.m — Contrato de obrasRepo

**Onda:** 6 (bloqueada — NO-GO D-9)  
**Data:** 2026-07-12  
**Escopo:** consolidar TST-002 travando o último repositório estruturante (`obrasRepo`). Patrimônios e veículos permanecem em `localStorage` e não expõem repositório Supabase — sem alvo neste lote.

## Alvo

- `src/lib/repositories/obras.ts` — `obrasRepo`.

## Arquivo criado

- `src/lib/__tests__/integracao/repositories-contrato-m.test.ts` — 18 casos.

## Contratos travados

- Projeções de resumo: `listResumo` (id, nome — order nome), `listResumoComCodigo`/`listResumoComCodigoNome` (order codigo/nome), `listComClienteId`, `listMinContrato`, `listComCodigoTotvs` (limit 2000, sem ordem), `listDashboard` (9 colunas fixas), `listComCliente` (join `clientes(nome)`).
- Leituras por id via `maybeSingle`: `getCentroCustoTotvs`, `getNome`, `getPedidoValorContrato` — devolvem `null` em ausência e propagam erro.
- `getById`: join `clientes(nome, cnpj)` + `eq('id',...)` + `limit(1)`, devolve primeiro registro ou `null`.
- Mutações: `create`/`update`/`remove` com verbos e `eq('id',...)`, todos propagam erro.

## Resultado

- Lote M: 18/18 ✔
- Suite total: **672/672** ✔
- `tsgo --noEmit`: verde (nenhuma mudança em código de produção)

## Acumulado TST-002 (bateria completa)

13 lotes · 47 superfícies · **244 casos** cobrindo todos os repositórios Supabase reais em `src/lib/repositories/**` e queries de `financeiro-totvs`.

## Próximo

`TST-002 encerrada`. Próximo passo: revisar `docs/modernizacao/EXECUCAO/00_EXECUTIVO/07_DECISOES.md` marcando TST-002 como pré-condição ATENDIDA e reabrir o gate da Onda 6 (D-9 → avaliar demais bloqueios pendentes antes de mover para GO).
