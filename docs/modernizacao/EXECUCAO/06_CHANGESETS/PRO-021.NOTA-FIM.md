# PRO-021 · Nota-fim (slice-01..04)

**Onda:** 07 · Contratos
**Achado:** PRO-021 — Contratos: sem gestão documental (arquivo anexo, assinaturas).
**Status:** ✅ Concluído em 4 slices.

## Escopo entregue
- **slice-01:** upload de anexos por contrato com armazenamento local + tipo/nome/tamanho.
- **slice-02:** versionamento por documento (v1, v2, …) preservando históricos.
- **slice-03:** status (`rascunho`/`vigente`/`vencido`) e validade por documento.
- **slice-04:** alertas de documento vencido/vencendo integrados ao `NotificationBell` com badge por severidade.

## Arquivos-chave
- `src/hooks/useContratoDocumentos.ts`, `src/hooks/useDocumentosContratoVencendo.ts`.
- `src/lib/contratos/documentos.ts` (módulo puro).
- `src/components/contratos/ContratoDocumentosPanel.tsx`.
- `src/components/layout/NotificationBell.tsx`.
- `docs/modernizacao/EXECUCAO/06_CHANGESETS/PRO-021.slice-01..04.md`.

## Contrato respeitado
- Persistência local; sem alterações de schema/RLS.
- Sino respeita `contratos:visualizar` + preferências de categoria.

## Débitos / oportunidades futuras
- Silenciar alerta por documento (reaproveitar `useContratoAlertaSilencio`).
- Assinaturas eletrônicas formais (fora do escopo P3).
- Migração para storage remoto (backend M12).

## Próximo passo
Dashboard consolidado M12 (candidato próximo alvo da Onda 08) — ou retomar próximo achado paralelo da Onda 07 (`PRO-002`, `PRO-016`, `PRO-020`).
