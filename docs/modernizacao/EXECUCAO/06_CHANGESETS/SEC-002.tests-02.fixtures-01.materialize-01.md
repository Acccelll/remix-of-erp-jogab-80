# SEC-002.tests-02.fixtures-01.materialize-01 — Apêndice de _lib/fixtures.sql e _lib/jwt.sql

**Onda:** 2 (OPEN)
**Ref:** R192b · **Round:** compact-01
**Data:** 2026-07-12
**Escopo:** materializa o complemento canônico especificado em R192, via `>>` (append-only) em `supabase/tests/sec-002/_lib/fixtures.sql` (prefixo 57 linhas preservado) e `_lib/jwt.sql` (prefixo 23 linhas preservado). Não bloqueado por D-6 (fixtures independentes da reclassificação C↔D). Condicional a D-3: `user_setores` fica comentado, `set_jwt_segregation()` emite `raise notice 'SKIP D-3'`.

## 1. Write set físico (3 arquivos)

| # | Caminho | Ação |
|---|---------|------|
| 1 | `supabase/tests/sec-002/_lib/fixtures.sql` | append: 6 users, C2, board/obra/card membros complementares, fixture condicional `solicitacoes_financeiras` |
| 2 | `supabase/tests/sec-002/_lib/jwt.sql` | append: 5 helpers `pg_temp.set_jwt_{gm,member,outsider,owner,segregation}` |
| 3 | `.github/sec002-evidence/tests-02-fixtures-01/manifest.json` | manifest com hashes e status D-3/D-6 |

## 2. Preservação byte-a-byte

Método: shell `>>` — por construção não modifica bytes anteriores. Manifest registra `prefix_sha256_baseline` (fixtures: `e21923a…`, jwt: `b1eef646…`) e `sha256_after`. Qualquer verifier futuro pode reabrir os arquivos, ler os primeiros 57/23 bytes-lógicos e comparar com o baseline documentado.

## 3. Semântica de compatibilidade

- **Sem D-3:** `set_jwt_segregation()` retorna via `raise notice`; runner não quebra; testes que dependem de segregation ficam `SKIP` (o helper de assert `deny` continua satisfeito por membership padrão).
- **Sem tabela `solicitacoes_financeiras.solicitante_id`:** `do $$ ... $$` no fixture pula insert; nenhum erro emitido.
- **Idempotência:** todos os inserts usam `on conflict do nothing`; roda N vezes = mesmo estado final.

## 4. Invariantes verificadas (FIM01-1…FIM01-8)

| ID | Verificação | Resultado |
|----|-------------|-----------|
| FIM01-1 | Prefixo fixtures.sql (57 linhas) preservado por `>>` | ✅ |
| FIM01-2 | Prefixo jwt.sql (23 linhas) preservado por `>>` | ✅ |
| FIM01-3 | `rg -c "^\\set U_(OBR1\|BRD1\|CRD1\|DP\|FIN\|OUT)"` = 6 | ✅ |
| FIM01-4 | `rg -c "pg_temp.set_jwt_(gm\|member\|outsider\|owner\|segregation)"` = 5 | ✅ |
| FIM01-5 | Zero `security definer`, zero `grant`, zero `alter role` no apêndice | ✅ |
| FIM01-6 | `user_setores` insert comentado (D-3 pendente) | ✅ |
| FIM01-7 | Fixture `solicitacoes_financeiras` em `do $$` com `if exists` guard | ✅ |
| FIM01-8 | `verify-wave-2-preconditions.mjs --json` = `{ ok:true }` PC1..PC7 | ✅ |

## 5. Habilitação a jusante

Manifest com `applied:true` é pré-condição de:

- R193 (`gen-tests-02.mjs materialize`) — depende também de D-6.
- R194 (`run-sec-002-tests.sh materialize`).
- PC8/PC9 (R195).

## 6. Não tocado

`_lib/teardown.sql`, `_lib/assert.sql`, `batch-*/`, `helpers/`, `scripts/sec002/*`, `.github/sec002-evidence/INDEX.md`, `supabase/migrations/`, `src/`, `supabase/functions/`, apply-map, workflows, banco.

## 7. Impacto em produção

**Nenhum.** `_lib/*.sql` só é lido por arquivos pgTAP em `supabase/tests/`. PC1..PC7 permanecem verdes; `INDEX.md`=44 linhas; `EXPECTED_INDEX_ROWS=44`.
