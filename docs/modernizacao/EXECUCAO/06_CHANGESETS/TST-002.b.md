# TST-002.b — Expansão dos testes de integração de repositories

**Onda:** pré-condição da Onda 6 (Stage Gate: "TST-002 em curso").
**Achado:** TST-002 — lote B.

## Contexto

Onda 6 permanece **NO-GO** (ver **D-9**): faltam TST-001.b (E2E consolidado)
e progresso contínuo de TST-002. Este lote expande a cobertura da baseline
(lote A: `obrasRepo`, `fornecedoresRepo`) para dois repositórios adicionais
de alto uso.

## Mudanças

- Novo arquivo `src/lib/__tests__/integracao/repositories-contrato-b.test.ts`
  com 14 casos cobrindo:
  - `clientesRepo`: `list` (sucesso, `data null`, erro), `listIdNome`,
    `create`, `update`, `remove`.
  - `insumosRepo`: `listAtivos` (sucesso, `data null`, erro via `unwrap`),
    `listPrecos`, `create`, `update`, `toggleAtivo`.
- Estilo idêntico ao lote A (mesmo mock recorder do cliente supabase, sem
  banco).

## Validação

- `bunx vitest run src/lib/__tests__/integracao/repositories-contrato-b` → **14/14**.
- Suíte completa: **451/451 verdes** (baseline pós-DS-001 era 437).
- `bunx tsgo --noEmit` → verde.

## Cobertura acumulada de TST-002

| Lote | Repositories                              | Casos |
| ---- | ----------------------------------------- | ----- |
| A    | `obrasRepo`, `fornecedoresRepo`           | 9     |
| B    | `clientesRepo`, `insumosRepo`             | 14    |

Restantes (para lotes C+): `cards`, `boards`, `cardsExtra`, `cronograma`,
`dp`, `dpHolerite`, `financeiro`, `inspecoes`, `obraDetalhe`, `planejamento`,
`rdo`, `requisicoes`, `suprimentos`.

## Próximo

Continuar em lotes incrementais junto de cada Achado que tocar o repositório
(objetivo: destravar Onda 6 combinado com TST-001.b).
