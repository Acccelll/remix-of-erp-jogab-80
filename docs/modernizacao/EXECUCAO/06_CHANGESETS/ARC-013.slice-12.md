# ARC-013 · slice-12 — `cardSetoresRepo` via hooks canônicos

**Onda:** 6 (ARC-013)
**Escopo:** reduzir dispersão de `cardSetoresRepo`, o repo de cards extra mais consumido em componentes, movendo leituras/mutações para `src/hooks/useCards.ts`.

## Alterações

### `src/hooks/useCards.ts`
- `cardsQueryFns.listSetoresByCard(cardId)`.
- `cardsQueryFns.listSetoresBySetor(setor, subsetor?)`.
- `cardsQueryFns.listStatusSetoresPorCardsESetor(cardIds, setor)`.
- `useInsertCardSetor()`.
- `useInsertCardSetores()`.
- `useRemoveCardSetor()`.
- `useUpdateCardSetorStatus()`.
- `useUpdateCardSetorStatusComSubsetor()`.

### Componentes migrados
- `QuadroKanban.tsx` — leituras do kanban e mudança de trilho passam pelo hook/queryFns.
- `CardGenericoDialog.tsx` — adicionar/remover setor passa por mutations canônicas.
- `CardRecursoDialog.tsx` — leitura de setores e update de status de trilho passam por hook/queryFns; update derivado de status global usa `useUpdateCard()`.
- `AdicionarRecursoDialog.tsx` — criação dos trilhos de setor usa `useInsertCardSetores()`.
- `MoverCopiarCardMenu.tsx` — clonagem de setores usa `cardsQueryFns.listSetoresByCard()` + `useInsertCardSetores()`.

## Validação

- `bunx tsgo --noEmit` — sem erros.
- `rg "cardSetoresRepo" src/components src/hooks` deve manter `cardSetoresRepo` apenas em `src/hooks/useCards.ts` dentro do escopo migrado; repositório e testes de contrato permanecem.

## Próximo passo

`ARC-013.slice-13` — migrar próximo repo extra de maior impacto no fluxo de recursos (`cardRecursosRepo`) para hooks/queryFns canônicos, começando por `QuadroKanban`, `AdicionarRecursoDialog`, `CardRecursoDialog` e `useAlertasPrazo`.