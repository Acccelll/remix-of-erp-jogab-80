# SEC-002.validate-evidence-index-01.self-register-guard-promote-01.materialize-01 — Materialização canônica da promoção do `self-register-guard` a gate

**Onda:** 2 (bloqueada — depende de `SEC-001` fechar e da Onda 2 ser formalmente aberta)
**Data:** 2026-07-12
**Depende de:**
- `SEC-002.pr-checklist-workflow-01.evidence-validator-bump-01.materialize-01` (R67) — workflow base `v2026-07-12.4` com 11 gates operacionais + `validate-evidence-index`;
- `SEC-002.validate-evidence-index-01.self-register-guard-01.materialize-01` (R73) — reforço S1–S6;
- `SEC-002.validate-evidence-index-01.self-register-guard-01.tests-01.materialize-01` (R75) — runner T1–T5 `scripts/sec002/test-validate-evidence-index-self-register-guard.mjs`;
- `SEC-002.evidence-index-01.self-register-guard-bump-01.materialize-01` (R77) — `INDEX.md` sem órfãos (linhas 17–19);
- `SEC-002.validate-evidence-index-01.self-register-guard-promote-01.spec-01` (R78) — invariantes P1–P7.

**Escopo:** materializar, em conteúdo canônico versionado neste documento, o workflow `.github/workflows/sec002-pr-checklist.yml` com o novo step `sec002/test-validate-evidence-index-self-register-guard` inserido **como último passo do job**, imediatamente após `validate-evidence-index`. O cabeçalho `apply-map: v2026-07-12.4` e a ordem dos 11 gates operacionais herdada de R67 permanecem literalmente inalteradas. **Nenhum arquivo de produção é gravado nesta etapa.** Gravação física permanece bloqueada até a abertura formal da Onda 2.

## 1. Invariantes cobertas

| Regra | Descrição executável |
|---|---|
| W1–W14 | herdadas de R50/R57 |
| W15–W17 | herdadas de R66/R67 (`apply-map: v2026-07-12.4`, `validate-evidence-index` presente, gates operacionais imutáveis) |
| P1 | cabeçalho `apply-map: v2026-07-12.4` preservado |
| P2 | os 11 gates operacionais permanecem na mesma ordem, com o mesmo `run:` |
| P3 | novo gate inserido **imediatamente após** `validate-evidence-index` |
| P4 | novo gate executa `node scripts/sec002/test-validate-evidence-index-self-register-guard.mjs` sem flags |
| P5 | novo gate condicionado a `steps.parse-apply-id.outcome == 'success'`; `parse-apply-id` recebe `id: parse-apply-id` (metadata, sem mudança de `run:`) |
| P6 | nenhum outro step é alterado |
| P7 | falha do runner faz o job falhar com o código `E_EVIDX_S*` emitido em stderr |

## 2. Artefato — `.github/workflows/sec002-pr-checklist.yml`

**Caminho:** `.github/workflows/sec002-pr-checklist.yml`
**Encoding:** UTF-8, LF, sem BOM.

```yaml
# sec002-pr-checklist
# apply-map: v2026-07-12.4
# consumers: parse-apply-id (parser) + 11 gates + validate-evidence-index (evidence) + test-validate-evidence-index-self-register-guard (evidence-test)
name: sec002-pr-checklist
on:
  pull_request:
    paths:
      - "**"

concurrency:
  group: sec002-pr-checklist-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  checklist:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      PR_TITLE: ${{ github.event.pull_request.title }}
      PR_BODY: ${{ github.event.pull_request.body }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: parse-apply-id  # consumer id="parse-apply-id" category="parser"
        id: parse-apply-id
        run: |
          APPLY_ID="$(node scripts/sec002/parse-apply-id.mjs)"
          echo "APPLY_ID=${APPLY_ID}" >> "$GITHUB_ENV"

      - name: validate-apply-map
        run: node scripts/sec002/validate-apply-map.mjs

      - name: validate-pr-checklist
        run: node scripts/sec002/validate-pr-checklist.mjs

      - name: validate-changed-files
        run: node scripts/sec002/validate-changed-files.mjs

      - name: validate-staging-parity
        run: node scripts/sec002/validate-staging-parity.mjs

      - name: validate-go-no-go
        run: node scripts/sec002/validate-go-no-go.mjs

      - name: validate-comms-log
        run: node scripts/sec002/validate-comms-log.mjs

      - name: validate-dri-matrix
        run: node scripts/sec002/validate-dri-matrix.mjs

      - name: validate-acceptance-gates
        run: node scripts/sec002/validate-acceptance-gates.mjs

      - name: validate-evidence-archive
        run: node scripts/sec002/validate-evidence-archive.mjs

      - name: sec002-consumers-guard
        run: node scripts/sec002/check-consumers.mjs

      - name: sec002-freeze-guard
        run: node scripts/sec002/freeze-guard.mjs

      - name: sec-002-tests
        run: node scripts/sec002/run-tests.mjs

      - name: validate-evidence-index  # consumer id="validate-evidence-index" category="evidence"
        run: node scripts/sec002/validate-evidence-index.mjs

      - name: sec002/test-validate-evidence-index-self-register-guard  # runner T1–T5 (R75)
        if: steps.parse-apply-id.outcome == 'success'
        run: node scripts/sec002/test-validate-evidence-index-self-register-guard.mjs
```

Contratos:

- `parse-apply-id` recebe `id: parse-apply-id` para permitir a guarda `if: steps.parse-apply-id.outcome == 'success'` (P5). Nenhum `run:` é alterado.
- Os 11 gates operacionais e `validate-evidence-index` permanecem literalmente idênticos a R67 §2.
- O novo step é aditivo, final, sem flags, sem `env`, sem `working-directory` e sem permissões extras.
- O comentário de topo é atualizado apenas para acrescentar a nova entrada em `consumers:`; a diretiva `apply-map: v2026-07-12.4` permanece.

## 3. Ordem final congelada

| Ordem | Tipo | Step | Comando |
|---:|---|---|---|
| 0 | pré-gate | `parse-apply-id` | `node scripts/sec002/parse-apply-id.mjs` |
| 1 | apply-map | `validate-apply-map` | `node scripts/sec002/validate-apply-map.mjs` |
| 2 | gate operacional | `validate-pr-checklist` | `node scripts/sec002/validate-pr-checklist.mjs` |
| 3 | gate operacional | `validate-changed-files` | `node scripts/sec002/validate-changed-files.mjs` |
| 4 | gate operacional | `validate-staging-parity` | `node scripts/sec002/validate-staging-parity.mjs` |
| 5 | gate operacional | `validate-go-no-go` | `node scripts/sec002/validate-go-no-go.mjs` |
| 6 | gate operacional | `validate-comms-log` | `node scripts/sec002/validate-comms-log.mjs` |
| 7 | gate operacional | `validate-dri-matrix` | `node scripts/sec002/validate-dri-matrix.mjs` |
| 8 | gate operacional | `validate-acceptance-gates` | `node scripts/sec002/validate-acceptance-gates.mjs` |
| 9 | gate operacional | `validate-evidence-archive` | `node scripts/sec002/validate-evidence-archive.mjs` |
| 10 | gate operacional | `sec002-consumers-guard` | `node scripts/sec002/check-consumers.mjs` |
| 11 | gate operacional | `sec002-freeze-guard` | `node scripts/sec002/freeze-guard.mjs` |
| 12 | gate operacional | `sec-002-tests` | `node scripts/sec002/run-tests.mjs` |
| 13 | gate de evidência | `validate-evidence-index` | `node scripts/sec002/validate-evidence-index.mjs` |
| 14 | gate de evidência (novo) | `sec002/test-validate-evidence-index-self-register-guard` | `node scripts/sec002/test-validate-evidence-index-self-register-guard.mjs` |

Contagem auditável: `1 pré-gate + 1 validate-apply-map + 11 gates operacionais + 2 gates de evidência`.

## 4. Delta auditável sobre R67

```diff
-# consumers: parse-apply-id (parser) + 11 gates + validate-evidence-index (evidence)
+# consumers: parse-apply-id (parser) + 11 gates + validate-evidence-index (evidence) + test-validate-evidence-index-self-register-guard (evidence-test)
```

```diff
       - name: parse-apply-id  # consumer id="parse-apply-id" category="parser"
+        id: parse-apply-id
         run: |
```

Append final:

```yaml
      - name: sec002/test-validate-evidence-index-self-register-guard  # runner T1–T5 (R75)
        if: steps.parse-apply-id.outcome == 'success'
        run: node scripts/sec002/test-validate-evidence-index-self-register-guard.mjs
```

## 5. Procedimento de gravação futura

1. Substituir `.github/workflows/sec002-pr-checklist.yml` pelo conteúdo de §2.
2. Confirmar que `scripts/sec002/test-validate-evidence-index-self-register-guard.mjs` existe conforme R75 e que `INDEX.md` está no estado R77.
3. Rodar localmente:

```bash
node scripts/sec002/validate-apply-map.mjs
node scripts/sec002/check-consumers.mjs
node scripts/sec002/validate-evidence-index.mjs
node scripts/sec002/test-validate-evidence-index-self-register-guard.mjs
node scripts/sec002/freeze-guard.mjs --dry-run
```

Saída esperada:

```text
[sec002/validate-apply-map] OK
[sec002/consumers-guard] OK
[sec002/validate-evidence-index] OK
[sec002/test-validate-evidence-index-self-register-guard] OK T1
[sec002/test-validate-evidence-index-self-register-guard] OK T2
[sec002/test-validate-evidence-index-self-register-guard] OK T3
[sec002/test-validate-evidence-index-self-register-guard] OK T4
[sec002/test-validate-evidence-index-self-register-guard] OK T5
[sec002/test-validate-evidence-index-self-register-guard] OK
[sec002/freeze-guard] OK
```

## 6. Evidência obrigatória

`.github/sec002-evidence/pr-checklist-workflow-self-register-guard-promote-01/manifest.json`:

```json
{
  "sec002_apply_id": "pr-checklist-workflow-self-register-guard-promote-01",
  "schema_version": "v2026-07-12.4",
  "source": "docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.validate-evidence-index-01.self-register-guard-promote-01.materialize-01.md",
  "changed_files": [
    ".github/workflows/sec002-pr-checklist.yml"
  ],
  "checks": [
    "node scripts/sec002/validate-apply-map.mjs",
    "node scripts/sec002/check-consumers.mjs",
    "node scripts/sec002/validate-evidence-index.mjs",
    "node scripts/sec002/test-validate-evidence-index-self-register-guard.mjs",
    "node scripts/sec002/freeze-guard.mjs --dry-run"
  ],
  "hashes": {
    ".github/workflows/sec002-pr-checklist.yml": "<sha256>"
  },
  "refs": ["R38", "R58", "R61", "R63", "R65", "R67", "R72", "R73", "R75", "R77", "R78", "R79"]
}
```

Esta evidência será indexada em apply futuro, preservando E7.

## 7. Critérios de rejeição futura

- Cabeçalho `apply-map` diferente de `v2026-07-12.4`.
- Novo step inserido em posição diferente da 14 (fora do final do job).
- `run:` do novo step com flags, redirecionamentos ou caminho diferente do especificado.
- Ausência da guarda `if: steps.parse-apply-id.outcome == 'success'` (viola P5).
- Ausência de `id: parse-apply-id` (torna P5 inefetivo).
- Reordenar, remover, renomear ou alterar `run:` de qualquer um dos 11 gates operacionais herdados.
- Introdução de `secrets`, `permissions: write-*`, rede externa, `working-directory` ou `env` no novo gate.
- Bump de apply-map, freeze-guard ou `INDEX.md` neste mesmo apply.
- Alteração em `src/**`, `supabase/functions/**` ou `supabase/migrations/**`.

## 8. Impacto em produção

**Nenhum.** Documento de materialização canônica. Não grava workflow executável nesta etapa, não altera dados, não aplica migração, não muda função, não muda frontend e não altera política RLS.
