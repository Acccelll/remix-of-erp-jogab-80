# ARC-004.slice-09 · Responsabilidades → paradigma único TanStack Query

**Onda:** 6  
**Achado-raiz:** ARC-004 (resíduos `useSyncQueryToState`)

## Escopo
- `useResponsabilidadesPatrimonios.ts`: adiciona `useEncerrarResponsabilidade` e `useEncerrarResponsabilidadesDoColaborador` com padrão canônico (`onMutate` snapshot → `onError` restore → `onSettled` invalidate).
- `ResponsabilidadesStateContext.tsx`: leitura direta do cache (`useResponsabilidadesPatrimonios().data ?? []`); `setResponsabilidadesPatrimonios` vira wrapper de `qc.setQueryData`. Operações delegam às mutations otimistas; lógica de patrimônios (via `setPatrimonios`) preservada.
- `useSyncQueryToState.ts`: **removido** (sem consumidores).

## Validação
- `tsgo --noEmit` limpo.
- Assinatura pública `useResponsabilidadesStateContext()` preservada.
- Nenhum resíduo `useSyncQueryToState` no repositório.
