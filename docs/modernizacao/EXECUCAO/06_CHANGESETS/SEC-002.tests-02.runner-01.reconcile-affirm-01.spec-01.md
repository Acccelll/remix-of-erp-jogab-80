# SEC-002 · tests-02 / runner-01 / reconcile-affirm-01 / spec-01

- **Ref:** R205' (afirmação pós-reconciliação, governança pura)
- **Tipo:** especificação (nenhum artefato executável tocado)
- **Onda:** 2 — OPEN
- **Apply-map:** `v2026-07-12.4` (preservado)
- **Antecessores:** R188 (`runner-01.spec-01`), R191, R200'..R203' (D-6 cycle), R204' (`gen-01.reconcile-affirm-01`)

## 1. Objetivo

Afirmar formalmente que, após a materialização de **D-6a** em R203' e a afirmação de `gen-01` em R204', o contrato `SEC-002.tests-02.runner-01.spec-01` **permanece válido byte-a-byte** — sem bump, sem edição, sem novo write set executável — e que os sumários por lote definidos em `runner-01.spec-01 §5` passam a coincidir 1:1 com a distribuição do CSV reconciliado.

Este changeset é **puramente declaratório**: não escreve `scripts/sec002/run-sec-002-tests.sh`, não conecta ao banco, não altera `runner-01.spec-01`, e não avança nenhuma dependência dura (helpers-02/03/assert-01/gen-01.materialize-01).

## 2. Write set deste changeset

| # | Caminho | Ação |
|---|---------|------|
| 1 | `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.tests-02.runner-01.reconcile-affirm-01.spec-01.md` | criação (este arquivo) |

`SEC-001.README.md` recebe append de 1 linha no índice de changesets como registro do round. Nenhum outro arquivo tocado.

## 3. Conciliação de sumário (`runner-01.spec-01 §5.1`)

Sumário canônico esperado por `--batch all` sobre o CSV reconciliado (todos verdes):

```text
=== SEC-002 pgTAP ===
batch-B: 39/39 ok
batch-C: 96/96 ok
batch-D: 70/70 ok
batch-E: 110/110 ok
total:   315/315 ok, 0 FAIL, 0 SKIP, 0 TIMEOUT
```

Distribuição = 13×3 + 24×4 + 14×5 + 22×5 = 39 + 96 + 70 + 110 = **315**, coincidente com R204' §3 e com `GEN01-2`. `solicitacao_comentarios` contribui 4 arquivos em `batch-C/` (`gm`/`member`/`outsider`/`anon`), zero em `batch-D/`.

## 4. Efeito sobre `runner-01.spec-01`

- **§2 CLI** — sem bump; `--batch B|C|D|E|all` inalterado.
- **§4 Execução por arquivo** — sem bump; contrato de isolamento (`--single-transaction`, `ON_ERROR_STOP=1`, `timeout 30s`) preservado.
- **§5.1 Formato humano** — sem bump; contagens já expressas em termos de `X/Y ok`, agora `Y` derivado do CSV reconciliado sem exceção.
- **§5.2 Formato JSON** — sem bump.
- **Dependências duras** — inalteradas: `helpers-02.materialize-01`, `helpers-03.materialize-01` (D-3), `tests-02.assert-01.materialize-01`, `gen-01.materialize-01`, `DATABASE_URL`.

## 5. Invariantes deste changeset (RAR-1 … RAR-8)

- **RAR-1** — `SEC-002.inventory-02.csv` **não é tocado** (baseline `8c18a694442e…d66a404ef` preservado). ✅
- **RAR-2** — `SEC-002.tests-02.runner-01.spec-01.md` **não é tocado** byte-a-byte (sem bump). ✅
- **RAR-3** — `SEC-002.tests-02.gen-01.spec-01.md` **não é tocado** byte-a-byte. ✅
- **RAR-4** — Nenhum `.sh`, `.sql`, `.mjs`, `_lib/`, batch, migration, verifier, `src/`, workflow, edge function ou banco tocado. ✅
- **RAR-5** — Σ sumário declarada (315) = Σ derivada do CSV × spec (315); zero arquivo `batch-D/solicitacao_comentarios__*.sql` esperado. ✅
- **RAR-6** — `INDEX.md` = 45 linhas e `EXPECTED_INDEX_ROWS = 45` preservados. ✅
- **RAR-7** — `SCHEMA_VERSION`, `DEFAULT_APPLY_ID` e `Apply-map: v2026-07-12.4` preservados. ✅
- **RAR-8** — `verify-wave-2-preconditions.mjs --json` permanece `{ ok: true }` com PC1..PC7. ✅

## 6. Delegação aberta

Próximo passo do ciclo de afirmação: `SEC-002.tests-02.gate-01.reconcile-affirm-01.spec-01` (fechar o loop `reconcile → gen → runner → gate` com o gate PC8/PC9 opt-in reafirmado sobre o CSV reconciliado).

Bloqueios materiais do pipeline continuam sendo os dois externos:

1. **GO GM → `helpers-02.deploy-01`** (R193).
2. **D-3 → `helpers-03.apply-01`** (R194).

## 7. Impacto em produção

**Nenhum.** Afirmação documental exclusivamente. Banco, migrations, policies, `_lib/`, batches, scripts executáveis, workflows, `src/` e apply-map permanecem intocados.
