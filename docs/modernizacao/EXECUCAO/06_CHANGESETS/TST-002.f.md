# TST-002.f — Contratos de query dos repositories `cards` + `cardsExtra`

**Onda:** 6 (bloqueada — NO-GO D-9)  
**Data:** 2026-07-12  
**Escopo:** cobertura incremental antes de ARC-005.

## Alvo
- `src/lib/repositories/cards.ts` — `cardsRepo` (getById, update)
- `src/lib/repositories/cardsExtra.ts` — `cardAnexosRepo`, `cardRecursosRepo`,
  `cardLabelsRepo`, `cardLabelLinksRepo`, `cardComentariosRepo`,
  `cardAtividadesRepo`, `cardLocalRepo`, `cardCustomFieldValoresRepo`,
  `cardBoardPosicaoRepo`, `cardGruposNegociacaoRepo`, `cardViewsSalvasRepo`,
  `cardsExtraRepo`.

## Arquivo criado
- `src/lib/__tests__/integracao/repositories-contrato-f.test.ts` — 21 casos.

## Contratos travados
- Colunas explícitas em `cards`/`card_recursos`/`card_labels` (PERF-002).
- `insertMany([])` faz short-circuit sem chamar a rede.
- `upsertByCard` usa `onConflict: "card_id"`; `cardBoardPosicaoRepo.upsertMany` propaga onConflict.
- `getById`/`getFull` usam `.single()` e propagam erro via unwrap.

## Resultado
- Lote F: 21/21 ✔
- Suite total: **530/530** ✔ (68 arquivos)
- `tsgo --noEmit`: verde

## Acumulado TST-002
5 lotes · 13 repositórios · **76 casos**.

## Próximo
`TST-002.g` — `notasFiscaisRepo` + `pedidosComprasRepo` (últimas superfícies antes de destravar sub-onda 5.F).
