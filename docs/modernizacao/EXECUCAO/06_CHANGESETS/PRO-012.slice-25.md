# PRO-012 · slice-25 — Filtro rápido "Somente em piora"

## Escopo
Adicionar filtro rápido acima da tabela principal para exibir apenas
contagens cujo `delta` da série das últimas 10 importações é positivo
(mais ignoradas no fim da janela do que no início).

## Alterações
- `src/pages/suprimentos/ContagensCiclicas.tsx`
  - Novo estado `apenasPiora: boolean`.
  - `useMemo` `rowsExibidas` aplica o filtro sobre `rowsOrdenadas`
    (`serie.length >= 2 && delta > 0`). Preserva a ordenação atual.
  - `useMemo` `totalPiora` conta linhas elegíveis (usado no rótulo do botão
    e para desabilitar quando não há nenhuma piora).
  - Toolbar acima da tabela com botão toggle (`aria-pressed`), contador
    `(N)`, mensagem "Nenhuma contagem em piora no momento." e contador
    global `X de Y` à direita.
  - Bloco da tabela envolto em fragment `<>...</>` para acomodar toolbar +
    tabela sem alterar espaçamento do container pai.

## Contrato de execução
- UI-only, sem Cloud/RLS/schema. Reaproveita `rowsOrdenadas`/`rowsTendencia`.

## Validação
- `bunx tsgo --noEmit` sem erros.
- Manual: com o filtro ativo, tabela mostra apenas contagens em piora;
  ordenação (Δ/Σ do slice-23) permanece respeitada; botão fica desabilitado
  quando nenhuma contagem elegível existe.

## Próximo passo sugerido
`PRO-012.slice-26` — persistir preferências da tabela (sort e filtro) em
`localStorage` para manter estado entre visitas à página.
