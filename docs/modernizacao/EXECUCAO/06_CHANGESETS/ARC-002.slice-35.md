# ARC-002 · Onda 6 · slice-35 — ColaboradoresStateProvider real

## Alterações
- `src/contexts/colaboradores/ColaboradoresStateContext.tsx` (novo): roda
  `useColaboradoresState` (com `getCurrentPlayer` via `useAuthState` e
  `getFuncoes` via `useFuncoesContext`), executa a query `useColaboradores`,
  sincroniza estado, e domicilia `useUpdateColaborador`. Expõe também
  `getColaboradores` estável e `colaboradoresLoaded`.
- `src/contexts/AppContext.tsx`: substitui `useColaboradoresState({...})` e o
  segundo `useUpdateColaborador` por bridge `useColaboradoresStateContext()`.
  Remove `colaboradoresRef` local e `setColaboradores` do payload de
  `useAppQueries`; `dataLoaded` inclui `colaboradoresLoaded`.
- `src/contexts/app/useAppQueries.ts`: enxugado — remove `useColaboradores`,
  tipo `Colaborador`, `setColaboradores` do arg e contribuição em `dataLoaded`.
- `src/App.tsx`: `<ColaboradoresStateProvider>` montado acima de
  `<AppProvider>`, dentro de `<OportunidadesStateProvider>` (após
  `<FuncoesProvider>` para satisfazer a dependência de `funcoes`).

## Validação
- `tsgo --noEmit` sem erros.
- Consumidores continuam usando fachadas (`useApp`, `useColaboradoresContext`).
