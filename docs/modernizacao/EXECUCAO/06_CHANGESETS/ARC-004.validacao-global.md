# ARC-004 · Validação Global — Conclusão

**Onda:** 6

## Escopo
Varredura pós-slice-09 para confirmar erradicação do paradigma duplo:

- `rg "useSyncQueryToState" src/` → **0 ocorrências**
- `rg "contexts/app/use.*State" src/` → **0 ocorrências** (arquivos `use*State.ts` extirpados nos slices 01–08)
- `ls src/contexts/app/` → restam apenas utilitários puros (`helpers`, `mappers`, `useAuthSession`, `useCatalogosMutations`, `useColumnName`, `useExpiringDocuments`, `useHasAccess`, `useMobilizacoes`, `useStableGetter`, `useUpdateColaborador`). Nenhum estado de domínio.
- `tsgo --noEmit` limpo.
- `useState<Domain[]>` remanescentes em `src/pages/**` são exclusivamente estados locais de UI (dialogs, formulários, seleção).

## Pendência derivada (novo achado sugerido)
Páginas de `suprimentos/*` e `planejamento/*` fazem `apiFetch("obras")` direto e mantêm `useState<Obra[]>` local, bypassando `useObras()` cache. Não é regressão do ARC-004, mas viola o princípio de fonte única. Recomendação: abrir achado `ARC-010 · Consumo direto de API fora do cache canônico` na Onda 6.

## Estado do ARC-004
**Concluído.** Paradigma único TanStack Query estabelecido em todos os domínios: obras, colaboradores, patrimonios, veiculos, contratos, funcoes, oportunidades, players, responsabilidades, documentosTipo.
