# PRO-015 — NOTA-FIM (retroativa, fase 1)

**Onda:** 7 · **Achado:** PRO-015 — Importação TOTVS manual e periódica (sem agendamento)
**Slices:** `PRO-015.slice-01`
**Status:** ✅ ENCERRADO (fase 1 — cadência explícita + alerta; agendamento real diferido)

## Escopo entregue

- **`src/lib/storage/keys.ts`** — chave canônica `gestaobra:fin:importCadencia` (EST-003).
- **`src/hooks/useTotvsImportStatus.ts`** (novo)
  - `useImportCadencia()` — lê/grava cadência (`semanal | quinzenal | mensal`) em localStorage; evento `gestaobra:fin:cadencia:changed` sincroniza abas. Default `mensal`.
  - `useTotvsImportStatus(enabled)` — combina `financeiroRepo.getLatestSnapshotMeta()` com a cadência: `{ultimaImportacao, proximaPrevista, diasParaProxima, atrasoDias, status: em_dia | vence_hoje | atrasada | sem_snapshot}`.
- **`src/pages/financeiro/FinImportar.tsx`** — `<CadenciaCard />` com selector shadcn e painel de status colorido (última importação, próxima prevista, mensagem humana).
- **Notificações** (`useNotifPreferencias.ts` + `NotificationBell.tsx`) — nova categoria `totvs_import_atrasado`; bell soma `+1` quando status é `atrasada`/`vence_hoje`; seção com `DatabaseZap` link para `/financeiro/importar`, visível só para `financeiro`/`compras`/GM.

## Validação

- `bunx tsgo --noEmit` limpo.
- Default `mensal` preserva comportamento anterior — sem breaking change.

## Aceite parcial

**Coberto:** periodicidade esperada explícita, configurável, com alerta ao operador quando o snapshot excede o intervalo.

**Débitos residuais (fase 2 — Onda 8, trilho Integrações):**

- Agendamento real (edge function cron chamando endpoint TOTVS).
- Cadência por empresa/obra (hoje única por dispositivo).
- Dashboard histórico de aderência (SLA da importação).

## Rastreabilidade

- Achado: `docs/modernizacao/00_INVENTARIO/PRO-015`.
- Slice: `PRO-015.slice-01.md`.
- Registro de conclusão: `05_ONDAS/ONDA_07/10_Checklist_Conclusao.md`.
