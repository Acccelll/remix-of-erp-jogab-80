# ARC-002 · Onda 6 · slice-32 — ObrasProvider real

## Alterações
- `src/contexts/ObrasContext.tsx`: real; roda `useObrasState` + query `useObras`,
  sincroniza estado, expõe `obras`+CRUDs e `obrasLoaded`.
- `src/contexts/AppContext.tsx`: substitui `useObrasState()` por `useObrasContext()`;
  remove `setObras` de `useAppQueries`; `dataLoaded` inclui `obrasLoaded`.
- `src/contexts/app/useAppQueries.ts`: remove `useObras`, tipo `Obra`, `setObras`
  do arg, a query e contribuição em `dataLoaded`.
- `src/App.tsx`: `<ObrasProvider>` promovido para acima do `<AppProvider>`
  (dentro de `<PlayersProvider>`); instância antiga abaixo removida.

## Validação
- `tsgo --noEmit` sem erros.
- `useObrasContext()` e `useApp().obras/...` continuam funcionando via bridge.
