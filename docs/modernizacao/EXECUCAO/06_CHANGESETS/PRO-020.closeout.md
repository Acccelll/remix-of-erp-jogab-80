# PRO-020 · closeout — Cronograma de recorrência de contratos

**Onda:** 08 · **Achado:** PRO-020 · **Status:** concluído.

## Slices entregues
- **slice-01** — Módulo puro `projetarRecorrencia` (contratos × meses) com testes.
- **slice-02** — Aba **Cronograma** no `ContratosHub` com matriz obra × mês.
- **slice-03** — Seletor de horizonte 3/6/12/24 meses no cronograma.
- **slice-04** — Persistência do horizonte em `localStorage` (`gestaobra:contratos:cronograma:horizonte`).
- **slice-05** — Export XLSX por contrato individual (`projetarPorContratoMes` + `cronograma_por_contrato_<H>m_*.xlsx`).
- **slice-06** — Filtros locais **Status** / **Obra** no cronograma, aplicados também aos exports agregado e por contrato.

## Fora do escopo (não regride)
- Escrita em Financeiro (D-8 mantido).
- Persistência dos filtros locais do cronograma (Status/Obra) — decisão consciente para não poluir preferências entre sessões.
- Sync entre abas do horizonte persistido.

## Validação
- `bunx tsgo --noEmit` limpo em todos os slices.
- `vitest run recorrencia.test.ts` — 5/5.
- Módulos puros (`projetarRecorrencia`, `projetarPorContratoMes`, `filtrarContratosCronograma`) isolados e reutilizáveis por outros módulos.

## Próximo achado
`PRO-030.slice-02` — expandir catálogo de notificações (NC + snapshot velho) sobre o framework já parametrizado em slice-01.
