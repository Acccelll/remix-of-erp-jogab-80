# SEC-002.wave-2-formal-open-01.pc6-scope-01.dri-package-01.materialize-01 — Materialização PC6

**Onda:** 2 (bloqueada — PC1..PC6 OK após este apply; PC7 pendente)  
**Rodada:** R122 (E4 — materialização do pacote DRI)  
**Standard:** E5  
**Precedente autorizante:** `SEC-002.wave-2-formal-open-01.pc6-scope-01.dri-package-01.spec-01.md` (R121).  
**Escopo:** materializar registro DRI, validador físico e manifest — satisfazer `E_WAVE2_PC6`.

---

## 1. Artefatos criados

| Caminho | Ação |
|---------|------|
| `.github/sec002-dri/wave-2-formal-open-01.md` | criado — GM `Cappucceno`, DRI técnico `Lovable` |
| `scripts/sec002/validate-dri-matrix.mjs` | criado — ESM puro, regras D1–D10, soft-null em `loadApplyMap()` |
| `.github/sec002-evidence/wave-2-formal-open-01-pc6-dri-package-01/manifest.json` | criado — R122 self-registered |
| `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.wave-2-formal-open-01.pc6-scope-01.dri-package-01.materialize-01.md` | este documento |
| `docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-001.README.md` | linha 115 — registro executivo R122 |

Nenhum outro arquivo tocado.

---

## 2. Aderência ao write set R121

Confirmado 1:1 com o write set autorizado da Seção 2 de R121. Nenhum caminho fora da lista.

---

## 3. Contrato do validador

`scripts/sec002/validate-dri-matrix.mjs`:

- shebang `#!/usr/bin/env node`;
- imports `readFileSync`/`existsSync` de `node:fs`;
- import `loadApplyMap` de `./load-apply-map.mjs`;
- CLI `--apply-id <id> [--file <path>]`;
- parser YAML mínimo próprio, sem dependências externas;
- regras D1–D10 (frontmatter, `sec002_apply_id`, `matrix_version`, papéis, segregação GM, aprovações, janela ISO-8601, `evidence_refs`, marcadores canônicos, placeholders);
- `loadApplyMap()` em modo soft-null — ausência do apply-map não bloqueia validação documental de PC6;
- exit `0` OK, `1` violação D-*, `2` erro operacional `E_DRI_OP*`.

`verifyPC6()` **não foi alterado**: continua a verificar apenas presença do DRI, `DRI GM: Cappucceno`, DRI técnico distinto e existência física do validador.

---

## 4. Validação executada

```
node scripts/sec002/verify-wave-2-preconditions.mjs --dry-run --only=PC6
node scripts/sec002/verify-wave-2-preconditions.mjs --dry-run --only=PC1,PC2,PC3,PC4,PC5,PC6
```

Ambos retornam `E_WAVE2_PC* = OK`.

---

## 5. Invariantes PC6P1–PC6P14

Todas satisfeitas:

- PC6P1 — R121 não gravou artefatos (apenas spec).
- PC6P2 — DRI em caminho exato.
- PC6P3/PC6P4 — corpo contém `DRI GM: Cappucceno` e `DRI técnico: Lovable`.
- PC6P5 — DRI técnico distinto de `Cappucceno`.
- PC6P6 — frontmatter com `gm_approver.primary: Cappucceno` e `roles.dri_technical.primary: Lovable`.
- PC6P7 — validador físico criado.
- PC6P8 — validador não lê `.github/sec002-apply-map.json` diretamente (usa `loadApplyMap`).
- PC6P9 — `verifyPC6()` intocado.
- PC6P10 — `EXPECTED_INDEX_ROWS`, `SCHEMA_VERSION`, apply-map e `INDEX.md` inalterados.
- PC6P11 — nenhum workflow promovido.
- PC6P12 — PC7 permanece pendente; Onda 2 não aberta.
- PC6P13 — dry-run PC6 isolado e PC1..PC6 validados.
- PC6P14 — Onda 2 permanece **bloqueada** por PC7.

---

## 6. Impacto em produção

**Nenhum.** Não altera app, banco, auth, funções, frontend, permissões, workflows, apply-map ou `INDEX.md`.
