# ARC-012.slice-04 — Migração de leituras auxiliares de `insumos`

**Onda:** 6 · **Achado:** ARC-012 · **Slice:** 04

## Alterações
- **`src/pages/suprimentos/Estoque.tsx`**: `insumosRepo.listAtivosBasico()` → `useInsumosAtivosBasico()`. `insumos` deixa de fazer parte do `load()` local; `Promise.all` reduzido a saldos + movimentações.
- **`src/pages/suprimentos/CurvaABC.tsx`**: `insumosRepo.listResumoPrecos()` → `useInsumosResumoPrecos()`. `load()` local carrega apenas orçamento + itens de composição.
- **`src/pages/suprimentos/Composicoes.tsx`**: `insumosRepo.listAtivos()` → `useInsumosAtivos()`. `load()` local mantém composições + itens.

## Comportamento
Sem alterações funcionais. Leituras de insumos passam a compartilhar cache com `Insumos.tsx` e `Orcamento.tsx` (invalidação automática após mutações).

## Validação
- `tsgo --noEmit`: limpo.
- `rg "insumosRepo" src/` fora de `hooks/useInsumos.ts`, `lib/repositories/insumos.ts` e `__tests__/`: sem ocorrências.

## Próximo passo
`ARC-012.slice-05` — marcar `insumosRepo` como `@internal` (mesmo padrão de `ARC-011.slice-03` para `fornecedoresRepo`) e encerrar ARC-012 na trilha de `insumos`.
