# ARC-013 · Slice 21 — Encapsular `cardAtividadesRepo` em `useCards`

## Escopo
Migrar leituras/inserts de `cardAtividadesRepo` para hooks/queryFns canônicos em `src/hooks/useCards.ts`, removendo imports diretos em `CardGenericoDialog` e `MoverCopiarCardMenu`.

## Alterações
1. **`src/hooks/useCards.ts`**
   - Import interno de `cardAtividadesRepo`.
   - `cardsKeys.atividades(cardId)` (`["card-atividades", cardId]`).
   - `cardsQueryFns.listAtividadesByCard`.
   - Helper `invalidateAtividades`. Mutation `useInsertCardAtividade`.

2. **`src/components/cards/CardGenericoDialog.tsx`**
   - Remove `cardAtividadesRepo` do import de `cardsExtra`.
   - Query usa `cardsKeys.atividades` + `cardsQueryFns.listAtividadesByCard`.
   - `logAtividade` usa `useInsertCardAtividade`.

3. **`src/components/cards/MoverCopiarCardMenu.tsx`**
   - Remove import direto; ações `movido_board` e `copiado_de` usam `useInsertCardAtividade`.

## Validação
- `tsgo --noEmit`: limpo.
- `rg cardAtividadesRepo src`: apenas `useCards.ts` (canônico), repo em `cardsExtra.ts` e teste de contrato (`repositories-contrato-f.test.ts`).
- Semântica preservada.

## Próximo passo
`ARC-013.slice-22` — encapsular `cardCustomFieldValoresRepo` (leituras/upserts) em hooks/queryFns canônicos e migrar `CardGenericoDialog`.
