# ARC-002.slice-38 · Onda 6 — Extração de `contratos` para `ContratosStateProvider`

## Escopo
- Novo `src/contexts/contratos/ContratosStateContext.tsx`: domicilia `useContratosState` + query `useContratos` com `useSyncQueryToState`; expõe `contratos`, `setContratos`, CRUDs, `contratosLoaded`, `getContratos` (getter estável).
- `AppContext.tsx`: consome `useContratosStateContext()`; usa `getContratos` como getter estável para `useMobilizacoes`. `dataLoaded` compõe `contratosLoaded`.
- `useAppQueries.ts`: remove `useContratos`/`setContratos` (agora no provider); permanece apenas com `responsabilidadesPatrimonios`.
- `App.tsx`: `<ContratosStateProvider>` promovido para acima do `<AppProvider>` (dentro de `<VeiculosStateProvider>`).

## Validação
- `tsgo --noEmit`: limpo.
- Consumidores do `ContratosProvider` (fachada) e `useApp().contratos/...` continuam funcionando via bridge.

## Próximo
`ARC-002.slice-39` — extrair `responsabilidadesPatrimonios` para provider próprio ou encerrar a frente de dados remotos residuais do `useAppQueries`, conforme contrato da Onda 6.