# ARC-012.slice-05 — `insumosRepo` marcado como `@internal`

**Onda:** 6 · **Achado:** ARC-012 · **Slice:** 05

## Alteração
- **`src/lib/repositories/insumos.ts`**: JSDoc `@internal ARC-012` no `insumosRepo`, listando os oito hooks públicos em `@/hooks/useInsumos` e reconhecendo os testes de contrato (`repositories-contrato-b.test.ts`, `repositories-contrato-l.test.ts`) como única exceção. Sem alteração de runtime.

## Validação
- `tsgo --noEmit`: limpo.
- Sem consumidores runtime fora de `useInsumos.ts`.

## Encerramento
Trilha `insumos` de ARC-012 encerrada. Padrão ARC-010/ARC-011 replicado.

## Próximo passo
`ARC-012.slice-06` — iniciar mapeamento e encapsulamento de `clientesRepo` (`useObrasFinanceiro`, `EditObraDialog`, `FinObras`, `FinClientes`, `FinFluxo`), começando pela criação de `src/hooks/useClientes.ts`.
