# ARC-013 · slice-11 — `cardsRepo.listKanbanByIds` via `cardsQueryFns`

**Onda:** 6 (ARC-013)
**Escopo:** encapsular a leitura remanescente `listKanbanByIds` em
`QuadroKanban.tsx`, eliminando o último import direto de `cardsRepo` em
componentes.

## Alterações

### `src/hooks/useCards.ts`
- `cardsQueryFns.listKanbanByIds(ids, obraId?)` — expõe leitura preservando o
  shape original `{data, error}` do Supabase (usada em `Promise.all` com
  outros repos dentro da query composta do kanban).

### `src/components/cards/QuadroKanban.tsx`
- Remove import direto de `cardsRepo`.
- Query composta usa `cardsQueryFns.listKanbanByIds(ids, obraId)`.
- Import consolidado: `import { cardsQueryFns, useUpdateGrupoNegociacaoCards } from "@/hooks/useCards"`.

## Validação

- `bunx tsgo --noEmit` — sem erros.
- `rg "from ['\"]@/lib/repositories/cards['\"]" src` retorna apenas
  `src/hooks/useCards.ts` (canônico) e o teste de contrato — objetivo do
  ARC-013 atingido para `cardsRepo`.

## Próximo passo

`ARC-013.slice-12` — auditar próximo repo mais consumido em componentes
(candidatos: `cardsExtraRepo`/`cardSetoresRepo`/`cardLabelsRepo` etc.),
selecionar o de maior dispersão e criar hook canônico correspondente.
