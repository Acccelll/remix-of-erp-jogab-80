# ARC-013.slice-03 — Migração de `Requisicoes.tsx` para `useRequisicoes`

**Onda:** 6 · **Achado:** ARC-013 · **Slice:** 03

## Alterações
- **`src/pages/suprimentos/Requisicoes.tsx`**:
  - Import de `requisicoesRepo` substituído por `useRequisicoesAll`, `useCreateRequisicao`, `useUpdateRequisicaoStatus`.
  - `requisicoesQuery.data` sincronizado em `items` via `useEffect` para preservar a UI existente (filtros, previews, etc.).
  - `load()` passou a chamar `requisicoesQuery.refetch()` (mantém orçamento como fetch imperativo local).
  - `handleSave` → `createMut.mutateAsync(payload)`; `changeStatus` → `statusMut.mutateAsync({ id, status })`. `saving` deriva de `createMut.isPending`. Toasts de erro emitidos pelos hooks; sucesso mantido na página.

## Comportamento
Sem regressão funcional. Cache passa a ser compartilhado com futuras páginas que consumirem `useRequisicoesAll`.

## Validação
- `tsgo --noEmit`: limpo.
- `rg "requisicoesRepo" src/pages/suprimentos/Requisicoes.tsx` → sem ocorrências.

## Próximo passo
`ARC-013.slice-04` — migrar `Cotacoes.tsx` (`updateStatus`), `OrdensCompra.tsx` (leitura via `listAll`) e `Restricoes.tsx` (`listResumoObra`) para os hooks correspondentes.
