# ARC-010.slice-03 · Suprimentos → cache canônico de obras

**Onda:** 6  
**Achado-raiz:** ARC-010 (consumo direto de API fora do cache canônico)

## Escopo
- `src/pages/suprimentos/Requisicoes.tsx`
- `src/pages/suprimentos/Cotacoes.tsx`
- `src/pages/suprimentos/OrdensCompra.tsx`
- `src/pages/suprimentos/Recebimento.tsx`
- `src/pages/suprimentos/Estoque.tsx`

## Mudança aplicada
- Remove `obrasRepo.listResumo()` dos carregamentos paralelos locais.
- Remove `useState<Obra[]>`/`setObras` como estado de domínio local.
- Deriva `obras` exclusivamente de `useObras().data`, mantendo shape local `{ id, nome }` para preservar filtros, selects, mapas `obraById` e dialogs existentes.
- Mantém os `load()` locais responsáveis apenas pelos dados específicos de suprimentos.

## Validação esperada
- `tsgo --noEmit` limpo.
- Zero `obrasRepo.listResumo()` remanescente nas páginas migradas neste slice.
- Comportamento preservado: filtros por obra, descrições de requisições/cotações/OCs/recebimentos e diálogo de transferência continuam alimentados pela fonte canônica.