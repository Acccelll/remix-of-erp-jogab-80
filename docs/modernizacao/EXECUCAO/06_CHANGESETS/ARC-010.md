# ARC-010 — Remover `services/` vestigial e reposicionar `dpHoleriteRepo`

- **Achado:** ARC-010 (P2, ISOLADA, Onda 3)
- **Categoria:** Arquitetura Frontend
- **Referência:** [Etapa 4](../../GOVERNANCA/01_AUDITORIA/ETAPA_04_ARQUITETURA_FRONTEND.md)

## Mudanças

- Movido `src/lib/dp/holerite-repo.ts` → `src/lib/repositories/dpHolerite.ts` (alinha ao padrão único de acesso a dados em `lib/repositories/`).
- Atualizados os 4 imports (`useDpHolerites`, `Fopag`, `ImportHoleriteDialog`, `HoleriteDetailDialog`) e o barrel `src/lib/dp/index.ts`.
- Ajustado import interno de `parser-holerite-xls` para caminho absoluto (`@/lib/dp/parser-holerite-xls`).
- Removido diretório `src/services/` (contendo apenas `colaboradorService.ts` vestigial, sem consumidores).

## Critérios de aceite

- [x] `rg "@/services"` retorna vazio no código-fonte.
- [x] `rg "@/lib/dp/holerite-repo"` retorna vazio (fora de comentário).
- [x] `tsgo` verde.
- [x] Comportamento funcional preservado (apenas re-localização + limpeza).

## Riscos

- Nenhum: refactor puro de caminho de módulo.
