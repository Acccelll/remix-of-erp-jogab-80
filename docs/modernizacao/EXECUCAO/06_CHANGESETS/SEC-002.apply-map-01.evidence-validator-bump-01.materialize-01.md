# SEC-002.apply-map-01.evidence-validator-bump-01.materialize-01 — Materialização canônica do apply-map `v2026-07-12.4`

**Onda:** 2 (bloqueada — depende de `SEC-001` fechar e da Onda 2 ser formalmente aberta)
**Data:** 2026-07-12
**Depende de:**
- `SEC-002.apply-map-01.materialize-01` (R41) — JSON + loader base;
- `SEC-002.apply-map-01.version-bump-01.materialize-01` (R47) — versão `v2026-07-12.2` (DRI);
- `SEC-002.apply-map-01.parser-bump-01.materialize-01` (R53) — versão vigente `v2026-07-12.3`;
- `SEC-002.evidence-index-01.materialize-01` (R59) — conteúdo canônico do `INDEX.md`;
- `SEC-002.validate-evidence-index-01.materialize-01` (R61) — conteúdo canônico do validador;
- `SEC-002.apply-map-01.evidence-validator-bump-01.spec-01` (R62) — contrato canônico deste bump.

**Escopo:** materializar, em conteúdo canônico versionado neste documento, o próximo delta do apply-map (`v2026-07-12.3 → v2026-07-12.4`), do loader e do validador, registrando `scripts/sec002/validate-evidence-index.mjs` como consumidor auditado (categoria `evidence`) e implementando as invariantes M17–M19 definidas em R62. **Nenhum arquivo de produção é gravado nesta etapa.** Gravação física permanece bloqueada até a abertura formal da Onda 2.

## 1. Racional

R61 congelou o conteúdo do validador do índice de evidências e R62 fixou o contrato do bump. Falta materializar o conteúdo exato dos três arquivos afetados, para que a gravação futura seja mecânica e verificável por hash.

## 2. Delta canônico de `.github/sec002-apply-map.json`

Aplicar exatamente estes deltas sobre o JSON pós-R53.

### 2.1 Metadados

```diff
-  "schema_version": "v2026-07-12.3",
+  "schema_version": "v2026-07-12.4",
-  "previous_version": "v2026-07-12.2",
+  "previous_version": "v2026-07-12.3",
```

### 2.2 Changelog

Inserir no topo de `changelog[]`:

```json
{
  "id": "bump-2026-07-12.4",
  "summary": "Registra validate-evidence-index.mjs como consumidor auditado do apply-map",
  "refs": ["R58", "R59", "R60", "R61", "R62"]
}
```

Entradas anteriores permanecem intactas.

### 2.3 Categorias

`categories[]` permanece inalterado em relação a R53:

```json
"categories": [
  "checklist",
  "go-no-go",
  "comms",
  "dri",
  "acceptance",
  "evidence",
  "errata",
  "freeze",
  "consumers",
  "parser"
]
```

Nenhuma categoria nova é introduzida; `validate-evidence-index` reutiliza `evidence`.

### 2.4 Consumidores

Anexar no final de `consumers[]`, após `parse-apply-id`:

```json
{
  "id": "validate-evidence-index",
  "path": "scripts/sec002/validate-evidence-index.mjs",
  "category": "evidence",
  "invariants": ["R1", "R2", "R3", "R4", "R5", "R6", "R7", "E1", "E2", "E3", "E4", "E5", "E6", "E7", "E8"],
  "invoked_by": [".github/workflows/sec002-pr-checklist.yml#validate-evidence-index"],
  "outputs": ["[sec002/validate-evidence-index] OK"]
}
```

Ordem final de `consumers[].id`: `validate-pr-checklist`, `validate-changed-files`, `validate-staging-parity`, `validate-go-no-go`, `validate-acceptance-gates`, `validate-evidence-archive`, `validate-dri-matrix`, `validate-errata`, `validate-post-rls-handoff`, `sec002-freeze-guard`, `sec002-pr-checklist`, `parse-apply-id`, `validate-evidence-index`.

### 2.5 Caminhos protegidos

Anexar em `protected_paths[]`, exatamente uma vez cada:

```json
"scripts/sec002/validate-evidence-index.mjs",
".github/sec002-evidence/validate-evidence-index-01/**",
".github/sec002-evidence/INDEX.md"
```

O terceiro caminho formaliza a proteção do índice canônico de evidências, complementando a materialização de R59.

## 3. Delta canônico do loader

Arquivo: `scripts/sec002/load-apply-map.mjs`.

```diff
-export const SCHEMA_VERSION = "v2026-07-12.3";
+export const SCHEMA_VERSION = "v2026-07-12.4";
```

`VALID_CATEGORIES` permanece inalterado (`evidence` já é válido). Adicionar regra no normalizador, imediatamente após a regra `parser` introduzida em R53:

```js
if (
  c.category === "evidence" &&
  typeof c.id === "string" &&
  c.id.startsWith("validate-") &&
  (!Array.isArray(c.outputs) || c.outputs.length === 0)
) {
  throw new Error(
    `E_APPLYMAP_EVIDENCE_OUTPUTS: consumidor ${c.id} category=evidence exige outputs[] não vazio`
  );
}
```

Nenhuma outra chave do loader muda. `getApply()` e `loadApplyMap()` mantêm assinatura.

## 4. Delta canônico do validador

Arquivo: `scripts/sec002/validate-apply-map.mjs`. Adicionar, após M16:

```js
// M17 — validate-evidence-index existe exatamente uma vez, com path/category/outputs/invariants corretos
{
  const hits = map.consumers.filter(c => c.id === "validate-evidence-index");
  if (hits.length !== 1) {
    fail("E_APPLYMAP_M17", `validate-evidence-index deve aparecer 1x em consumers[], encontrado ${hits.length}`);
  } else {
    const c = hits[0];
    if (c.path !== "scripts/sec002/validate-evidence-index.mjs")
      fail("E_APPLYMAP_M17", `validate-evidence-index.path inválido: ${c.path}`);
    if (c.category !== "evidence")
      fail("E_APPLYMAP_M17", `validate-evidence-index.category inválido: ${c.category}`);
    for (const e of ["E1","E2","E3","E4","E5","E6","E7","E8"]) {
      if (!Array.isArray(c.invariants) || !c.invariants.includes(e))
        fail("E_APPLYMAP_M17", `validate-evidence-index.invariants ausente: ${e}`);
    }
    if (!Array.isArray(c.outputs) || c.outputs.length === 0)
      fail("E_APPLYMAP_M17", `validate-evidence-index.outputs[] obrigatório`);
  }
}

// M18 — ordem canônica final de consumers[]
{
  const expected = [
    "validate-pr-checklist",
    "validate-changed-files",
    "validate-staging-parity",
    "validate-go-no-go",
    "validate-acceptance-gates",
    "validate-evidence-archive",
    "validate-dri-matrix",
    "validate-errata",
    "validate-post-rls-handoff",
    "sec002-freeze-guard",
    "sec002-pr-checklist",
    "parse-apply-id",
    "validate-evidence-index"
  ];
  const actual = map.consumers.map(c => c.id);
  if (JSON.stringify(actual) !== JSON.stringify(expected))
    fail("E_APPLYMAP_M18", `consumers[] fora da ordem canônica`);
}

// M19 — protected_paths[] cobre validador, evidência e INDEX.md, 1x cada
{
  const need = [
    "scripts/sec002/validate-evidence-index.mjs",
    ".github/sec002-evidence/validate-evidence-index-01/**",
    ".github/sec002-evidence/INDEX.md"
  ];
  for (const p of need) {
    const n = map.protected_paths.filter(x => x === p).length;
    if (n !== 1) fail("E_APPLYMAP_M19", `protected_paths[] deve conter ${p} exatamente 1x (encontrado ${n})`);
  }
}
```

Contrato de saída permanece: `[sec002/validate-apply-map] OK` em sucesso, `[sec002/validate-apply-map] FAIL` com códigos `E_APPLYMAP_M<n>` em falha.

## 5. Compatibilidade com consumidores

| Consumidor | Efeito do bump |
|---|---|
| `validate-evidence-index.mjs` | passa a ser listado em `consumers[]`; `sec002-consumers-guard` valida R1–R7 contra ele |
| `sec002-freeze-guard` | passa a proteger `scripts/sec002/validate-evidence-index.mjs`, `.github/sec002-evidence/validate-evidence-index-01/**` e `.github/sec002-evidence/INDEX.md` |
| `parse-apply-id.mjs` | inalterado — permanece em `consumers[]` na penúltima posição |
| demais consumidores | nenhuma alteração — ordem/contagem preservadas |
| `sec002-pr-checklist.yml` | **não é tocado** neste bump (invariante do §7 de R62) |

## 6. Invariantes preservadas

- M1–M16 permanecem inalteradas.
- `totals.applies`, `totals.sum_expected_asserts` e `totals.final_rls_suite_asserts` inalterados.
- Nenhum `apply_id`, `allowed_paths[]` ou `required_evidence_categories[]` de qualquer apply é modificado.
- Categoria `parser` e consumidor `parse-apply-id` preservados exatamente como em R53.

## 7. Procedimento de gravação futura

1. Editar `.github/sec002-apply-map.json` conforme §2.
2. Editar `scripts/sec002/load-apply-map.mjs` conforme §3.
3. Editar `scripts/sec002/validate-apply-map.mjs` conforme §4.
4. Executar localmente:

```bash
node scripts/sec002/validate-apply-map.mjs
node scripts/sec002/check-consumers.mjs
```

Saída esperada:

```text
[sec002/validate-apply-map] OK
[sec002/consumers-guard] OK
```

## 8. Evidência obrigatória

Registrar `.github/sec002-evidence/apply-map-evidence-validator-bump-01/manifest.json`:

```json
{
  "sec002_apply_id": "apply-map-evidence-validator-bump-01",
  "version_before": "v2026-07-12.3",
  "version_after": "v2026-07-12.4",
  "source": "docs/modernizacao/EXECUCAO/06_CHANGESETS/SEC-002.apply-map-01.evidence-validator-bump-01.materialize-01.md",
  "changed_files": [
    ".github/sec002-apply-map.json",
    "scripts/sec002/load-apply-map.mjs",
    "scripts/sec002/validate-apply-map.mjs"
  ],
  "checks": [
    "node scripts/sec002/validate-apply-map.mjs",
    "node scripts/sec002/check-consumers.mjs"
  ],
  "hashes": {
    ".github/sec002-apply-map.json": "<sha256>",
    "scripts/sec002/load-apply-map.mjs": "<sha256>",
    "scripts/sec002/validate-apply-map.mjs": "<sha256>"
  },
  "refs": ["R40", "R41", "R53", "R58", "R59", "R60", "R61", "R62", "R63"]
}
```

## 9. Critérios de rejeição futura

- `schema_version` do JSON e `SCHEMA_VERSION` do loader divergirem, ou qualquer valor diferente de `v2026-07-12.4`;
- `previous_version` diferente de `v2026-07-12.3`;
- `validate-evidence-index` ausente, duplicado, fora de ordem, ou com `category` diferente de `"evidence"`;
- `invariants[]` do consumidor sem qualquer item `E1`–`E8`;
- qualquer caminho de §2.5 ausente ou duplicado;
- validador sem M17, M18 ou M19 (ou sem os códigos `E_APPLYMAP_M17/18/19`);
- workflow `sec002-pr-checklist.yml` alterado neste bump;
- gravação física de `scripts/sec002/validate-evidence-index.mjs` neste changeset;
- qualquer alteração em `src/**`, `supabase/functions/**` ou `supabase/migrations/**`.

## 10. Impacto em produção

**Nenhum.** Documento de materialização canônica. Não grava arquivos executáveis nesta etapa, não altera dados, não aplica migração, não muda função, não muda frontend e não altera política RLS.
